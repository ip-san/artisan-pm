# Redmine 機能パリティ・チェックリスト

`/Users/sesoko/Desktop/workspace/artisan-pm`(本アプリ)と `/Users/sesoko/Desktop/workspace/redmine`(参照元 Redmine)を突き合わせた機能パリティの追跡ドキュメント。計画書 §6 で言及されている実務ドキュメントとして、Phase 0〜6 完了時点(2026-07-21)の状態を初回スナップショットとして作成した。

**凡例**: `done` = 実装済み / `partial` = 部分実装(要注記) / `missing` = 未実装

**運用方針**: 新しい機能を実装したら、該当する行のステータスを更新すること。恒久的に対応しない項目(スコープ外と判断したもの)は削除せず `missing` のまま残し、理由を注記する。

**作業バックログ(2026-09-19)**: 本チェックリストの `missing`/`partial` 行と Redmine 7.0.0 実ソースとの機械照合結果を、実装エージェントが自律ループで処理できる作業単位に再構成したものが [`docs/redmine-gap-backlog.md`](redmine-gap-backlog.md)。項目を完了したら**両方**を更新すること(バックログ §0.3 の状態列と、本チェックリストの該当行)。

**ドキュメント整備タスク着手(2026-07-30、ユーザー依頼のトリガー条件が成立)**: §0.5「ユーザー判断待ち」表のうち、ユーザー本人の判断・承認なしに着手できる項目(2FA必須設定・プロジェクト削除・ユーザー閲覧範囲・LDAP属性マッピング・自己アカウント削除・News/Wiki通知配線・複数リポジトリ 等)を全てdone化。プロジェクトコピーはRedmine本家ソースで規模を実測した上でMercurial/CVS/Bazaar/Filesystemアダプタと同格の「大型据え置き」として正式に対象外決定(詳細は同表の該当行参照)——これにより本チェックリスト上の「ユーザー判断待ち」表は消化完了とみなし、README.md・導入手順・設計解説(Mermaid図を多用)のドキュメント整備に着手する。PDF出力(依存パッケージ承認済み)と`login_required`(実装方針をRedmine本家に忠実な形へ訂正の上ユーザー承認済み)はいずれもその後のセッション内で実装完了 — 詳細は本表の該当行を参照。

**README.md整備時に発見した環境上の既知課題(2026-07-30、advisorレビューで指摘・確認)**: (1) ~~`.env.example`が`DB_CONNECTION=sqlite`のままで実運用とズレていた~~ → **修正済み(2026-07-30)**: `.env.example`を`DB_CONNECTION=pgsql`+`compose.yaml`の`pgsql`サービス(`DB_HOST=pgsql`)・`DB_PASSWORD=password`(`compose.yaml`の`POSTGRES_PASSWORD: '${DB_PASSWORD:-secret}'`フォールバックと一致させる必要があった値)へ直接更新、README.md側の「手動で5項目を編集する」手順自体も不要になったため削除・簡略化。(2) `vendor/bin/sail npm run build`が`Cannot find module './rolldown-binding.linux-arm64-gnu.node'`で実際に失敗することを実機で再確認(既にガントの行(§4)で「別件で発見」として記録済みの同一事象——rolldown-viteのネイティブバインディングがこの環境のアーキテクチャで解決できていない)。原因の根本調査・修正はこのイテレーションのスコープ外として据え置き、README.mdには既知の問題として明記した(未解決のまま)。

---

## 0. 全体サマリー — 優先度の高いギャップ

個別セクションを横断して特に影響の大きいものを先に挙げる。詳細は各セクション参照。

### 構造的なギャップ(単一機能の欠落ではなく、設計レベルの差分)

1. ~~**プロジェクト横断ビューが皆無**(マイページを除く)。~~ → **一部done**(2026-07-22〜29)。News(`news.global-index`、先行実装)に続き、Issues(`issues.global-index`)・TimeEntries(`time-entries.global-index`)・Search(`search.global-index`)・Calendar(`calendar.global-index`)・**Activity(`activity.global-index`、2026-07-29追加)**のトップレベルビューを追加(詳細は §4/§Search 参照)。Ganttは引き続き `/projects/{project}/...` 配下限定のまま。
2. ~~**管理者によるユーザー管理画面が皆無**~~ → **done**(2026-07-21)。`users/{index,form}.blade.php` で一覧/作成/編集/ロック・ロック解除に対応。強制パスワードリセットは「編集画面でパスワード欄に新しい値を入力」で代替(~~専用の「リセットして通知」フローはまだない~~ → `users/form.blade.php`の`sendPasswordReset()`がパスワード再設定リンクをメール送信、2026-09-19訂正)。詳細は §2 参照。
3. **アプリケーション設定が Redmine の 119 キー中わずか 6 項目**(`app_title`, `default_issues_per_page`, incoming-mail 系4項目)。表示/認証ポリシー/通知/添付ファイル制限/リポジトリ設定などのタブが丸ごと存在しない。
4. ~~**カスタムフィールド対応が Issue と Project の2種のみ**~~ → **一部done**(2026-07-22〜23)。Version・Group・TimeEntryActivity・Document・DocumentCategory・**User**にも対応(`CustomizableType::Version`/`::Group`/`::TimeEntryActivity`/`::Document`/`::DocumentCategory`/`::User`)。Version/Documentはプロジェクトのロール経由で可視性を解決、Group/TimeEntryActivity/DocumentCategory/Userは管理者専用リソースのためロールフィルタ自体が不要。Redmineの残り ~2種(主にプラグイン向けの拡張ポイント)は引き続き未対応。
5. ~~**REST API が Projects・Issues・Versions・Issue categories のみ**(Redmine は ~20 リソース群)。~~ → 現状`routes/api.php`は22リソース群(2026-09-19訂正)、残るギャップはバックログA-11参照。~~ファイルアップロード API もなし。~~ → **done**(2026-07-24、詳細は §添付ファイル(アップロードAPI) 参照)。~~API認証も OAuth2(Passport)のみで、スクリプト用途に適した API キー方式が存在しない。~~ → **API キー認証をdone**(2026-07-24、詳細は §API認証 参照)。Issues の DELETE は実装済み(詳細は §Issues REST API 参照)。
6. ~~**Issue のサブタスク(親子)機能がモデルはあるがUIがない**。~~ → **done**(2026-07-21)。課題フォームに親課題ID欄、詳細画面に親リンク/サブタスク一覧を追加(詳細は §サブタスク・親子関係 参照)。~~関連課題(`IssueRelation`)もUIが皆無だった。~~ → **done**(2026-07-21)。課題詳細画面に追加/削除UIを実装(詳細は §Issue Relations 参照)。~~IssueCategory はモデル自体が存在しない。~~ → **done**(2026-07-21)。`IssueCategory` モデル・プロジェクト単位の管理画面・課題フォーム/一覧/フィルタ/CSV連携まで実装(詳細は §Issue Categories 参照)。
7. ~~**添付ファイルが Issue/Version/News/Document にしか付かない**。Wiki ページ・フォーラム投稿に添付できない。サムネイル生成・説明文・ダウンロード数もない。~~ → **done**(2026-07-21〜22)。Wiki/フォーラム投稿への添付、ダウンロード数、説明文、サムネイル生成のすべてに対応済み(詳細は §添付ファイル 参照)。
8. ~~**Wiki の差分表示・リダイレクト・マクロエンジンが丸ごと未実装**。~~ → **done**(2026-07-21〜24)。差分表示・リダイレクトは既に完了(詳細は §Wiki 参照)。マクロエンジンは当初のスコープであった`{{toc}}`/`{{child_pages}}`/`{{collapse}}`/`{{include}}`をすべて実装(詳細は §マクロエンジン全体 参照)。Redmine本家にはこれ以外にも多数のマクロ(`{{macro_list}}`等)があるが、それらは当初スコープ外のまま。
9. ~~**Tracker・IssueStatus に管理画面が皆無**~~ → **done**(2026-07-21)。当初の監査では両方とも「done」と誤って報告されていたが、実際にはモデルのみでルート/画面が一切なく、さらにプロジェクト編集フォームにトラッカー選択欄が無いため UI 経由で作成したプロジェクトは課題を一切作成できない状態だった(トラッカーが0件のため)。この3点をまとめて修正。
10. ~~**ワークフロー遷移・フィールドルールの管理画面が皆無**~~ → **done**(2026-07-21)。`workflows/edit.blade.php` でトラッカー×ロール×適用対象(通常/作成者/担当者)を選んで遷移グリッド・フィールドルールグリッドを編集可能に。新規課題(`old_status_id IS NULL`)の遷移編集は意図的に対象外(`IssueService::create()` のステータス初期値決定がワークフローテーブルを一切参照しないため、現状は編集しても挙動に反映されない — §1 参照)。

### すぐ着手すべき小〜中規模の修正(見つかったバグ・欠落)

- ~~Journal の `private_notes` カラムが存在するのに一切セット/フィルタされていない~~ → **done**(2026-07-21)。`view_private_notes`/`set_notes_private` 権限を新設し配線(詳細は §Journal参照)。
- ~~カスタムフィールドの変更が Journal(監査証跡)に記録されない。~~ → **done**(2026-07-21)。`IssueService::update()` がカスタムフィールド値の設定を取り込み、コア属性の差分と同一Journalに`property: 'cf'`として記録(詳細は §Journal参照)。
- ~~カスタムフィールド値が `searchable => true` でも全文検索にインデックスされていない。~~ → **done**(2026-07-21)。`SearchService::searchIssues()` がScout(subject/description)の結果に`searchable`なカスタムフィールド値(`value_string`/`value_text`へのLIKE検索)をID単位でマージ。詳細は §クエリ/フィルタ/レポートエンジンおよび§検索参照。
- ~~Issue の一覧グルーピングが「現在のページ内のみ」で集計されており、全件SQL集計になっていない。~~ → **done**(2026-07-21)。グループ見出しの件数は`groupTotals()`がSQL `GROUP BY`で全件集計するように変更(表示行自体は引き続き現在のページ内のみ、パフォーマンス上の理由で意図的)。詳細は §クエリ/フィルタ/レポートエンジン参照。

---

## 0.5 残作業ロードマップ(2026-07-22 レビュー・最適化)

残り約98行(missing/partial)を価値×工数×依存関係で精査した結果。**「既存エンジンの再利用で完結する項目を先に、横断基盤が必要な項目は判断点として明示し、設定画面の行は対応機能と同時にしか作らない」**を原則とする。

### 近期キュー(ループ継続対象・この順で1項目ずつ)

| # | 項目 | 根拠 |
|---|---|---|
| ~~1~~ | ~~カレンダー: 開始日/期日マーカー表示~~ → **done**(確認2026-07-29) | `calendar/index.blade.php`の`weeks()`/`issueEntriesBetween()`で▶/◀/◆マーカー実装済み。詳細は§0.5本文/§4「カレンダー」行 |
| ~~2~~ | ~~ガント: クエリフィルタ連動~~ → **done**(確認2026-07-29) | `gantt/index.blade.php`が`InteractsWithQueryFilters`を使用。詳細は§4「ガント」行(2026-07-23) |
| ~~3~~ | ~~検索: all_words/titles_only/オープン課題のみトグル+`#123`ジャンプ~~ → **done**(確認2026-07-29) | `SearchService::search()`/`searchAcrossProjects()`が`allWords`/`titlesOnly`/`openIssuesOnly`引数を実装済み。詳細は§4「検索(モジュール横断)」行(2026-07-22) |
| ~~4~~ | ~~マイページ: 保存済みクエリブロック(issuequery相当)~~ → **done**(確認2026-07-29) | `app/Support/Dashboard/SavedIssueQueryBlock.php`が存在。詳細は§4「マイページ」行(2026-07-23) |
| ~~5~~ | ~~リポジトリ: 任意リビジョン間Diff~~ → **done**(確認2026-07-29) | `ScmAdapter::diff()`が`$fromRevision`/`$path`引数を実装、`repository.compare`ルートも存在。詳細は§4「Diff表示」行(2026-07-22〜23) |
| ~~6~~ | ~~Journal: 添付/関連変更の監査記録~~ → **done**(確認2026-07-29) | `IssueService::journalizeAttachment()`/`journalizeRelation()`実装済み。詳細は§1「属性変更の監査証跡」行(2026-07-22) |
| ~~7~~ | ~~カスタムフィールドの表示列・CSV列対応(課題一覧)~~ → **done**(確認2026-07-29) | `issues/index.blade.php`の`availableColumns()`が`cf_{id}`キーを提供。詳細は§1「表示列・CSV列としてのカスタムフィールド」行(2026-07-22) |
| ~~8~~ | ~~トラッカーの複製~~ → **done**(2026-07-22) | 小。ワークフローコピーが既にあるため残りは属性+CF紐付けのコピーのみ |
| ~~9~~ | ~~プロジェクト作成時のデフォルトモジュール/トラッカー~~ → **done**(2026-07-22) | 中。設定「プロジェクト」タブの行はこの機能と同時に部分消化 |
| ~~10~~ | ~~受信メール設定の拡張+リポジトリ設定(自動フェッチ等)~~ → **done**(2026-07-22) | 小×2。既存settingsパターンの追記 |

### ユーザー判断待ち → 2026-07-30、ユーザーより「100%を目指して続けて」の包括承認取得、全項目着手可

**訂正(2026-07-30)**: 「2FA(TOTP)」行は本テーブルで長らく未着手として記載されていたが、実際には§2「二要素認証(TOTP)」行(313行目)の通りFortify経由で本体機能・管理者による強制無効化まで既に`done`(2026-07-23)。本テーブル自体の更新漏れだった(他の設定タブ行でも過去に複数回発生した「実装済みなのに古いステータスのまま放置」と同種)。`rest_api_enabled`・「2FA必須設定」ともに同日中に実装済み(詳細はそれぞれ「API認証」節・下表参照)。

| 項目 | 判断点 | 状況(2026-07-30時点) |
|---|---|---|
| ~~**メール通知基盤**(最重要判断)~~ → **一部done**(2026-07-29、ユーザー承認済み) | 課題の作成/更新メール通知・通知設定タブ(`notified_events`/`mail_from`/`plain_text_mail`/`emails_footer`)・ユーザーごとの`mail_notification`設定を実装(詳細は§1「メール通知(課題)」参照)。~~Wiki通知配線~~ → **done**(2026-07-30、詳細は§1「メール通知(Wikiページ)」参照)。~~News通知配線~~ → **done**(2026-07-30、詳細は§1「メール通知(News)」参照)。~~メール確認付き登録~~ → **done**(2026-07-30、詳細は下部「登録モード」行参照) | 残り: @mentionミニフェーズ(前提2つが必要、詳細は下表参照) |
| ~~@mentionミニフェーズ~~ → **一部done**(2026-07-30) | 前提2つ(全ユーザー必須のlogin欄+公開プロフィール画面)を含む3段構成なら実現可能。単独では見送り済み(§1「作成者/担当者の自動Watch」行の調査メモ参照) | 前提1・前提2ともにdone(詳細はchecklist §2該当行)。**@mention本体は課題(説明文+コメント)・Wikiページ本文まで実装**(詳細は§1「作成者/担当者の自動Watch・@mention・自動整理」行参照)。News/フォーラムコメントへの拡張は**対象外**(Redmine 7.0.0で`acts_as_mentionable`を持つのはIssue説明・Journalコメント・Wiki本文のみで、Newsコメントとフォーラム投稿は@mention非対応 — 2026-09-20にソースで確認、パリティ上のギャップではない) |
| **`autologin`**(ログイン状態を保持チェックボックス) → **done**(2026-07-30) | 小規模、既存の設定タブパターンで追加 | `Setting::autologin`(既定false、Redmine同様チェックボックス自体を非表示)+`EnforceAutologinSetting`ミドルウェア(`config/fortify.php`のFortifyグローバルミドルウェアに追加、`login.store`ルートでのみ`remember`入力を除去)。Fortifyの`AttemptToAuthenticate`は`$request->boolean('remember')`を直接読むため割り込みフックが無く、入力自体を先に除去する方式を採用。2FA有効ユーザーの場合、Fortifyは`login.store`時点で`remember`をセッションの`login['remember']`にスタッシュし2段階目の`two-factor.login.store`で読み出す設計(`RedirectIfTwoFactorAuthenticatable`/`TwoFactorLoginRequest::remember()`)だが、スタッシュより前の`login.store`側で入力を除去しているため2段階目にも正しく伝播することをE2Eテストで確認済み |
| ~~`rest_api_enabled`~~ → **done**(2026-07-30) | 小規模 | 詳細は「API認証」節参照 |
| ~~「2FA必須設定」(認証設定タブの最後のトグル)~~ → **done**(2026-07-30) | 小規模、既存の設定タブパターンで追加 | Redmine本家の`Setting.twofa`(`0`=無効/`1`=任意/`2`=全ユーザー必須/`3`=管理者のみ必須、文字列値)をそのまま実装。`User::mustActivateTwoFactor()`(Redmineの`User#must_activate_twofa?`相当)+新規`App\Http\Middleware\EnforceTwofaRequired`(`twofa.required`エイリアス、`routes/web.php`の`auth`・`session.timeout`と同じグループに追加)が、対象ユーザーの認証済みリクエストをすべて`profile.index`(2FA設定フォームがある画面)へリダイレクト、`profile.index`自身は無限リダイレクトを避けるため除外。Fortifyの2FA関連ルート(`two-factor.enable`等)はこのアプリの`routes/web.php`グループの外(Fortify自身が別途`auth`ミドルウェアで保護)にあるため影響なし。**グループ単位の必須(2026-09-20に対応)**: Redmine本家の`twofa_optional?`(値`1`/`3`)で「所属グループに`twofa_required`なグループが1つでもあれば強制」する経路を、`groups.twofa_required`列・グループ編集画面のチェックボックス(サイト設定が任意/管理者のみ必須のときだけ有効、それ以外は保存値を保持、Redmineの`groups/_form.html.erb`と同じ)・`User::mustActivateTwoFactor()`の判定追加で実装。テスト: `tests/Feature/Users/GroupTwofaRequiredTest.php`。テストは`User::mustActivateTwoFactor()`の生の返り値(4段階すべて)とミドルウェアのHTTPリダイレクト挙動(profile.index自身が除外されること・未確認のシークレットだけでは免除されないこと・既に2FA有効かつ確認済みのユーザーは対象外になることを含む)の両方を直接アサート。**既知のスコープ境界**: Livewireの`/livewire/update`エンドポイントは`livewire.middleware_group`(`web`)配下で登録されており、本アプリの`['auth','session.timeout','twofa.required']`グループの外にあるため、既にレンダリング済みのページを開いたままのユーザーは管理者が設定を変更した後もそのページ上のコンポーネント操作を継続できてしまう。これは`session.timeout`(既にdone)にも共通する本アプリのミドルウェア設計上の既知の特性であり、今回新規に生じた欠陥ではない |
| ~~`login_required`(ログイン必須化)~~ → **done(2026-07-30)** | **実装前に方針を訂正**: 当初提案した「オンで公開プロジェクトの課題/Wiki閲覧のみ匿名許可」という案は、Redmine本家の`ApplicationController#check_if_login_required`(`return true if User.current.logged?; require_login if Setting.login_required?`)と実は意味が逆(本家は`login_required = false`=既定=匿名に公開プロジェクトを広く許可、`true`=例外なく全ブロック)だったため、忠実路線(既定`true`=現状維持、`false`にした場合のみ匿名到達を許可)に訂正してから実装。**設定**: `Setting::get('login_required', true)`(既定true=本アプリの従来の挙動を維持)、設定タブ「認証」節にチェックボックス追加。**ミドルウェア**: 新規`App\Http\Middleware\EnforceLoginRequiredSetting`(`login.required`エイリアス、`bootstrap/app.php`)——`$request->user() === null && Setting::get('login_required', true)`なら`AuthenticationException`を投げ、`auth`ミドルウェアと同じ挙動(ログイン画面へリダイレクト)を再現。プロジェクトが公開かどうかの判定はこのミドルウェアの責務外で、各Voltコンポーネントの`mount()`が呼ぶ既存のポリシー(`ProjectPolicy`/`IssuePolicy`/`WikiPagePolicy`、いずれも`?User $user`already対応済み)がそのまま担当。**匿名到達可能にしたルート**(`routes/web.php`、対象ルートに個別`->withoutMiddleware('auth')->middleware('login.required')`を付与): `issues.index`/`issues.show`/`issues.pdf`(課題詳細と同じ`IssuePolicy::view`ゲートのため道連れで開放——閲覧できる課題なのにPDFリンクだけログイン画面に弾かれるという不整合を避けるため)、`wiki.index`/`wiki.date-index`/`wiki.show`、`attachments.show`/`attachments.thumb`(課題・Wiki本文の添付ファイル/インライン画像がブラウザから直接読み込めないと画面自体が壊れて見えるため必須)。**ルート登録順を保つための実装判断**: 対象ルートを別グループに切り出す案は却下——`issues/create`等のリテラルセグメントと`issues/{issue}`のワイルドカードが同じグループ内で「特定を先に」という既存の並び順に依存しており、別グループへの抽出はマッチング順序を壊す(`route:list`のコンパイル済み正規表現で実際に確認)。個別ルートへの`->withoutMiddleware()`/`->middleware()`チェーンならファイル内の元の並び順を一切変えずに済むため、この方式を採用。**advisorレビューで発見・修正した実バグ2件(いずれもnullユーザーが初めてこれらの画面に到達したことで露見)**: (a) `issues/show.blade.php`のウォッチボタンが`@can('watch', ...)`のガード無しで無条件に描画されており、`Issue::isWatchedBy(User $user)`(非nullable引数)に`auth()->user()`(null)を渡して`TypeError`——Wiki側の同等ボタンには元々`@can('watch', $wikiPage)`が付いていたのに課題側だけ欠落していた不整合。`@can('watch', $issue)`を追加して解消。(b) `visibleJournals()`(非公開コメントのフィルタ用computed)が`auth()->user()->can(...)`をnull安全演算子なしで直接呼んでおり、ゲスト到達で`Error: Call to a member function can() on null`——`$user !== null &&`のガードを追加(ゲストは常に非公開コメントを一切見られない、という意図した挙動に修正)。**テストで発見した経緯の教訓**: 最初に書いたゲストテストは空のIssue/WikiPage(journal・attachment・relationなど無し)でのアクセスのみを検証しており、上記2件のバグはどちらも通過してしまっていた(advisorレビューで指摘)——journal(公開+非公開)・添付ファイル・関連課題を持つ密なフィクスチャに差し替えて再現・修正確認した上で、テストにも同じ密なフィクスチャを反映済み。テストは`tests/Feature/Authorization/LoginRequiredTest.php`に11件(既定値でのリダイレクト、公開プロジェクトの課題一覧/詳細閲覧、非公開プロジェクトでの拒否、密なフィクスチャでのクラッシュ確認+非公開コメント非漏洩確認、添付ファイル/サムネイルの閲覧可否、匿名ロール自体が未登録の場合の拒否、Wiki一覧/日付順/詳細閲覧、インライン画像付きWikiページでのクラッシュ確認、対象外ルートが変わらずログイン必須のままであることの確認、認証済みメンバーが設定値に関わらず影響を受けないことの確認)、`tests/Feature/Settings/SettingsFormTest.php`に1件追加。**意図的な対象外**: `issues.atom`(Atomフィード購読は「閲覧」ではなく別チャネルの機能とみなし対象外のまま)。プラグインが登録する`x-hook`(`issues.show.details_bottom`等)が匿名到達を前提としない実装になっている可能性は、個々のプラグイン実装側の責務として本タスクのスコープ外(advisorレビューで指摘、対応不要と判断)。**既知の非対称性(advisorレビューで指摘、意図的に未修正)**: `Issue::relevantCustomFields()`は`$user = auth()->user()`が`null`の場合`rolesFor(null, ...)`を呼ばず`collect()`(空)に短絡しているため、公開プロジェクトで`Anonymous`ロールにカスタムフィールド閲覧権限を与えていてもゲストには一切表示されない——`AuthorizationService::can(null, ...)`など他のゲスト経路はすべて`rolesFor(null, $project)`経由で`Anonymous`ロールを正しく参照するのに、この1箇所だけ独自に空へ短絡する非対称な実装になっている。ゲストへの表示が常に「本来より少ない」方向にしか外れないため安全側ではあるが、`rolesFor(null, ...)`に統一する修正は本タスクのテストが検証していないゲスト可視範囲の拡大になるため今回は見送り、既知の挙動として記録するに留める |
| ~~PDF出力(課題/Wiki/ガント)~~ → **done(全4スライス完了、2026-07-30、ユーザーが`barryvdh/laravel-dompdf`追加を個別承認)** | `barryvdh/laravel-dompdf`の依存追加はユーザー承認済み(`composer.json`/`composer.lock`に反映、`vendor:publish`で`config/dompdf.php`も生成) | **課題PDF出力+Wikiページ単体PDF出力+ガントチャートPDF出力+Wiki一括PDF出力を実装**(複数リポジトリ機能で採用したスライス分割方針を踏襲)。`/projects/{project}/issues/{issue}/pdf`(`issues.pdf`)を新設、課題詳細画面に「PDF」リンクを追加。**実装方式はRedmine本家と異なる意図的な簡略化**: Redmine本家(`Redmine::Export::PDF::IssuesPdfHelper#issue_to_pdf`)はTCPDFの座標指定セル描画で1行ずつ手組みするが、本アプリは印刷用に組んだBladeビュー(`resources/views/pdf/issue.blade.php`)をdompdf(HTML/CSS→PDF)でレンダリングする方式を採用——プラグイン設定UIで採用した「個別描画コードでなく汎用レンダラー」という判断と同じ考え方の転用で、対象フィールド自体はRedmine本家のissue_to_pdfと同じ(基本属性・説明・カスタムフィールド・履歴コメント)網羅している。**開発中に発見した重大な問題と対処(すべて実機で経験的に確認)**: (1) dompdfは既定でCJKフォントを一切持たず、`@font-face`でシステムフォントパス(`/usr/share/fonts/...`)を指定しても既定の`chroot`設定(`base_path()`のみ許可)により黙って拒否されHelveticaにフォールバックし、日本語が全て`?`になる不具合を確認(Ghostscriptの`txtwrite`デバイスでPDFからテキスト抽出して検証)。対処として、IPAゴシックフォント(`resources/fonts/ipag.ttf`、IPAフォントライセンスv1.0の下で再配布可、ライセンス全文を`resources/fonts/IPA_Font_License_Agreement_v1.0.txt`として同梱=同ライセンス第3条2項(3)の「本契約の写しを許諾プログラムに添付」要件を充足)をアプリ本体にバンドルし`resource_path()`経由で参照する方式に変更、これでchroot内に収まり動作を確認。(2) dompdfは`<table>`内の要素で`<body>`からの`font-family`継承が不安定なため(実機で確認: 段落内は正しくレンダリングされるがテーブルセル内だけ`?`になる現象)、`* { font-family: 'IPAGothic', sans-serif; }`で全要素に明示指定。(3) 見出し等の太字要素は`font-weight: normal`のフォントフェイスしか登録していないと非CJKフォントにフォールバックするため、同一ttfファイルを`font-weight: normal`/`bold`の両方で登録。(4) `barryvdh/laravel-dompdf`のパッケージ既定設定は`enable_font_subsetting => false`(dompdf本体の既定`true`を上書き)になっており、フォントサブセット化が無効なままIPAゴシック全体(数千字体)を埋め込んだ結果、1ページのPDFが約8.9MBに肥大化することを確認。`config/dompdf.php`を`vendor:publish`した上で`enable_font_subsetting => true`に変更し、実際に使用する字体のみ埋め込むよう修正(同条件で再検証したところ約26KBまで縮小、Ghostscriptでのテキスト抽出でも日本語が正しく読めることを確認)。**advisorレビューで発見・修正**: (a) テストが`%PDF`のマジックバイトのみを確認しており、上記(1)のフォント欠落バグが再発しても検知できない状態だった→dompdfが`/BaseFont`名を圧縮せず生バイトに書き出す性質を利用し、レスポンス本文に`IPAGothic`という文字列が含まれることを直接アサートするテストに変更。(b) 非公開課題へのアクセス拒否テストが実際には「非メンバーだから拒否される」ケースしか検証しておらず、`is_private`ゲート自体を判別するテストになっていなかった→`issues_visibility`が`default`(DB列自体の既定値は`all`のため明示指定が必要)のプロジェクトメンバーで作成者/担当者でもない、という判別可能な設定に修正。(c) `issues.show`の`visibleJournals`は非公開コメントをその投稿者自身には表示するが、当初のPDF実装は非公開コメントを一律除外しており挙動が食い違っていた→`show.blade.php`と同じ「非公開かつ投稿者本人でない場合のみ除外」ロジックに修正。テストは`tests/Feature/Issues/IssuePdfTest.php`に5件(通常ダウンロード+フォント埋め込み確認、非メンバー拒否、非公開課題への限定的アクセス拒否、カスタムフィールド値の反映、非公開コメントの投稿者本人/他メンバー間での表示差異)。CJKフォント埋め込み用の`@font-face`宣言は`resources/views/pdf/issue.blade.php`・`resources/views/pdf/wiki.blade.php`の両方で必要になったため、共通化にあたり本アプリに前例の無かった`@include`ではなく既存の唯一の慣習である`<x-component>`アノニマスコンポーネント(`resources/views/components/pdf/cjk-font.blade.php`、`<x-pdf.cjk-font />`として利用)を採用(laravel-best-practicesスキルのレビューで指摘・修正)。**Wikiページ単体PDF出力も同日中に追加実装(2026-07-30)**: `wiki.show`(`resources/views/livewire/wiki/show.blade.php`)に既存の`exportTxt`/`exportHtml`と同じ並びで`exportPdf()`を追加、`export_wiki_pages`権限で認可(既存の`export`ポリシーをそのまま流用)、`resources/views/pdf/wiki.blade.php`を新規作成。**課題PDFとの実装上の重要な相違点(実機で経験的に発見)**: `IssuePdfController`はdompdfの`Response`をそのまま返すプレーンコントローラ(`<a href>`の実GETリクエストなのでLivewireを経由しない)だが、`wiki.show`の`exportPdf()`はLivewireコンポーネントのアクションであり、同じように生の`Response`を返すと「Malformed UTF-8 characters」エラーでLivewireの内部AJAXレスポンスのシリアライズが壊れることを実機で確認(dompdfが生成するバイナリ本体をLivewireがJSONエンコードしようとして失敗する)。既存の`exportTxt`/`exportHtml`と同じ`response()->streamDownload()`パターンに合わせて回避——「LivewireアクションからのファイルダウンロードはstreamDownload()を使う」という、他の2つの既存メソッドが暗黙に守っていたルールを、今回PDFで初めて破ってエラーを踏んでから学んだ形。テストは`tests/Feature/Wiki/WikiPdfExportTest.php`に3件(ダウンロード+フォント埋め込み確認、権限なしでの拒否、権限なしでのPDFボタン非表示=`assertDontSee('PDF')`という判別不能なアサーションではなく`wire:click="exportPdf"`という実際のゲート対象を直接アサートするようadvisorレビューで修正)。**ガントチャートPDF出力も同日中に追加実装(2026-07-30)**: `gantt.index`(`resources/views/livewire/gantt/index.blade.php`)に`exportPdf()`を追加、`viewGantt`権限で認可、`resources/views/pdf/gantt.blade.php`を新規作成(A4横向き、`setPaper('a4', 'landscape')`)。表示中の課題が1件も無い(`rangeStart === null`)場合は空のPDFを生成せず404を返す。WikiページPDFと同じ理由でLivewireアクションから`streamDownload()`を使用。**開発中に発見・修正した重大なレイアウトバグ(gs文字列抽出だけでは検知できず、Ghostscriptでラスタライズした画像を目視して発見)**: 当初のCSSは行ラベルを`position: absolute`、タイムライン側のバー行を`position: relative`で`top`オフセットする組み合わせだったが、`relative`要素の`top`は元のフロー位置に加算されるだけで置き換わらないため、2行目以降でラベルとバーの行が徐々にズレていく不具合があった(課題1件・マイルストーン1件のみの検証では気づけず、`%PDF`マジックバイトや`IPAGothic`文字列のアサーションだけのテストも通ってしまう=フォント欠落バグと同じ「レンダリングはされるが内容が誤っている」失敗モード)。3課題+マイルストーン1件でPDFをGhostscriptの`png16m`デバイスでラスタライズしてReadツールで目視確認したところズレを確認、ラベル行・タイムライン行の両方を`position: absolute`(`.chart`基準)に統一し、`top`をすべて同じ`行インデックス × ROW_HEIGHT`で計算する方式に修正、再ラスタライズしてバーとラベルが正しく揃うことを確認。**ラスタライズ検証の手順(次のレイアウトを持つPDF出力機能に再利用可能)**: `docker compose exec -T laravel.test bash -c "gs -sDEVICE=png16m -r150 -o /tmp/out.png -dBATCH -dNOPAUSE /tmp/in.pdf"`でコンテナ内でラスタライズ→`docker cp <container>:/tmp/out.png /tmp/local.png`でホストに取り出し→Readツールで画像として目視確認。**課題PDF・WikiページPDFの検証範囲についての既知の限界**: この2つはGhostscriptの`txtwrite`によるテキスト抽出のみで検証しており(文字の可読性は確認済み)、今回発見したような表のセル位置ズレ・ブロック要素のレイアウト崩れの類はテキスト抽出では検出できないため未検証のまま(テスト自体は全てグリーンだが、レイアウトの目視確認という観点では課題/Wiki側は「未確認」であり「問題なし確認済み」ではないことを記録しておく)。**Redmine本家との意図的な相違点**: HTML版のガントチャート(`gantt.index`本体)が描画する先行/ブロック関係の接続線(inline SVGの`<line>`要素)はPDF出力には含めていない——dompdfのSVGサポートが不安定で、CJKフォントと同水準の実機検証(この乖離を踏まえた確認)を今回のスライス内で行いきれなかったため。バー・月次帯・マイルストーンというチャートの主要情報自体には影響しない。テストは`tests/Feature/Gantt/GanttPdfTest.php`に4件(ダウンロード+フォント埋め込み確認、権限なしでの拒否=mount()時点の`viewGantt`認可がexportPdf到達より先に効く、日付未設定課題のみの場合の404、マイルストーンバージョンありなしでのPDFサイズ差分によるレンダリング確認)。**Wiki一括PDF出力も同日中に追加実装(2026-07-30、これで本項目は完了)**: `wiki.index`(`resources/views/livewire/wiki/index.blade.php`)に`exportPdf()`を追加、`resources/views/pdf/wiki-export.blade.php`を新規作成。Redmine本家`WikiController#export`のformat.pdf(`Redmine::Export::PDF::WikiPdfHelper#wiki_pages_to_pdf`、`pages.group_by(&:parent_id)`を使った階層順の再帰書き出し)に相当し、プロジェクトの全Wikiページを1つのPDFに結合する——既存の`exportZip('txt'|'html')`(ページごとに独立したファイルをzip化)とは性質が異なるため、`exportZip`のフォーマット引数を3つ目に拡張するのではなく独立した`exportPdf()`メソッドとして実装。**前回記載していた申し送り事項の訂正**: 「`exportZip`が`'pdf'`を拒否するテストは次スライスで更新が必要」と記載していたが、実際に採用した設計(独立メソッド)では`exportZip`のフォーマット引数自体に一切手を加えていないため、`tests/Feature/Wiki/WikiExportTest.php`の「exportZip rejects an unrecognized format」テストは無改修のまま今も正しく通り続けている(128/128でグリーン確認済み)——予測が外れた理由も含めて記録しておく。**階層順の実装**: 新規private `hierarchicalOrder()`(`parent_id`でグループ化し、ルートページをタイトル順に辿りながらその子孫を深さ優先・再帰的にタイトル順で辿る)が本家の`write_page_hierarchy`相当。ページ間は`page-break-before: always`で1ページ目から区切る。**開発中に発見した実装上の問題(実機で経験的に確認)**: CSSの`:first-child`セレクタと`page-break-before`の組み合わせがdompdfで確実に効かず(先頭エントリだけ改ページを回避しようとしたが、実際にラスタライズして確認したところプロジェクト名だけの実質空白1ページが残ってしまった)、Bladeの`$loop->first`で先頭エントリにだけ`page-section`クラスを外す方式に変更して解決(修正前後をそれぞれGhostscriptの`png16m`デバイスでラスタライズし、`docker cp`でホストに取り出してReadツールで目視比較して確認)。**既知の制約(4つのPDF出力すべてに共通、advisorレビューで指摘され今回検証)**: Wikiページ本文の埋め込み画像(`![img](attachment:...)`)は、Markdownレンダラーが`route('attachments.show', $media)`という認証必須のアプリ内URLに変換するが、dompdfは`enable_remote => false`のためこの画像を取得できない。実機検証の結果、例外は発生せず(添付ファイル付きページでのPDF生成に成功)、Ghostscriptでラスタライズして目視確認したところ画像領域自体は完全に空白になる一方で、`<img alt="...">`のalt属性値がその場にプレーンテキストとして描画されることを確認した(例: `![img](wiki-test.png)`→画像は表示されず「img」という文字がそのまま本文中に残る)。ブラウザの壊れた画像アイコンのような視覚的な破損表示にはならないが、「画像抜きの本文」ではなく「altテキストが地の文に紛れ込む」という見た目になる点はクラッシュではないにせよ完全に無害とは言えないため、静かな欠落として4つのPDF出力すべてに共通する既知の制約として記録する。**性能上の注意点**: Wiki一括PDF出力は全ページのMarkdownレンダリング+dompdf変換を同期的なLivewireアクション1回の中で行うため、非常にページ数の多いWikiでは処理時間が問題になりうる(Redmine本家の同機能も同様の単一リクエスト構成のため、本アプリ固有の弱点ではない)。テストは`tests/Feature/Wiki/WikiBulkPdfExportTest.php`に5件(ダウンロード+フォント埋め込み確認、権限なしでの拒否、権限なしでのPDF出力リンク非表示、親子階層の順序=`hierarchicalOrder()`をreflectionで直接検証、ページが1件も無いWikiでもPDFが生成されることの確認) |
| ~~プロジェクト削除~~ → **done**(2026-07-30)。**コピーは対象外と正式決定(2026-07-30)** | 削除のnested set cascade設計は、実は本アプリが既に依存している`kalnoy/nestedset`(`Project`が`NodeTrait`を使用済み)が`deleted`イベントで子孫を自動削除+`_lft`/`_rgt`再採番まで面倒を見るため、追加設計不要だった | 詳細は§2「プロジェクト削除」節参照。**コピー(Redmine本家`ProjectsController#copy`/`Project#copy`)の規模をRedmine本家ソースで実測した上で対象外と判断**: `Project#copy`(project.rb:935〜)は`copy_members`/`copy_wiki`/`copy_versions`/`copy_issue_categories`/`copy_issues`/`copy_queries`/`copy_boards`/`copy_documents`の8メソッド+依存する`Issue#copy_from`(issue.rb:315-349)を合わせて計9メソッド・約265行、1トランザクション内で連鎖実行。特に`copy_issues`(約100行、最大のメソッド)は: (1)`root_id, lft`順でソートし親を子より先にコピー(nested set)、(2)クローズ/ロック済みバージョンを一時的に再オープンして再割当てを可能にした後で状態を復元、(3)バージョン/カテゴリはid不一致のため**名前突き合わせ**で再解決、(4)`old_issue_id => new_issue`のマップを保持して`parent_issue_id`を付け替え、(5)**全課題コピー後に2周目のループ**を回し、双方向の`IssueRelation`(`relations_from`/`relations_to`)を同じマップで付け替え(`cross_project_issue_relations`設定が有効かつコピー先に対応課題が無い場合は元の課題を指したままにするフォールバック付き)——「課題Aが課題Bをblockし、両方コピーされる場合の新ID相互解決」という想定していた懸念点が実際に本家の実装に存在することを確認。本アプリの対応するEloquentモデル(Member/WikiPage/Version/IssueCategory/Issue/Query/Board/Document/IssueRelation)は全て既に存在しており、データ層の欠落が理由ではない——難所は「二周走査+idリマップ+名前突き合わせ+一時的な状態変更と復元」という、部分実装では正しさを検証しづらいビジネスロジックの複雑さそのもの。トラッカー/メンバー/カスタムフィールド値のみをコピーする部分スライスも検討したが、Redmine利用者が「プロジェクトコピー」に期待する主要ペイロードは課題そのものであり、それを欠いた部分実装は「動いているように見えて実は主要機能が無い」状態になりかねないため見送り。Mercurial/CVS/Bazaarアダプタ・Filesystemアダプタと同格の「大型据え置き」として正式に対象外決定。個別承認なしに再着手しないこと |
| ~~プロジェクトあたり複数リポジトリ~~ → **done**(2026-07-30) | スキーマ変更(1対1→1対多) | 詳細は§4「複数リポジトリ」行参照。スライス1(スキーマ+モデル)〜2a(identifier列)〜2b前半(リゾルバ統一)〜2b後半(識別子ありルーティング+URL生成一本化+識別子書式バリデーション+リポジトリ作成/既定変更フォーム+画面ごとの切替UI)まで全て完了 |
| Mercurial/CVS/Bazaar/Filesystemアダプタ | Git+SVNに続く追加SCMアダプタ | **Mercurial/CVS/Bazaarは着手不可と判明(2026-07-30)**: Sailコンテナに`hg`/`cvs`/`bzr`のいずれのバイナリも入っておらず、`GitAdapter`/`SvnAdapter`が採用している「実バイナリを使ったend-to-endテスト」パターンが書けない。バイナリ導入はCLAUDE.mdの依存追加承認ゲート対象のため、着手する場合は個別承認が必要。**Filesystemアダプタは設計上のミスマッチにより保留(2026-07-30調査)**: Redmine本家の`lib/redmine/scm/adapters/filesystem_adapter.rb`を確認したところ、実装しているのは`entries`(ディレクトリ一覧)と`cat`(ファイル内容取得)の2つのみで、リビジョン履歴・diff・blame(annotate)に相当する概念自体が存在しない(`info`の`lastrev`は常に`nil`)——単なるディレクトリブラウザであり、バージョン管理システムではない。本アプリの`ScmAdapter`インターフェース(`log`/`diff`/`blame`を含む5メソッド)をこのまま実装しようとすると、これら3メソッドは常に空/no-opを返すことになり、リポジトリ画面の「変更履歴」「Diff」「注釈」タブがFilesystemタイプでは常に空になる(Redmine本家はこれらのUI要素自体をリビジョン概念が無い場合に非表示化しており、空表示ではなく単に出さない)。インターフェースを「未対応の操作」を表現できる形に変更するか、UIを黙って空にするかのどちらかを選ぶ必要があり、価値の割に設計変更コストが見合わないため保留 |
| ~~LDAP属性マッピング~~ → **done**(2026-07-30) | 小規模 | 詳細は §5「LDAPカスタムフィルタ」節参照 |
| ユーザー閲覧範囲(`Role.users_visibility`) | **done**(2026-07-30)。2026-07-30の前回調査で「実質ノーオペ」と結論していたが、advisorレビューで「本当に汎用ユーザー検索機能が皆無か」を`grep`で再確認したところ結論が覆った | **前回調査の誤り**: `Project::assignableUsers()`・ウォッチャー追加候補は確かにプロジェクトメンバー限定だが、`resources/views/livewire/projects/members.blade.php`の`userCandidates()`(プロジェクトメンバー追加画面のユーザー検索、Redmineの`autocomplete_for_user`/`render_principals_for_new_members`に相当)は既存メンバーを除くだけで**サイト全体のアクティブユーザーを名前/メールの部分一致で検索できる**汎用オートコンプリートだった。このアクセス権は`manage_members`権限(`ProjectPolicy::manageMembers`)で、管理者専用ではなく任意のロールに付与できるプロジェクト権限のため、Redmine本家の`users_visibility`が対象とする「非管理者が他ユーザーをどこまで検索できるか」という制約がそのまま当てはまる実例だった(`groups/form.blade.php`の同種検索はグループ管理自体が管理者専用のため対象外、Redmine本家も admin は`Principal.visible`で無条件`all`扱いのため一致)。**実装内容**: `roles`テーブルに`users_visibility`列(`all`/`members_of_visible_projects`、既定`all`、Redmine本家の既定と一致)を追加、`Role`モデルに`UsersVisibility`enumとして配線(`issues_visibility`と同じ確立パターン)。新規`AuthorizationService::hasSiteWideUserVisibility(?User $user): bool`(Redmine本家の`Principal.visible`スコープの分岐ロジックを移植 — 管理者は無条件true、それ以外は「(グループ経由を含む)自分の全メンバーシップのいずれかのロールが`users_visibility=all`を持つか」、メンバーシップが一件も無ければ`NonMember`組み込みロールの設定を、ゲスト(null)なら`Anonymous`組み込みロールの設定を見る)と、新規`AuthorizationService::visibleProjectIds(?User $user): Collection`(Redmine本家の`User#visible_project_ids`≒`Project.visible(self).pluck(:id)`相当、`ProjectPolicy::view`が既に採用している「非アーカイブ かつ (公開 または メンバー)」という簡略化definitionを踏襲 — プロジェクト全件に対して`Gate::allows`をN回呼ぶ代わりにクエリ1本で解決)を追加。新規`User::scopeVisibleTo(Builder $query, ?User $viewer)`(`hasSiteWideUserVisibility()`/`visibleProjectIds()`を使う唯一の実施点)を追加し、`projects/members.blade.php`の3箇所すべて——検索候補を出す`userCandidates()`、ドロップダウンからの選択を確定する`selectUser()`、フォーム送信を受ける`addMember()`——から呼ぶ形に統一。Role編集フォームに「ユーザーの閲覧範囲」セレクトを追加(`issuesVisibility`と同じ配線パターン: mount/prefillFromCopySource/validate/save)。**ドライバイ修正**: `userCandidates()`に`->where('status', UserStatus::Active)`を追加(Redmine本家の`Principal.active.visible...`の`.active`部分が本アプリには元々無く、ロック済み/承認待ち/削除済みユーザーもメンバー追加候補に出てきてしまっていた——テストで確認・修正)。**advisorレビューで発見・修正したバグ(2件)**: (1) `Eloquent\Model::value('users_visibility')`はcastを経由するため`UsersVisibility`enumインスタンスを返すが、実装当初はこれを`UsersVisibility::All->value`(生文字列)と`===`比較しており常にfalseになっていた(ゲスト/メンバーシップ皆無ユーザーの組み込みロール参照の2箇所)。「ゲストがAnonymousロールのusers_visibility=allを正しく尊重するか」を検証するテストが実際に失敗して発覚、比較先を`UsersVisibility::All`(enumそのもの)に修正。(2) より重大: 制限は当初`userCandidates()`(検索候補の表示)にしか掛けておらず、選択を確定する`selectUser($userId)`と送信を受ける`addMember()`は任意の`$userId`/クライアント設定可能な`selectedUserId`プロパティをそのまま信頼していたため、`users_visibility=members_of_visible_projects`のロールを持つ非管理者でも`selectUser()`を直接呼べば可視範囲外の任意ユーザーの氏名/メールアドレスをそのまま画面に表示させられ、`->set('selectedUserId', N)->call('addMember')`で可視範囲外のユーザーを実際にメンバー追加までできてしまっていた——検索ドロップダウンの絞り込みだけでは、生のidを受け取るハンドラ自体を素通りする経路を防げないことが原因。`scopeVisibleTo()`を3箇所すべてに適用して修正、修正前に一度ガードを外して両テストが実際に404ではなく200を返すことを確認した上で復元。テストはAuthorizationService単体で13件(admin/ゲスト/メンバーシップ皆無/グループ経由メンバーシップ/複数ロールの最寛容判定/`visibleProjectIds`の公開・非公開・アーカイブ済みの各境界)、`projects.members`Livewireコンポーネント経由のE2Eで8件(既定`all`ロールは無制限に検索可能・`members_of_visible_projects`ロールは無関係ユーザーを検索不可・公開プロジェクトのメンバーは検索可能・検索者自身は常に検索可能・非アクティブユーザー除外・`selectUser()`直接呼び出しでの情報開示防止・`addMember()`への直接`selectedUserId`注入防止)を追加。REST API(`Api/V1/UserController::index`)は元々管理者専用(`UserPolicy`が`Gate::before`の管理者バイパス以外すべて拒否)のため対象外を確認済み、`groups/form.blade.php`の同種検索も同じ理由(グループ管理は管理者専用)で対象外を確認済み |
| 自己アカウント削除(unsubscribe) | 関連レコード再割当を伴う実削除 | **done**(2026-07-30)。詳細は§2「ユーザー」行参照 |

### 保留の再確認(現状維持を推奨)

- **意図的スコープ外の継続**: Issueのnested set化(63)、プロジェクト間サブタスク(66)、CSVインポートの高度マッピング(166-167)、ステータス一括編集の選択制約(163)、XML API — いずれも計画書で合意済みの逸脱。
- **単独では割に合わない**: 文書の作成者グルーピング(333、添付のアップロード者記録が前提)。~~Wikiのannotate(284、ニッチ)~~ → Wiki「Annotate/Blame」行は`done(2026-07-22)`(2026-09-19訂正)。Wiki開始ページ(290)・リアクション(142)は2026-07-30に着手・完了、下記および該当行を参照。
- **REST API拡張**(430-435): 機械的だが量が多い。APIの実利用者が現れた時点でまとめて1フェーズにするのが効率的。

### 運用原則(今回の最適化で明文化)

1. 設定画面のタブ/行は、対応する機能実装と**同じコミット**でのみ追加する(空の設定項目を先に作らない)。
2. 横断基盤(メール通知・プロジェクト横断ビュー)は近期キューを消化してから、承認を得て独立ミニフェーズとして着手する。プロジェクト横断ビュー(368/388他)は「クエリ+一覧」の型が固まっているため、メール通知より着手障壁が低い。
3. 過去2回発生した「実装済みなのにmissing表記」(Version CRUD、フォーラム添付、Watch権限ゲート)の再発防止として、行を消化する際は周辺行の実態も同時に確認する。

---

## 1. 課題管理(Issues / Workflow / Custom Fields)

対象: Issues, Trackers, IssueStatuses, IssuePriorities, Workflow, Journals, Watchers, IssueRelations, IssueCategories, Versions, 一括編集, CSVインポート/エクスポート。

### Issues 本体

| 機能 | 状態 | 備考 |
|---|---|---|
| 課題の作成/編集/閲覧 | done | `IssueService::create/update`, `issues/{form,show}.blade.php` |
| 課題本文・コメントのMarkdownレンダリング | done(2026-07-22) | 前回の調査で発見した欠落を解消。`issues/show.blade.php`の説明文・各Journalコメント(`notes`)を`WikiMarkdownRenderer`に通すよう変更(`renderedDescription`/`renderedNotes()`)、`#123`課題リンク・`[[Page]]`Wikiリンク・インライン画像参照(添付ファイル)がすべて課題側でも有効に。~~課題フォームの入力プレビューは対象外~~ → **done**(2026-07-23): Wikiフォームの`showPreview`/`previewHtml()`パターンをそのまま`issues/form.blade.php`に移植。説明文入力欄の下に「プレビュー」トグルボタンを追加、既存添付ファイルへのインライン画像参照も解決(新規選択中の未アップロードファイルはWiki側と同様に対象外) |
| 更新時の属性差分 Journal 記録 | done(2026-07-22訂正) | **訂正**: 従来「category_id・カスタムフィールドは記録されない」と誤記されていたが、実際には`JOURNALED_ATTRIBUTES`に`category_id`含め15項目が既に含まれ、カスタムフィールドも`diffCustomFieldSnapshots()`で別途記録済み(詳細は下の「属性変更の監査証跡」行を参照、内容が重複していたため本行はそちらに合わせて訂正) |
| 課題削除 | done(2026-07-21) | 詳細画面に削除ボタンを配線(`delete_issues`権限+確認ダイアログ)。工数は`nullOnDelete`で保持(切り離されるのみ)、子課題も`nullOnDelete`でトップレベル化。~~Redmineの`params[:todo]`(工数の再割当/削除選択)は意図的に対象外、常に保持のみ~~ → **`todo`を実装(2026-09-20)**: `IssueService::delete()`に`IssueTimeEntryDisposition`(`destroy`/`nullify`/`reassign`)と`reassign_to_id`を追加。課題詳細画面は作業時間がある課題にだけ確認パネル(3択+付替先の課題番号)を表示し、無い課題は従来の確認ダイアログのまま。REST API `DELETE /issues/{id}?todo=&reassign_to_id=`にも対応(不正な`todo`・別プロジェクト/自身/存在しない付替先は422で何も削除しない)。**Redmineとの意図的な差分**: `todo`省略時の既定は工数を**残す**(nullify)。Redmineの確認画面は「削除」が既定でAPIも省略時は削除だが、本アプリは従来から保持しており、既存クライアントの工数を黙って消さないため既定は変えていない。**一括削除にも同じ選択肢を追加(2026-09-20、A1-35)**: 選択した課題に作業時間があれば確認パネル(残す/一緒に削除/選択外の別課題へ付け替え)を表示、付替先が選択中・存在しない・別プロジェクトのときは何も削除せずエラー、不正な値も拒否、全削除は1トランザクション。**未対応**: 子課題の再帰削除(A1-34、要承認)。テスト: `IssueDeletionTest.php`・`IssueApiTest.php` |
| 課題のコピー | done(2026-07-21) | 詳細画面の「コピー」リンクが`?copy_from=<id>`付きで新規課題フォームを開き、トラッカー/優先度/カテゴリ/担当者/対象バージョン/題名/説明/日付/カスタムフィールドをプリフィル。ステータス/進捗率/作成者は通常の新規課題と同じ初期値。ジャーナル/添付/関連/親子は意図的にコピー対象外(軽量な「似た課題から始める」機能として設計)。2026-09-19注記: これは詳細画面の`?copy_from=`プリフィル経路の話で、一括コピー経路の`IssueService::copy()`は`$copyAttachments`引数を持つ(バックログA1-08)。**コピー時の引き継ぎを追加(2026-09-20)**: コピー元の添付/子課題/ウォッチャー/`copied_to`関連を、フォームのチェックボックスで選んで引き継ぐ(`IssueService::finishCopy()`、一括コピーと共通)。設定「課題のコピー」`link_copied_issue`(yes/no/ask)・`copy_attachments_on_issue_copy`(yes/no/ask、既定はRedmine同様ask=チェックボックス表示)。子課題の欄は子があるときのみ、ウォッチャーは`add_issue_watchers`権限がありコピー元にウォッチャーがいるときのみ表示。「ジャーナル/関連(他種類)はコピーしない」は従来どおり。テスト: `IssueCopyOptionsTest.php` |
| プロジェクト間の課題移動 | done(2026-07-21) | `move_issues`権限(移動元)+移動先での`add_issues`が必要。`IssueService::moveToProject()`がカテゴリ/対象バージョン/親をリセットし、移動先の非メンバーである担当者を解除、この課題を親としていた子課題も切り離す。Journalに記録 |
| 担当者「自分」ショートカット・作成時の既定開始/期日 | done(2026-07-23) | 課題フォームに「自分に割り当てる」ボタン(プロジェクトメンバーかつ未自己割当時のみ表示)。新規課題の開始日は作成日をデフォルトに。**期日の既定オフセット設定を追加**: Redmineの`Setting.default_issue_due_date_offset_in_days`相当(設定画面「新規課題の期日の既定値(作成日からの日数)」、空欄=既定値なし=Redmine自体の初期値と同じ)。`issues/form.blade.php`で`?copy_from=`によるプリフィル処理の**後**に適用(`??=`で既に値がある場合は上書きしない)、Redmineの`build_new_issue_from_params`がcopy_from処理の後に`||=`で適用する順序と一致。0日を指定した場合は当日が期日になる(負数は設定画面のバリデーションで拒否)。**開始日の既定を設定化(2026-09-20)**: Redmineの`Setting.default_issue_start_date_to_creation_date`(`IssuesController#build_new_issue_from_params`で`||=`、コピー後に適用)。本アプリは従来から常時「作成日」だったため設定の既定は**オン**(Redmine自体の既定はオフ — `config/settings.yml`で確認、意図的な差分)。設定「課題トラッキング」のチェックボックス、コピー元に開始日がなければ作成日を適用。**未対応**: REST API作成・受信メール作成への適用(Redmine本家は両方に適用、バックログA5-16b)。テスト: `IssueFormDefaultsTest.php` |
| 楽観的ロック(競合解決) | done(2026-07-22) | `issues.lock_version`列を追加。`IssueService::update()`が任意の`$expectedLockVersion`引数を受け取り、現在値と不一致なら`StaleIssueUpdateException`を投げて保存前に中断(保存のたびに`lock_version`をインクリメント)。課題編集フォームがフォーム読み込み時のlock_versionを保持し送信時に照合、競合時はエラーバナー表示で上書きを防止。一括編集/リポジトリ連携等プログラム的な更新は引き続き未指定(常に許可 — Redmineの`bulk_update`も楽観的ロックを使わない)。**REST APIに対応(2026-09-20)**: `PUT /issues/{id}`が任意の`lock_version`(Redmineのsafe attribute)を受け付け、古ければ**409**(現在の`lock_version`を返す、何も変更しない)。課題のペイロードに`lock_version`を追加(Redmineのshow.api.rsbにはないが、クライアントが値を知る唯一の手段のため)。省略時は従来のlast-write-wins。**Redmineとの差分**: Redmineは古い更新に空のエラーで422を返すが、409を採用。テスト: `IssueApiTest.php` |
| 新規課題のワークフロー(`old_status_id` が空の行) | done(2026-09-20) | `WorkflowService::initialStatuses()`が新規課題の開始ステータス(作成者のロール+作成者=author行の遷移先、なければトラッカー既定のみ、管理者は全て)を返し、課題作成フォームは選択肢が2つ以上のときだけステータス欄を表示、保存時は許可外(改ざん)を既定に戻す。トラッカー既定が許可外なら最初の許可ステータスから開始。管理画面のワークフロー編集の先頭に「(新規課題)」行を追加(保存・コピーも対応)。REST APIの作成は従来どおり既定ステータス固定(`status_id`未対応)。 |
| 課題の履歴タブ(`history_default_tab`) | done(2026-09-20) | 課題詳細の履歴にタブ(すべて/コメント/プロパティ変更、関連リビジョンがあり`view_changesets`があればチェンジセット)を追加。コメント=notesありのjournal、プロパティ変更=detailsありのjournal。URL`?tab=`と個人設定`history_default_tab`(既定=すべて)で初期タブを選べる。作業時間タブは既存の「工数」欄が同じ役目のため設けていない(=Redmineの作業時間タブは未対応)。 |
| 工数の派生値(`estimated_remaining_hours`、`spent_hours`/`total_spent_hours`/`total_estimated_hours`の列・API) | done(2026-09-20) | `Issue::estimatedRemainingHours()`(予定×(100−進捗)/100)を追加。課題一覧(プロジェクト)に列「予定工数/合計予定工数/残り工数/作業時間/合計作業時間」(CSVにも反映、計算列は並べ替え不可、必要時のみ`withCount('children')`/`withSum`で一括集計)、詳細画面に「残り工数(予定)」、REST APIの課題に`estimated_hours`/`total_estimated_hours`/`estimated_remaining_hours`と(`view_time_entries`保有者のみ)`spent_hours`/`total_spent_hours`を追加。グローバル課題一覧の列は未対応。 |
| 課題の細分化権限(`add_issue_notes`/`edit_own_issues`/`set_own_issues_private`/`manage_subtasks`) | done(2026-09-20) | `IssuePolicy::update`は`edit_issues`または(自分が作成した課題に対する)`edit_own_issues`。コメント投稿は`addNotes`(`add_issue_notes`)で、フィールドを編集できなくても可(課題詳細のコメント欄と引用)。非公開フラグは`setPrivate`(新規: `set_issues_private`か`set_own_issues_private`)と`setPrivateOn`(既存: 自分の課題のみ`set_own_issues_private`)。親課題の設定は`manage_subtasks`(なければ欄を隠し、保存しても親は変わらない)。マイグレーションで従来の代用権限(`edit_issues`→add_issue_notes/edit_own_issues/manage_subtasks、`set_issues_private`→set_own_issues_private)を持つロールへ自動付与。コメントだけの人には編集フォームを開かせず、詳細画面のコメント欄で対応(Redmineの編集フォーム内コメントとは異なる)。REST APIの課題更新は`update`(=`edit_issues`/`edit_own_issues`)のまま。 |
| 編集画面からの直接工数記録 | done(2026-07-22) | Redmineの`_edit.html.erb`内`log_time`fieldset相当。`log_time`権限を持つメンバーには課題編集フォームに時間/作業分類/コメントのインライン欄を表示し、課題保存と同じ送信で`TimeEntry`を作成(時間未入力ならスキップ)。別画面(`time-entries.create`)からの記録も引き続き利用可能 |
| `is_private`(非公開課題)フラグ | done(2026-07-21) | `issues.is_private`。`set_issues_private`権限保持者のみ作成/編集画面でON可能(サーバー側でも再チェックし、権限のない編集者が既存の非公開課題を意図せず公開化することを防止)。Journal記録・詳細画面のバッジ表示も対応 |
| ロール別の課題閲覧範囲(全て/デフォルト/自分のみ) | done(2026-07-21) | `Role.issues_visibility`(all/default/own) + `AuthorizationService::issueVisibilityFor()`。`IssuePolicy::view`と課題一覧のクエリで3段階を正しく強制:all=無条件、default=非公開課題は作成者/担当者のみ(Redmineの`Issue.visible_condition`と同じ規則、2026-07-21に`is_private`実装と合わせて修正)、own=作成者/担当者のみ。複数ロール保持時は最も緩い設定が優先 |
| Atom フィード / REST API 拡張(`include=`) | partial(2026-07-24) | プロジェクトの活動(activity.atom)・フォーラム(boards.atom)・お知らせ(news.atom)・課題一覧(issues.atom、2026-07-22追加)のAtomフィードを実装。課題一覧のフィードは`Issue::scopeVisibleTo()`+未クローズのみという固定条件(HTML版一覧の既定状態と同じ)で配信し、現在の絞り込み/ソート/グループ化状態は反映しない(Redmine本家は反映するが、News/Boardの各フィードも同様に「最新N件」の非フィルタ版であるため、一貫した意図的なスコープ限定)。**REST APIの`include=`パラメータをIssues show/indexに追加(2026-07-24)**: 詳細は下の「Issues」行を参照 |

### サブタスク・親子関係

| 機能 | 状態 | 備考 |
|---|---|---|
| 親子データモデル | done(2026-07-21) | 課題フォームに「親課題ID」欄を追加(同一プロジェクト内の課題IDを指定)。自己参照・循環参照(子孫を親に設定)はバリデーションで拒否。課題詳細画面に親へのリンクとサブタスク一覧を表示 |
| Nested set(ストレージ設計) | missing(意図的) | 隣接リスト設計を採用(`parent_id`のみ、`root_id`/`lft`/`rgt`なし)。ただし「計画書§7」への参照はワークスペース内に該当ファイルが見つからず、出典を検証できていない — 未検証の引用として扱うこと |
| 親子並べ替えUI | N/A(2026-07-30 再調査) | 当初「未実装」と記録されていたが誤り。Redmine本体(`app/views/issues/_subtasks.html.erb`、`app/helpers/issues_helper.rb`の`render_descendants_tree`)を確認したところ、サブタスク一覧は`issue.descendants.visible...sort_by(&:lft)`でnested setの挿入順に単純ソートされるだけで、ドラッグ&ドロップや上下移動ボタンなど手動並べ替えのUIはRedmineに存在しない(`app/views/issues/`配下に`sortable`/`drag`関連のコードなし)。つまりこの項目はRedmineに存在しない機能であり、パリティのギャップではない。実装するとRedmineには無い新機能の追加になるため、着手を見送った |
| 親への集計ロールアップ(優先度/日付/進捗率) | done(2026-07-22) | Redmineの`Issue#recalculate_attributes_for`相当を`IssueService`に実装。子課題の作成/更新(親の付け替え含む)のたびに親(および祖先)を再計算: 優先度は未クローズの子課題のうち最高優先度(全て閉じていればカタログの既定優先度にフォールバック)、日付は子課題の開始日の最小値〜期日の最大値、進捗率は予定工数で重み付けした平均(クローズ済み子課題は100%扱い、未見積の子課題は見積済み子課題の平均見積で重み付け)。`parent_issue_priority`/`_dates`/`_done_ratio`設定(既定オン)で個別に無効化可能。課題フォームは子課題を持つ課題でこれらの項目を無効化し注記を表示 |
| ステータスやサブタスクからの進捗率導出 | done(2026-07-22訂正) | **訂正**: 従来「単純なスライダー入力のみ」と記載していたが、ステータスからの導出(`issue_done_ratio`設定、2026-07-21実装済み)とサブタスクからの導出(`parent_issue_done_ratio`設定、上の「親への集計ロールアップ」行で2026-07-22実装)の両方が既に完了しており、記載漏れだった |
| プロジェクト間サブタスク | missing(意図的、範囲外) | 親子はプロジェクトスコープに限定(カテゴリ/バージョンと同様の設計判断)。Redmine本家の`Issue#valid_parent_project?`(`Setting.cross_project_subtasks`、system/tree/hierarchy/descendants)相当の設定は未実装 |
| `reschedule_on!`の下方向カスケード | N/A(2026-07-30調査、再訂正)→**実装(2026-09-20、A1-19)**: 日付導出(`parent_issue_dates`オン)の親が後続課題のとき、親自体ではなく葉(子孫のうち子を持たない課題)を移す。開始日が新しい最短開始日より前(または未設定)の葉だけが、稼働日数の期間を保ったまま最短開始日へ移り、親の日付は葉から再集計される。日付が変わった親(導出)は、自身の後続課題も再スケジュールする。`parent_issue_dates`オフなら従来どおり親を直接移す。**Redmineとの差分**: 後続課題は後ろへ押すだけで、先行課題が早く終わっても前へ引き戻さない。テスト: `RescheduleHierarchyTest.php`。以下は2026-07-30時点の調査記録 | 当初「Ganttのドラッグ&ドロップ操作で呼ばれる」と記載していたが、`redmine/app/views/gantts/`配下(`_chart.html.erb`/`show.html.erb`)を実際に`grep`したところ`drag`/`draggable`/`mousedown`/`sortable`のいずれも見つからず、Redmine本家のGanttチャート自体にドラッグ&ドロップUIが存在しないと判明——前回の「親子並べ替えUI」と同種の、呼び出し元を実装まで確認せず記載してしまった誤り。`Issue#reschedule_on!`(issue.rb:1435)の下方向カスケードが実際に呼ばれる経路は`IssueRelation`経由の後続課題連動のみで、これは既に§「関連日付からの自動リスケジュール」行(2026-07-24)で意図的な簡略化として記録済み(後続課題自身の日付を直接変更、子への伝播はしない)。つまりこの項目に実装すべき残りの機能は無い |
| 子孫を含めた予定/実績工数の集計 | done(2026-07-22) | `issues.estimated_hours`列を新設(課題フォームに入力欄追加、空欄は0ではなくnullとして保存)。`Issue::spentHours()`/`totalSpentHours()`/`totalEstimatedHours()`をRedmineの同名メソッド相当で実装(子課題を持つ場合は`descendantIds()`の再帰CTEで子孫全体を合算、末端課題は自身の値のみ)。課題詳細画面に「合計: X時間」として表示 |

### Journal(履歴・コメント)

| 機能 | 状態 | 備考 |
|---|---|---|
| コメント追加 | done | — |
| 属性変更の監査証跡 | done(2026-07-22) | 添付/関連の変更記録を追加(Redmineの`Journal#journalize_attachment`/`#journalize_relation`相当、`IssueService::journalizeAttachment()`/`journalizeRelation()`)。添付は`property='attachment'`+`prop_key`=メディアID、追加時は`new_value`・削除時は`old_value`にファイル名(課題**作成時**の添付はRedmine同様記録しない — 作成自体がJournalを持たないため)。関連は両側の課題にそれぞれJournalを作成し、`prop_key`はその課題から見た関連タイプ(受け側はRedmineの反転名 blocked/duplicated/follows/precedes)、値は相手課題ID。履歴表示にも両プロパティの描画を追加 |
| プライベートノート(`view_private_notes`) | done(2026-07-21) | `set_notes_private` 権限保持者がコメント投稿時に非公開フラグを立てられる(サーバー側でも権限を再チェックし、クライアント改ざんを無効化)。`view_private_notes` を持たないユーザーには非公開Journalを非表示(自分自身が書いたものは例外的に閲覧可、Redmineの`Journal#visible?`と同様)。活動フィード(`IssueJournalActivityProvider`)は既存どおり非公開Journalを丸ごと除外 |
| 過去コメントの引用返信 | done(2026-07-21) | 各コメントの「引用」ボタンでコメント入力欄に`>`引用形式をプリフィル |
| 個別 Journal の編集 | done(2026-07-29、訂正あり) | Redmineの`Journal#editable_by?`相当を実装。`edit_issue_notes`(全コメント編集可)/`edit_own_issue_notes`(自分のコメントのみ)権限を新設(`App\Policies\JournalPolicy`)。`issues/show.blade.php`の履歴表示に編集ボタン+インライン編集(テキストエリア)を追加。属性変更のみのJournal(コメント本文が無いもの)は編集ボタン自体を出さない(Redmineの編集フォームがコメント付きJournalからしか到達できないのと同じ制約)。編集時は`updated_at`が`created_at`と異なる場合に「(編集済み)」表示(Redmineの`Journal#updated_by_time`相当、編集履歴自体は保持しない簡略化)。**訂正(実装後レビューで発覚)**: 当初「削除」機能(`deleteJournal`、`JournalPolicy::delete()`、DELETE APIエンドポイント含む)も実装しテストまで通したが、参照元Redmine実チェックアウト(`/Users/sesoko/Desktop/workspace/redmine`)を`grep`で確認したところ`JournalsController`は`resources :journals, :only => [:edit, :update]`で`destroy`アクション自体が存在せず、`Journal`モデルにも`destroyable_by?`は無いことが判明 — Redmine自体に「Journal削除」機能が無い。削除機能は本文を空文字にする`update`で代替されており(Journal自体は残り、同一Journal内の属性変更詳細も保持される、`Journal#isEmpty()`が本文・詳細の両方が空の場合のみ一覧から非表示にする)、独自に追加した削除機能は`edit_own_issue_notes`権限だけで同一Journal内の属性変更監査証跡ごと消去できてしまう実害のあるスコープ逸脱だったため撤去。`notes`を空文字で更新することを明示的に許可(`UpdateJournalRequest`/Livewireの`saveJournalEdit()`とも`nullable`に変更)し、Redmine同様「本文を空にする」ことが実質的な削除操作として機能する。**可視性ゲート**: `editingJournalId`はLivewireの公開プロパティとしてクライアントから任意のJournal IDを送れるため、`JournalPolicy::update()`側で`Issue`の閲覧可否(`view_issues`権限・`issues_visibility`・`is_private`)と非公開ノートの可視性(`view_private_notes`、自分の投稿は例外)を明示的に再チェック(Redmineの`Journal.visible`スコープが`editable_by?`より先に効くのと同じ順序)。テンプレート側の表示制御だけに依存しない、直接リクエスト送信を想定したテストを追加済み。**編集者の記録とプライベートフラグの編集を追加(2026-09-20)**: `journals.updated_by_id`列を追加し、編集時に編集者を記録して「(X が編集 日時)」と表示(編集前に記録された古い行は従来の「(編集済み)」表示のまま)。編集フォームに「非公開コメントにする」(`set_notes_private`権限者のみ、それ以外は現状維持)。テスト: `JournalEditMetadataTest.php` |
| 変更点を含むプライベートノートの分割記録 | done(2026-07-23) | Redmineの`Journal#split_private_notes`相当。これまで課題編集フォーム(`issues/form.blade.php`)には非公開メモのチェックボックスが無く、属性変更とコメントを同時に保存すると常に1つの公開Journalに混在していた(コメント単体の`issues/show.blade.php`側は既に対応済みだったため、このギャップは編集フォーム経由の保存のみ)。`IssueService::update()`に`commentIsPrivate`引数を追加し、非公開コメント+属性/カスタムフィールド変更が同時に発生した場合のみJournalを分割: 属性変更を持つ公開Journalと、ノートのみを持つ非公開Journalの2件を作成(コメントのみ、または変更点のみの場合は分割不要のため従来どおり1件)。`Issue::journals()`に`orderBy('id')`を副次ソートとして追加し、同一秒に作成される分割後の2件の表示順を安定化。フォーム側は`show.blade.php`と同じ`setNotesPrivate`権限ゲート+サーバー側再チェックのチェックボックスを追加 |
| イベント別の通知粒度 | done(2026-07-22〜29) | `IssueService::update()`のJournal作成条件と`IssueUpdated`イベント発火条件が食い違っていたバグを修正(2026-07-22): 属性変更が無くコメントのみの更新でもJournalは作成されるのに`IssueUpdated`(Webhookの`issue.updated`が購読)は発火しないという不整合があった。現在はコメント単体でも発火。**メール通知システムを実装(2026-07-29)**: 詳細は下の「メール通知(課題)」行参照 |
| メール通知(課題) | done(2026-07-29) | Redmineの課題メール通知(作成/更新)を実装。`IssueCreated`/`IssueUpdated`イベント(Webhookと共有、`IssueUpdated`に`actor`/`journal`を追加)に`App\Listeners\SendIssueMailNotifications`を配線、`App\Support\Mail\NotificationRecipients::forIssue()`が宛先を解決: ①`Setting::notified_events`でイベント種別自体のON/OFF、②各ユーザーの`mail_notification`設定(`App\Enums\MailNotificationOption`: all/selected/only_my_events/only_assigned/only_owner/none、Redmineの`User::MAIL_NOTIFICATION_OPTIONS`相当)、③自分自身の変更を通知するかどうか(`User::no_self_notified`、後述の訂正参照)、④`IssuePolicy::view`による可視性チェック、の4段階で絞り込み。`App\Mail\IssueNotificationMail`(`App\Notifications\IssueNotification`経由でShouldQueue)がRedmine形式の件名(`[プロジェクト - トラッカー #ID] (ステータス) 題名`)と属性差分(Journalの`property=attr`/`cf`の両方~~、`attachment`/`relation`は対象外~~ → 2026-09-20に`attachment`(添付ファイル名)と`relation`(関連種別ラベル+相手課題`#ID`、逆方向名blocked/duplicated/copied_fromも対応)の行もメール本文(HTML/テキスト)に描画。**通知経路も整備(2026-09-20、A6-04b)**: 専用イベント`IssueJournalRecorded`(メールリスナーのみ、`issue.updated`のWebhookは発火しない — RedmineもWebhookはJournalではなく課題レコードの変更から出す)で、添付の追加/削除(`journalizeAttachments()`が複数ファイルを1 Journal・1通に集約)と関連の追加/削除(両側の課題それぞれ)をメール通知。フォーム/APIの添付は保存ごとに1通。**Redmineとの差分**: Redmineは編集と同じJournalに添付を含めて1通だが、本アプリは編集のメールと添付のメールの2通になる。テスト: `IssueJournalNotificationTest.php`)を描画、`Setting::plain_text_mail`でHTML部省略、`Setting::mail_from`で送信元上書き、`Setting::emails_footer`で署名追加に対応。**プロフィールに`mail_notification`選択欄を追加**(自己管理のみ、管理者による他ユーザー編集フォームは対象外)。`notified_events`の選択肢は今回配線した課題の2キー(`issue_added`/`issue_updated`)のみ提供(§0.5運用原則1: 対応するリスナーが無いイベント種別のトグルは追加しない)。**意図的な簡略化2点**(コード側にコメント明記): (1) Redmineの`selected`(プロジェクト単位で通知を選択)は該当UIが無いため`only_my_events`に縮退。~~(2) 宛先候補のプロジェクトメンバー解決は`Project::assignableUsers()`と同じ「直接メンバーのみ、グループ展開なし」の簡略化を踏襲~~ → **グループ展開を実装(2026-09-20)**: `Project::memberUserIds()`が直接メンバーとメンバーであるグループの所属ユーザーを合算し(Redmineはグループの各ユーザーにメンバー行を持つため`notified_users`に含まれる)、`NotificationRecipients::resolve()`(課題・Wiki・Newsの全通知)が使用。閲覧権限の確認は従来どおり最終段で適用。担当者候補(`assignableUsers()`)は直接メンバーのみのまま(グループ担当と同じ設計判断が必要、バックログA1-20)。テスト: `IssueMailNotificationTest.php`。Wiki・Newsのメール通知は2026-07-30にdone化(下の「メール通知(Wikiページ)」「メール通知(News)」行参照)。メール確認付き登録も2026-07-30にdone化(下部「登録モード」行参照)。~~@mention通知は引き続き未着手(前提2つが必要、詳細は§0.5参照)~~ → 課題・Wikiは2026-07-30に実装済み、課題(説明・コメント)とWikiは実装済みで、Newsコメント/フォーラム投稿はRedmineも@mention非対応(2026-09-20確認、パリティ対象外)。**訂正(2026-07-29)**: ③段の「自分自身の変更を通知しない」は当初グローバル設定`Setting::self_notification`として実装したが、`/Users/sesoko/Desktop/workspace/redmine`で確認した結果Redmine本家に同名のグローバル設定は存在せず、実際は送信者(`@author`)自身の`UserPreference#no_self_notified`(`Mailer#mail`が`@author.pref.no_self_notified`を見て`:to`/`:cc`から送信者自身を除外)であることが判明。`users.no_self_notified`列(既定値`true`、`Setting::default_users_no_self_notified`の実際の既定値`1`と一致させる形でマイグレーション/モデル`$attributes`双方をtrueに設定)に置き換えて修正。新規ユーザー作成の3経路すべて(自己登録`CreateNewUser`・管理者による作成`users/form.blade.php`・オンザフライLDAPプロビジョニング`AuthenticateUser::provisionFromDirectory()`)で`Setting::default_users_no_self_notified`から明示的にシード、プロフィール画面で本人が個別に上書き可能 A6-05(2026-09-20): 個人設定`notify_about_high_priority_issues`(既定オフ)を追加。オンの有効なプロジェクトメンバーには、優先度の並び順が既定優先度より上の課題を、通知設定(`none`含む)に関わらずメールする(`NotificationRecipients::forHighPriorityIssue`、閲覧権限のない人・自分の変更を通知しない設定の実行者は除外、他の条件と重複しても1通)。。**`selected`(選択したプロジェクトのみ通知)に対応(2026-09-20、A3-09)**: `members.mail_notification`列を追加し、プロフィールの通知設定で「選択したプロジェクトのイベントと、自分の関与するイベントのみ通知」を選ぶとメンバーになっているプロジェクトのチェックボックスが出る(所属が無ければこの選択肢は出ない)。チェックしたプロジェクトの全イベント+自分が作成者/担当者/ウォッチャーのイベントを通知(従来は「自分の関与するイベント」と同じ扱いだった)。`NotificationRecipients::resolve()`が判定。**Redmineとの差分**: 管理者のユーザー編集画面には通知設定が無いため管理者経由では設定できない。テスト: `SelectedProjectsNotificationTest.php`。**通知イベントの追加(2026-09-20、A6-01)**: 設定「メール通知」に`issue_note_added`(コメント付きの更新は、`issue_updated`が無効でもこれだけで通知)、`message_posted`(フォーラム投稿/返信)、`document_added`、`file_added`(プロジェクト/バージョンのファイル追加)を追加。後の3つは`SendProjectEventMailNotifications`と共通の`ProjectEventNotification`(件名+見出し+リンク+本文)で、宛先は全メンバー(通知オフを除く、お知らせと同じ全段階)+フォーラムはトピックのウォッチャー、閲覧権限で絞る(`NotificationRecipients::forProjectEvent()`)。**Redmineとの差分**: `issue_status_updated`/`issue_assigned_to_updated`/`issue_priority_updated`と、ファイル追加のメールでのリンク先(ファイル一覧まで)。テスト: `ProjectEventMailTest.php` |
| メール通知(Wikiページ) | done(2026-07-30) | Redmineの`WikiContent#after_create`/`after_update`(`/Users/sesoko/Desktop/workspace/redmine`の`app/models/wiki_content.rb`・`app/models/mailer.rb`まで確認)を参照実装として移植。既存の`WikiPageCreated`/`WikiPageUpdated`イベント(元々Webhook用)に新規`App\Listeners\SendWikiPageMailNotifications`を追加配線。**宛先解決はIssueと異なる簡略ロジック**: Redmine本家の`Project#notified_users`(`members.mail_notification=true OR users.mail_notification='all'`)はIssueの6段階(`only_assigned`/`only_owner`を含む)と違い担当者/作成者の概念を一切見ない。これに合わせ`App\Support\Mail\NotificationRecipients::forWikiPage()`を追加、共通の`resolve()`ヘルパーに新規パラメータ`assignedAndOwnerTiersRequireWatcher`(既定false=Issue互換)を追加してWiki呼び出し時のみ`true`を渡すことで、`only_assigned`/`only_owner`ティアのユーザーもウォッチしていない限り通知対象に含めない(Issueは無条件に候補へ含め、コールバックで絞り込む設計のまま維持、後方互換)。**textChanged判定を追加**: Redmine本家の`saved_change_to_text?`(リネーム/移動のみの更新ではメールしない)を再現するため、`WikiPageUpdated`イベントに`bool $textChanged`プロパティを追加(既定`true`、後方互換)。`WikiPageService::update()`はテキストが実際に変わったかを事前判定して渡し、`moveToProject()`は常に`textChanged: false`を渡す(テキスト自体は変更しないため)。Webhook側(`DispatchWebhooksForWikiPageEvent`)はこのフラグを見ず従来通り常時発火(本アプリ独自機能側の挙動は変更しない意図的な判断。2026-09-19訂正: Redmine 7.0.0はコアにWebhook(`app/models/webhook.rb`、`acts_as_webhookable`、権限`use_webhooks`)を持つため「概念が無い」は誤り、バックログA12-01/02)。`App\Mail\WikiPageNotificationMail`/`App\Notifications\WikiPageNotification`はIssue版と同型(`ShouldQueue`境界・`plain_text_mail`・`mail_from`・`emails_footer`対応)。**`notified_events`に`wiki_content_added`/`wiki_content_updated`の2キーを追加**(既定の通知対象イベント一覧には含まれない — Redmine本家の`config/settings.yml`の`notified_events`既定値も`issue_added`/`issue_updated`のみで、Wiki系は本家でも既定無効なため一致)。テストは実際の`WikiPageService::create()`/`update()`/`moveToProject()`経由のE2Eで検証(11件): 既定無効の確認、有効化後の通知、`none`ティア除外、`only_assigned`ティアがウォッチしない限り通知されないこと(Issueとの違いを明示的に検証)、リネームのみでは送信されないこと、テキスト変更時は送信されること、`no_self_notified`、権限フィルタ、プロジェクト間移動での非送信、件名/本文フォーマット、`plain_text_mail` |
| メール通知(News) | done(2026-07-30) | Redmineの`News#after_create_commit`/`Comment#after_create_commit`(`app/models/news.rb`・`app/models/comment.rb`・`app/models/mailer.rb`・`app/models/user.rb`の`notify_about?`まで確認)を参照実装として移植。News/NewsCommentにはWebhook用イベントすら無かったため新規`App\Events\NewsCreated`/`App\Events\NewsCommentCreated`を追加、`News/form.blade.php`の作成分岐・`news/show.blade.php`の`addComment()`から配線(**編集では配線しない**: Redmine本家の`News`に`after_update`フックが無く、既存項目の編集はメール対象外という事実をソースで確認した上での意図的な一致)。**宛先解決はIssue/Wikiとさらに異なる**: Redmine本家の`User#notify_about?(News)`は`when News; true`(ブランク/`none`以外の全ティアを無条件許可)で、Issueの担当者/作成者判定もWikiの「ウォッチしていなければ`only_my_events`等は対象外」という絞り込みも一切行わない。`App\Support\Mail\NotificationRecipients::forNews()`を追加、`resolve()`に新規パラメータ`allTiersRequireMembershipOrWatch`(News呼び出し時のみ`true`)を追加し、全ティアを`isMember || isWatcher`一本化(Issue/Wikiの既存分岐は無変更)。**コメント通知はさらに宛先が広い**: Redmine本家の`Mailer.deliver_news_comment_added`は`news.notified_users | news.notified_watchers`と、News単体をウォッチしている「非メンバー」も宛先に加える(`news_added`のみの通知には無い経路)ため、`forNews()`は`eventKey === 'news_comment_added'`のときだけウォッチャーIDを候補集合に含める。**意図的な対象外**: Redmine本家は`news_added`のCC先として`notified_watchers_for_added_news`(プロジェクトの「News」モジュール自体をウォッチしている非メンバーへのCC、`project.enabled_module('news')`がウォッチ対象になるRedmine独自の仕組み)も持つが、本アプリに「プロジェクトのモジュール単位でウォッチする」という概念自体が無いため対象外(`news_comment_added`側の「Newsアイテム単体をウォッチする」機能とは別物、そちらは実装済み)。**`notified_events`に`news_added`/`news_comment_added`の2キーを追加**(Wiki同様、Redmine本家の既定値にも含まれないため既定は無効のまま)。テストは実際の`news.form`/`news.show`Livewireコンポーネント経由のE2Eで検証(9件): 既定無効、有効化後の全ティア通知(`only_assigned`ティアもウォッチ不要で届くことをWikiとの対比で明示)、`none`ティア除外、作成時の自動ウォッチ+`no_self_notified`、**編集では送信されないこと**、コメント投稿時にメンバーと「ウォッチしている非メンバー」の両方に届くこと(後者は組み込みNon-memberロールに`view_news`を付与しないと`NewsPolicy::view`で弾かれることをデバッグで発見・テストに反映)、権限フィルタ、件名(`Re: `プレフィックスの有無)/本文フォーマット、`plain_text_mail` |
| テキスト差分表示・リアクション | **done**(テキスト差分2026-07-22〜24、リアクション2026-07-30) | テキスト差分表示を実装: Redmineの`JournalsController#diff`相当(`Redmine::Helpers::Diff`を使う点も含め、既存の`App\Support\Diff\WordDiffer`をそのまま再利用)。説明文の変更(`property='attr' AND prop_key='description'`、Redmine本家がdiffリンクを出す唯一の`attr`プロパティ)に加え、**長文形式(`CustomFieldFormat::Text`)カスタムフィールドの変更(`property='cf'`)も追加(2026-07-24)**: Redmine本家の`change_as_diff?`が`text`フォーマットのCFに限り差分表示するのと同じ条件。`issues.journal-detail-diff`ルート/`mount()`のガードを拡張し、見出しをカスタムフィールド名に切り替え。**リアクション機能を実装(2026-07-30)**: Redmine本家の`Reaction`モデル(`app/models/reaction.rb`、`db/migrate/20250423065135_create_reactions.rb`)を調査した結果、GitHub風の複数絵文字ピッカーではなく**単一のいいね(サムズアップ)トグル**で、`reaction_type`列自体が存在しないと判明——本アプリもこれに忠実に、`type`列なしの`reactions`テーブル(ポリモーフィック`reactable`+`user_id`、`(reactable_type, reactable_id, user_id)`一意制約)のみを新設。Redmine本家の`Redmine::Reaction::REACTABLE_TYPES`(`Journal`/`Issue`/`Message`/`News`/`Comment`)に対応する5モデル全てに`App\Concerns\HasReactions`トレイト(`reactions()` morphMany + `isReactedBy(?User $user)`)を配線(本アプリでの対応: `Journal`/`Issue`/`Message`/`News`/`NewsComment`)。新規`App\Services\ReactionService`が本家の`Redmine::Reaction.editable?`(`reactions_enabled`設定・ログイン済み・対象の閲覧可否・プロジェクトが`active`)を移植、`canReact()`/`toggle()`の2メソッドで完結。**設定**: `reactions_enabled`(既定true)を設定画面に追加。**UI**: 共通Bladeコンポーネント`<x-reaction-button>`(👍アイコン+件数、トグル、権限が無い場合は件数>0の時のみ非活性表示)を`issues.show`(課題本文+各Journal)・`news.show`(お知らせ本文+各コメント)・`messages.show`(トピック本文+各返信)の3ページに配線、各ページのLivewireコンポーネントに`toggleReaction(string $type, int $id)`アクションを追加(タイプ文字列は`'issue'`/`'journal'`/`'news'`/`'news_comment'`/`'message'`のホワイトリスト、FQCNをクライアントへ渡さない設計)。Journalは本家同様に専用の閲覧可否ポリシーを持たないため、`ReactionService`内に`JournalPolicy::update`の可視性判定(親issueの閲覧可否+非公開ノートは`view_private_notes`か本人のみ)を複製する形で反映。ポリモーフィックのモデル型解決に既存の`Relation::enforceMorphMap()`(`MorphMapServiceProvider`)を利用するため、`journal`/`news_comment`のエイリアスを追加登録。当初は反応後の表示件数が古いままになる懸念(Livewireがリクエストをまたいで読み込み済みリレーションを保持する場合の既知の落とし穴)から`toggleReaction()`側でコンポーネント束縛済みインスタンスを再利用する実装にしていたが、フィックスを外した状態でレンダリング結果を検証するテストを追加した上で実際に外して確認したところ問題なく最新件数が表示されたため(本アプリの各ページは`$this->issue`等を毎リクエスト素朴に再取得しリレーションを引き継がない実装のため該当せず)、この対応は撤回し単純な`::query()->findOrFail()`のみに戻した。テストは`tests/Feature/Reactions/ReactionTest.php`に11件(課題/Journal/お知らせ/コメント/フォーラム投稿・返信でのトグル追加削除とレンダリング後の件数表示確認、非公開Journalへの反応拒否、`NonMember`ロールの`view_issues`有無による許可/拒否、クローズ済みプロジェクトでの拒否、他プロジェクトのJournal IDを渡した場合の拒否、`reactions_enabled=false`での拒否、DB一意制約、Journal数を変えた2課題間で`reactions`/`issues`/`projects`クエリ件数が一致すること=Journal件数に比例しないことの確認)。**N+1修正(2026-07-31)**: `/laravel-best-practices`レビューで`<x-reaction-button>`の`$reactable->reactions->count()`がBladeテンプレート内での遅延ロードクエリ(db-performance.mdの「No Queries in Blade Templates」に抵触)になっていたと判明——`isReactedBy()`もリレーションの中身(誰が反応したか)を要するため`withCount()`では不十分と判断し、`issues.show`/`news.show`/`messages.show`の各`mount()`・`comments()`・`replies()`側で`reactions`(および`journals.reactions`)を都度`with()`/`load()`する対応に変更。**さらに`/simplify`のマルチエージェントレビューで、`ReactionService::canReact()`内部が`journal->issue->project`/`newsComment->news->project`/`message->board->project`をたどっており、`reactions`だけ解消してもこの経路がJournal/コメント/返信の件数分だけ遅延クエリを再発生させていたと判明**——`journals.issue.project`(課題側)・`news.project`(お知らせコメント側)・`board.project`(フォーラム返信側)も同じ`with()`/`load()`に追加。加えて`issues/show.blade.php`内で5箇所に重複していた`journals.*`の再読み込み処理を`reloadJournals()`private methodへ集約(`reloadRelations()`と同じ既存パターンを踏襲)。 |

### Watchers

| 機能 | 状態 | 備考 |
|---|---|---|
| 自分のWatch/Unwatch | done | — |
| Watch権限ゲート | done(2026-07-22訂正) | **訂正**: 従来「`view_issues`があれば誰でも可能」を理由にpartialとされていたが、Redmine自体が自己Watchに専用権限を持たず「対象を閲覧できること」だけを要求する(`acts_as_watchable`)ため、`IssuePolicy::watch`の現挙動はRedmine忠実で完了扱いが正しい。他ユーザーの追加/削除は別権限`add_issue_watchers`(下の行)で既にゲート済み |
| 他ユーザーをWatcherとして追加/削除 | done(2026-07-21) | 詳細画面にウォッチャー一覧+追加(プロジェクトメンバーのセレクト)+削除を配線。`add_issue_watchers`権限で保護(`IssuePolicy::manageWatchers`)。追加対象はプロジェクトメンバーに限定、単純なセレクトのみでオートコンプリートは未実装。**新規課題フォームでのウォッチャー指定(2026-09-20、A1-01)**: `add_issue_watchers`権限者にだけ、有効なプロジェクトメンバーのチェックボックスを表示し、作成時に`Watcher`を作る。フォームが出した候補以外のID(非メンバー・ロック中)は無視。編集画面には出さない。テスト: `IssueFormWatchersTest.php` |
| ウォッチャー権限(課題/Wiki/フォーラム) | done(2026-09-20) | Redmineと同じ`view_*_watchers`/`add_*_watchers`/`delete_*_watchers`(課題は`view_issue_watchers`と`delete_issue_watchers`を追加、Wiki `*_wiki_page_watchers`、フォーラム`*_message_watchers`)を登録。`IssuePolicy`/`WikiPagePolicy`/`MessagePolicy`の`manageWatchers`を`viewWatchers`/`addWatchers`/`deleteWatchers`に分離(ニュースは従来どおり`manage_news`)。一覧は`view_*`、追加/削除ボタンとAPI(`POST/DELETE /issues/{id}/watchers`、`include=watchers`)は各権限で判定。マイグレーションで従来の代用権限を持つロールへ自動付与(view_*←各閲覧権限、追加/削除←`add_issue_watchers`/`edit_wiki_pages`/`edit_messages`)。フォーラムの引用返信は既存、返信のウォッチは本家もトピックのみでギャップではない(C-24)。 |
| 作成者/担当者の自動Watch・@mention・自動整理 | **done**(自動整理2026-07-31) | 作成者は作成時に、担当者は割当変更のたびに自動Watch(2026-07-21、`IssueService::autoWatch()`)。**自動整理を実装(2026-07-31)**: Redmine本家の`Watcher.prune`/`redmine:watchers:prune`(app/models/watcher.rb、`watchable.visible?(user)`がfalseになったWatcher行を削除する手動/cronタスク)を調査の結果、実在する未実装機能と確認(4連続で判明した「実は存在しない」誤記載とは異なる)。新規`App\Jobs\PruneUnwatchableWatchersJob`(全`Watcher`行を`watchable`ごとに`Gate::forUser($watcher->user)->allows('view', $watcher->watchable)`で判定し、falseなら削除)を`routes/console.php`に`daily()`で登録——本家はcronでの手動運用を前提とした素のrakeタスクだが、本アプリは`PruneExpiredPendingUploadsJob`と同じ「保守系スイープは自動スケジュール」という既存方針に倣い自動実行とした点が意図的な逸脱(本家との差異として明記)。テストは`tests/Feature/Watchers/PruneUnwatchableWatchersJobTest.php`に3件(プロジェクトメンバーシップ剥奪後のWatcher削除・閲覧権限が残っている場合は削除しない・プロジェクトアーカイブ後のNews watcherの削除——`Issue`/`News`の2つのwatchable型で確認)。**@mention(課題)をdone化(2026-07-30)**: 見送り理由だった2つの前提(全ユーザー必須login欄・公開プロフィール画面)が同日中に解消したため再着手。Redmine本家の`Redmine::Acts::Mentionable`(lib/redmine/acts/mentionable.rb、Issue/Journal/WikiContentにmix-in)を参照実装として、新規`App\Support\Mail\MentionParser`(app/Support/Mail/、`NotificationRecipients`と同じ名前空間に配置)を追加。`extractLogins(?string $text): array`が本文中の`@login`トークンを抽出(フェンスドコードブロック・インラインコード・blockquote行は除外——本家が生成テキストを走査する際にコード/引用領域を除外するのと同じ理由)、`newlyMentionedLogins(?string $before, ?string $after): array`が本家の核心的な仕様——**メンションは「今回の保存で新規に追加された`@login`」だけがトリガーになり、既存メンションを含んだまま無関係な編集をしても再通知されない**——を実装。**簡略化した点(意図的、コメントに明記)**: 本家の正規表現(`(?:^|\W)@([A-Za-z0-9_\-@\.]*?)(?=...)`、パンクチュエーションの先読みによる境界判定)を完全再現せず、ログイン文字集合から`@`を除いた`[A-Za-z0-9_.\-]+`の貪欲マッチ+末尾の`.`一つだけ除去、という簡略版に留めた(`login`自体に`@`を含められる`User::LOGIN_FORMAT_REGEX`との非対称は許容——そのようなloginはメンション対象外になる)。**通知経路への統合**: `IssueService::create()`は説明文全体からのメンション(`IssueCreated`イベントに`mentionedLogins`引数を追加)、`update()`は説明文の新規追加分+新規コメント本文の両方を合算(`IssueUpdated`イベントに同引数を追加、いずれもデフォルト`[]`で既存の直接コンストラクト呼び出し箇所は無改修)。`NotificationRecipients::forIssue()`に`$mentionedLogins`引数を追加し、新規`forMentionedUsers()`(ウォッチャー/メンバーであることを一切要求しない——Redmine本家の`notified_mentions`が`notified_users`/`notified_watchers`と対等にunionされる設計と一致)の結果を既存の階層別解決結果とマージ、最後に`->can('view', $issue)`で可視性フィルタ(本家の`notified_mentions`が`visible?`チェックを課すのと同じ役割、ただし本家は言及者=閲覧者のスコープで緩く解決するのに対し、本アプリは対象読者自身の`IssuePolicy::view`で厳密に判定するため、権限の無いユーザーへの誤送信は本家より確実に防止できる)。**メンション通知もSetting.notified_events(issue_added/issue_updated)の有効/無効に従う**——本家もメール送信全体がこの設定でゲートされるdeliver_issue_add/edit内でnotified_mentionsがunionされる構造のため、独立してオン/オフできる別設定にはしていない。ガードを一時的に外し、(1)無関係な編集での再通知が実際に発生すること、(2)ログイン欄必須化で追加した末尾改行拒否の正規表現同様の食い違いが無いことを確認した上で復元。**Wikiページ本文へのmentionも同日中に拡張(2026-07-30)**: 課題と全く同じ`MentionParser`/`NotificationRecipients::forMentionedUsers()`をそのまま再利用。`WikiPageService::create()`は新規本文全体から、`update()`は本文の新規追加分のみ(`$oldText = $page->currentVersion?->text`をDB更新前に保持してdiff)からメンションを検出、`WikiPageCreated`/`WikiPageUpdated`イベントに`mentionedLogins`引数を追加(いずれもデフォルト`[]`)。リネーム/移動のみの更新(`textChanged=false`)は本文diffを行わないため常に空配列——タイトルに`@login`が含まれていてもメンション扱いにならない(本家もWikiContentのtext属性のみをacts_as_mentionable対象にしており、タイトルは対象外であることと一致)。`NotificationRecipients::forWikiPage()`にも同じ`$mentionedLogins`引数を追加し、階層別解決結果とマージしてから`->can('view', $page)`で可視性フィルタ。ガードを一時的に外し、無関係な編集での再通知が実際に発生することを確認した上で復元。テストは`tests/Feature/Mail/WikiPageMentionNotificationTest.php`に5件(非メンバーへの通知・無関係ユーザーの除外・再編集での非再通知・新規メンションの追加検出・リネームのみではメンション扱いにならないことの確認)。**訂正(2026-07-31)**: 「News/フォーラムコメントへのmentionは次点候補」という上記の記載は誤りと判明——Redmine本家のソース(`grep -rl acts_as_mentionable app/models/`)を確認したところ、`acts_as_mentionable`は`issue.rb`(`description`)・`journal.rb`(`notes`)・`wiki_content.rb`(`text`)の3モデルにしか組み込まれておらず、News/Message/Commentのいずれにも@mention機能自体が存在しない。つまり本アプリが課題・Journal・Wikiページの3箇所に実装済みの時点で@mention機能はRedmine本家と完全に一致しており、追加実装すべき対象は無い(実装するとRedmineには無い新機能の追加になってしまう)——「親子並べ替えUI」「Gantt上のドラッグ&ドロップ」と同種の、呼び出し元/実装元を確認せず「次点候補」として記載してしまっていた誤り。グループ宛のメンション(本家も非対応)・メンションされたユーザーの自動Watch化(本家も行わない)はいずれもスコープ外。テストは`tests/Unit/Support/Mail/MentionParserTest.php`(12件、抽出/diff/コード・引用除外の単体テスト)と`tests/Feature/Mail/IssueMentionNotificationTest.php`(8件、非メンバーへの通知・権限なしユーザーの除外・mail_notification=noneの除外・コメント経由のメンション・再編集での非再通知・新規メンションの追加検出・自己メンションのno_self_notified・空配列の早期return)の計20件。**発見した既存の未解決事項(本イテレーションのスコープ外、記録のみ)**: `resources/views/mail/issues/notification.blade.php`はJournalの`notes`をprivate_notesかどうかに関係なくそのままメール本文に描画しており、`view_private_notes`権限を持たない受信者にも非公開コメントの内容が届きうる状態だった(メンション機能実装以前からの既存の欠落、通常の階層別通知でも同様に発生する)。メンション機能はこの受信者プールを「プロジェクトメンバー/ウォッチャーのみ」から「該当issueを閲覧できる任意のユーザー」に広げる(Redmine本家の`notified_mentions`が`notified_users`と対等unionされる設計上、意図的な仕様)ため、非公開コメント中で第三者を`@login`メンションした場合に露出範囲がわずかに広がる。メール本文への`view_private_notes`チェック自体の追加は本イテレーションのスコープ外(全通知経路に影響する既存の設計課題であり、@mention固有の修正ではないため)、次点の独立項目として記録 A4-14(2026-09-20): 自動Watchを個人設定`auto_watch_on`(`issue_created`=自分が作成、`issue_assigned_to_me`=自分が担当、`issue_contributed_to`=自分がコメント/更新)で切替可能に。`IssueService::autoWatch()`が新規作成・担当者変更・コメント/変更の各契機で、有効なユーザーの設定を見て追加。**既定は作成+担当(従来どおり)**、Redmineの既定(作成のみ)とは異なる。設定`default_users_auto_watch_on`で新規ユーザーの既定を指定。Redmineが要求する`add_issue_watchers`権限の確認はしない(従来どおり)。

### Issue Relations(関連課題)

| 機能 | 状態 | 備考 |
|---|---|---|
| データモデル | done(2026-07-21) | 課題詳細画面に「関連課題」セクションを追加。`manage_issue_relations` 権限で作成/削除を保護、閲覧は `view_issues` があれば誰でも可。追加時は対象課題IDを指定し、対象課題を閲覧できない場合(別プロジェクトで権限なし等)は403、自己参照・DB一意制約違反(重複)はバリデーションエラーとして表示 |
| 関連タイプ | partial(2026-07-22) | relates/blocks/duplicates/precedes/follows/**copied_to**(2026-07-22追加)。逆方向の blocked/duplicated/copied_from は依然として表示側のラベル反転のみ(新規Enum値は追加せず、blocks/duplicates/copied_toは表示側でfrom/to方向に応じたラベル反転)。precedes/followsは元々ユーザーが方向を選んで別々に保存する設計のため反転不要。`copied_to`はRedmineの`Issue#after_create_from_copy`と同じく`IssueService::copy()`実行時に常に自動作成(コピー元→コピー先、ユーザーが「関連追加」フォームから手動選択することは不可 — Redmine同様システム生成専用)。削除時のJournal記録(`journalizeRelation()`)も`copied_to`→`copied_from`の反転に対応。単発の`?copy_from=`プリフィル機能(既存の「似た課題から始める」機能)は元々関連を含め何も複製しない設計のため対象外のまま |
| precedes/follows の遅延日数(delay) | done(2026-07-23) | `issue_relations.delay`列を追加。関連追加フォームで先行/後続選択時のみ入力欄を表示し、それ以外の種別では保存時にRedmine同様nullにリセット。関連一覧に「X日後」として表示。**遅延日数に基づく日付の自動リスケジュール計算**(下の行に別記載)も実装済み |
| 関連日付からの自動リスケジュール・循環/プロジェクト間検証 | done(2026-07-24) | Redmineの`IssueRelation#validate_issue_relation`相当のうち検証系を実装: プロジェクトをまたぐ関連はデフォルト拒否(`cross_project_issue_relations`設定でオプトイン、Redmineの`Setting.cross_project_issue_relations`相当)、祖先/子孫関係にある課題同士の関連付けを拒否(`descendantIds()`利用)、`relates`の逆方向重複を拒否、`blocks`の直接循環(相互ブロック)を拒否。**`precedes`/`follows`チェーン全体の循環検出を追加(2026-07-24)**: Redmine本家の`Issue#would_reschedule?`相当(`IssueRelation::wouldCreateCycle()`、対象課題から`precedes`/`follows`チェーンを幅優先で辿り、新規関連の相手側に到達するかを判定)を実装 — 直接の往復だけでなく、A→B→Cのような多段チェーンを閉じるC→Aのような循環も拒否する。Web UI(`issues/show.blade.php`の`addRelation()`)とAPI(`StoreIssueRelationRequest`)の両方に同一ロジックを適用(元々両者は同じバリデーションロジックを別々に複製する設計のため、今回もその規約を踏襲)。Redmine本家の`would_reschedule?`が併せて辿る親子階層(leaves/ancestors)への伝播は対象外 — 本アプリの`IssueService::rescheduleSuccessors()`自体が既に`precedes`/`follows`チェーンのみに意図的にスコープを絞っている(既存の簡略化2点は下記のまま)ため、循環判定もそれと一貫させた。安全策として`IssueService`のリスケジュールカスケードと同じ考え方の最大50ホップ上限(独立した定数として実装、意図的にIssueServiceの定数とは結合させていない)を追加。**関連日付からの自動リスケジュール計算**を実装(`IssueService::rescheduleSuccessors()`/`rescheduleFromRelation()`、Redmineの`Issue#reschedule_following_issues`/`IssueRelation#set_issue_to_dates`相当): 先行課題の開始日/期日が変わるたび(`IssueService::update()`内)、または`precedes`/`follows`関連を新規作成した直後、後続課題の開始日を「先行課題の期日(無ければ開始日)+1+delay日」まで前進させる(既にそれ以降で始まっている場合は変更しない)。元の開始日/期日の日数差を保ったまま期日も追従、`precedes`→`precedes`のチェーンは`update()`の再帰呼び出しで多段カスケード。Redmineとの意図的な簡略化2点(いずれもコード側にコメントで明記): (1) 稼働日カレンダーが本アプリに存在しないため暦日ベースで計算(Redmineは稼働日ベース)。(2) 後続課題が親課題(子を持つ)の場合もRedmineのように子課題側へ伝播させず、後続課題自身の日付を直接変更(この扱いは既存のdone_ratioのような「親は導出値で編集不可」ロックが元々start_date/due_dateには無く、現状の「日付は誰でも自由編集可」という挙動と一致)。循環関連による無限再帰を防ぐため、カスケード中に一度リスケジュール済みの課題IDを再訪しない訪問済みセット+最大50ホップの上限を追加(今回の循環検出バリデーション追加により、新規作成時にこのケース自体は事前に拒否されるようになったが、既存データに対する安全策として引き続き維持)。**稼働日ベースの計算を追加(2026-09-20)**: 設定「課題トラッキング」に非稼働曜日(`non_working_week_days`)。`App\Support\Calendar\WorkingDays`(Redmineの`DateCalculation`の`working_days`/`add_working_days`/`next_working_date`)で、後続課題の最短開始日(先行課題の期日+1+遅延を稼働日で数える)と、後続課題の期間(稼働日数を保つ)を計算する。**既定は非稼働日なし(暦日計算のまま)**で、Redmineの既定(土日)とは異なる。全7曜日の指定は無視(Redmine同様)。ガントや遅延日数の表示(「N日後」)は暦日のまま。テスト: `WorkingDaysTest.php` |
| 重複課題のクローズ連動・ブロック中クローズ禁止 | done(2026-07-21) | 両方実装。ブロック中クローズ禁止は§クローズ可否のフィルタ参照。重複課題のクローズ連動は新設定`close_duplicate_issues`(デフォルトtrue)で制御、`IssueService`が open→closed の遷移(Redmineの`Issue#closing?`と同じ判定)のたびに`Issue::duplicates()`を再帰的にクローズ(相互複製の循環も、都度DBから再取得して既にクローズ済みか確認することで安全に停止する、Redmine自身と同じガード) |

### Issue Categories

| 機能 | 状態 | 備考 |
|---|---|---|
| カテゴリ機能全体 | done (2026-07-21) | `IssueCategory` モデル/`category_id`/プロジェクト単位の管理画面(`issue-categories.index`/`.form`)。`manage_categories` 権限で保護。既定担当者のヒント機能(選択時に未割当なら自動プリフィル、既存担当者は上書きしない)も実装。使用中カテゴリの削除ガード、他プロジェクトのカテゴリへの404ガードあり。課題一覧の表示列/フィルタ/CSVエクスポート、課題フォームのバリデーション(プロジェクトスコープ)、ワークフローのフィールドルール対象にも追加済み |

### Versions

| 機能 | 状態 | 備考 |
|---|---|---|
| Version CRUD・対象バージョン割当 | done(2026-07-22、ステータス訂正 2026-09-19) | **訂正**: 従来「done」と報告されていたが、実際には`VersionPolicy`のコメントに明記の通り「Files機能でのバージョン参照のみ対応、CRUD自体のUIは皆無」だった(Tracker/IssueStatusで過去に起きたのと同種の誤報告)。今回`versions/{index,form}.blade.php`を新規追加し、`manage_versions`権限でのCRUDを実装。対象バージョン割当(課題側の`fixed_version_id`選択)自体は元々課題フォームで動作済み |
| open/locked/closed ステータス | done(2026-07-22) | Redmineの`Issue#assignable_versions`相当を実装: 課題フォームの対象バージョン欄はオープンなバージョンのみ新規選択可能とし(ロック中/クローズ済みは除外)、既にそのバージョンが設定済みの課題は変更後も選択肢を維持。バリデーションも同じ許可リストで照合。`close_completed_versions`相当も実装: `Version::isCompleted()`(クローズ済み、または期日超過かつ未クローズの課題なし)+`Project::closeCompletedVersions()`(open/lockedのうち完了したものを一括クローズ)。バージョン一覧に管理者向けボタンを配線(Redmine同様、自動発火ではなく明示操作) |
| バージョン共有範囲(none/descendants/hierarchy/tree/system) | done(2026-07-22) | Redmineの`Version::VERSION_SHARINGS`を移植。`versions.sharing`列(enum、既定`none`)+`VersionSharing` enumを追加。`Project::sharedVersions()`がRedmineの`Project#shared_versions`をそのまま(kalnoy/nestedsetの`_lft`/`_rgt`列に対して)再現: 自プロジェクトの全バージョン+`system`(全プロジェクト)+`tree`(同一ツリー)+祖先の`descendants`/`hierarchy`+子孫の`hierarchy`。課題フォームの対象バージョン候補を`project->versions`から`sharedVersions()`へ変更、他プロジェクト由来のバージョンは「プロジェクト名 - バージョン名」で表示。`Version::allowedSharings()`(Redmine`allowed_sharings`相当)で選択可能な共有範囲を制限: `system`は管理者のみ、`hierarchy`/`tree`はツリー根プロジェクトで`manage_versions`を持つ場合のみ(サブプロジェクト管理者が勝手に共有範囲を広げられない)。バージョンフォームに共有セレクタ、一覧に共有バッジを追加。`rootProject()`ヘルパーをProjectに追加(kalnoyのクエリ拡張を避け素のEloquentビルダーで根を解決) |
| ロードマップ・完了率・遅延表示 | done(2026-07-23) | Redmineの`VersionsController#index`(ルート上は`roadmap`)相当。新規`versions.roadmap`ルート(`view_issues`権限、`manage_versions`とは別のアビリティ`viewRoadmap`で分離)で未完了バージョンを期日昇順(未設定は最後)に表示。`Version::issueCounts()`/`closedPercent()`/`completedPercent()`を追加(`completedPercent()`はIssueServiceの親課題進捗率ロールアップと同じ、予定工数で重み付けした平均のアルゴリズム)。期日超過は日数付きで赤字表示。課題一覧への絞り込みリンクを実装(下記「課題一覧のURLクエリ文字列によるフィルタ初期化」参照)、Redmineの`version_filtered_issues_path(version, status_id: '*'/'c'/'o')`相当を、本アプリの課題一覧が既に持つ`statusFilter`クイックトグル(未対応/完了/すべて)+`fixed_version_id`フィルタの組み合わせで再現(`versions/roadmap.blade.php`の`issuesUrl()`) |
| 予定/実績/残工数の集計 | done(2026-07-22) | `Version::estimatedHours()`/`spentHours()`/`estimatedRemainingHours()`をRedmineの同名メソッド相当で実装(予定/残工数は子課題を持つ課題を除いた末端課題のみ合算し二重計上を防止、実績工数はTimeEntry経由で階層に関わらず合算)。`versions/index.blade.php`の一覧に表示 |
| Wikiページ紐付け・既定バージョン設定 | done(2026-09-20) | Wikiページ紐付けを実装: `versions.wiki_page_title`列を追加(Redmine同様、外部キーではなくタイトル文字列で解決)。バージョンフォームでプロジェクト内のWikiページから選択(他プロジェクトのページは選択肢に出ず、バリデーションでも拒否)。一覧にリンク表示。**既定バージョン・既定担当者を実装(2026-09-20)**: 実体はRedmineの`projects.default_version_id`/`default_assigned_to_id`(`app/models/project.rb:43-44`)。`projects`に両列を追加(削除時NULL化のFK)、プロジェクト編集フォームに選択欄(既定バージョンはオープンな共有バージョン、担当者は割当可能メンバー、現在値は失効していても選択肢に残す)、新規課題フォームで`Project::usableDefaultVersionId()`/`usableDefaultAssigneeId()`を`?copy_from=`プリフィルの**後**に`??=`で適用(担当者はカテゴリ既定が優先、Redmineの`Issue#default_assign`と同順)、`Member`削除・アカウント削除で既定担当者をNULL化、REST APIの`ProjectResource`に`default_version`/`default_assignee`を追加し、`PUT /projects/{id}`で`default_version_id`/`default_assigned_to_id`を更新可能に(2026-09-20、A1-33: プロジェクトフォームと同じ選択肢 — オープンな共有バージョン/割当可能メンバー、既存の値はそのまま許可、nullで解除)。**バージョンフォームの「既定の対象バージョンにする」チェックボックスと一覧の「既定」表示も追加(2026-09-20、A1-32)**: Redmineの`Version#update_default_project_version`と同じく、チェックして保存したときだけプロジェクトの既定に設定し、外しても既存の既定は変えない(解除はプロジェクト設定から)。テスト: `tests/Feature/Projects/ProjectDefaultsTest.php` |

### Trackers

| 機能 | 状態 | 備考 |
|---|---|---|
| CRUD・並べ替え | done(2026-07-21) | `trackers/{index,form}.blade.php`。**訂正**: 当初のパリティ監査は「done」と報告していたが、実際にはモデルのみ存在しルート/画面が皆無だった(seeder/tinker以外に管理手段なし)。あわせてプロジェクト編集フォームにトラッカー選択チェックボックスが無く、UI経由で作成したプロジェクトには一切トラッカーを紐付けられない(=課題作成が機能しない)という連鎖的な欠落も判明・修正した |
| プロジェクトへの紐付け | done(2026-07-21) | `projects/form.blade.php` にチェックボックス追加、最低1つ必須 |
| トラッカー別デフォルトステータス | done(2026-07-21) | `trackers.default_status_id`。未設定時は全体の先頭ステータスにフォールバック。新規課題作成中のトラッカー切替で再計算(編集中の課題では既存ステータスを維持し変更しない) |
| トラッカー別コアフィールド非表示(ビットマスク) | done(2026-07-22) | Redmineの`Tracker::CORE_FIELDS`相当を実装。`trackers.disabled_core_fields`(JSON配列、ビットマスクではなくフィールドキー配列で保持)+トラッカー編集フォームのチェックボックス群。課題フォーム側は無効化されたフィールドをdisabled表示ではなく完全に非表示(Redmine同様)。`project_id`/`tracker_id`/`subject`/`is_private`は対象外(Redmineの`CORE_FIELDS_UNDISABLABLE`相当) |
| トラッカー/ワークフローのコピー | done(2026-07-22) | ワークフローのコピー(下の「ワークフローのコピー」行を参照)に加え、トラッカー自体の複製を実装。`trackers/form.blade.php`(新規作成時のみ)に「コピー元トラッカー」選択欄を追加、選択すると説明・既定ステータス・非表示フィールド・非公開既定値をその場でプリフィル(Redmineの`Tracker#copy_from`相当)、保存時にプロジェクトへの割り当てとカスタムフィールド紐付け(`custom_field_tracker`)もコピー先トラッカーへ複製。ワークフロールールのコピーは既存の別画面(トラッカー×ロール単位)を使う設計のまま、Redmine同様この操作には含めない |
| ロードマップ対象フラグ・デフォルト非公開・説明文テンプレート | partial(2026-07-22) | デフォルト非公開(`private_by_default`)を実装: Redmineの`Issue#safe_attributes=`と同じ条件(トラッカーに設定あり・フォームで未指定・`set_issues_private`権限あり)で新規課題の非公開チェックボックスを自動プリチェック。トラッカー切替時も再判定。**さらに(2026-07-22)**: `is_in_roadmap`(ロードマップ対象フラグ、既定true)を実装 — ロードマップ画面(`versions.roadmap`)が2026-07-22に完成したことで見送り理由が解消。トラッカー編集フォームにチェックボックスを追加し、`Version::issueCounts()`/`completedPercent()`に`?Collection $trackerIds`引数を追加(ロードマップ画面のみ`is_in_roadmap`が有効なトラッカーIDで絞り込み、バージョン自身の詳細/編集画面など他の利用箇所は全課題を対象のまま)。「説明文テンプレート」の実体はRedmineの`trackers.description`(`db/migrate/20190315102101_add_trackers_description.rb`)で、本アプリも`trackers.description`列+`trackers/form.blade.php`の入力欄で**実装済み**(2026-09-19訂正)。残る未対応なし。なお`is_in_chlog`はRedmine 7.0.0で廃止済み(`db/migrate/20210728131544_drop_is_in_chlog_column.rb`)のためパリティ対象外(2026-09-20確認) |
| カスタムフィールド紐付け | done | `custom_field_tracker` pivot |
| 使用中トラッカーの削除防止 | done(2026-07-21) | 参照している課題がある場合は削除をブロック(生のFK違反エラーではなく分かりやすいメッセージ) |

### Issue Statuses / Workflow

| 機能 | 状態 | 備考 |
|---|---|---|
| ステータス CRUD・並べ替え・`is_closed` | done(2026-07-21) | `issue-statuses/{index,form}.blade.php`。**訂正**: Trackerと同様、当初「done」と報告されていたが実際は画面が皆無だった |
| 使用中ステータスの削除防止 | done(2026-07-21) | — |
| デフォルト進捗率・ステータス変更時の一括更新 | done(2026-07-22) | `issue_statuses.default_done_ratio` + 設定「課題の進捗率」(手動/ステータスから算出)。算出モード時は`IssueService`が保存のたびに`done_ratio`をステータスの既定値で上書き(Redmineの`update_done_ratio_from_issue_status`と同じ`before_save`相当のタイミング)、フォームのスライダーも無効化。**一括更新**を実装: Redmineの`IssueStatus.update_issue_done_ratios`相当のボタンをステータス管理画面に追加(`issue_done_ratio`設定が「ステータスから算出」の時のみ表示)、既定値が設定されている全ステータスについて該当課題のdone_ratioを一括で既定値に上書き |
| ロール×トラッカー×新旧ステータスの遷移制御 | done | `WorkflowService::allowedTransitions` |
| 作成者/担当者限定の遷移 | done | — |
| ロール×トラッカー×ステータスのフィールド必須/読取専用 | done | コアフィールド+`cf_<id>` に適用 |
| ワークフロー遷移・フィールドルールの管理画面 | done(2026-07-21) | `workflows/edit.blade.php`。トラッカー×ロール×適用対象(通常/作成者/担当者)ごとに遷移グリッドとフィールドルールグリッド(コアフィールド+そのトラッカーのカスタムフィールド)を編集・保存 |
| ワークフローのコピー(トラッカー間/ロール間/一括複製) | done(2026-07-22) | Redmineの`WorkflowRule.copy`/`copy_one`相当を実装。ワークフロー管理画面に「ワークフローをコピー」欄を追加し、コピー元(単一トラッカー×単一ロール)からコピー先(複数トラッカー×複数ロールの直積、既存設定は削除して置き換え)へ`workflow_transitions`/`workflow_field_rules`を一括複製。~~Redmineの「トラッカー/ロールいずれかを省略すると全件対象」という省略記法は対象外とし、コピー元・コピー先とも明示選択必須というスコープを絞った実装~~ → **「コピー先と同じ」を実装(2026-09-20)**: バックログ作成時に「省略記法」と呼んでいたものの実体はRedmineのコピー画面の`--- 同じ ---`(`any`)選択肢。コピー元トラッカー/ロールのどちらか一方を「コピー先と同じ」にでき(各コピー先ペアが自分自身のトラッカー/ロールを元にする、`WorkflowRule.copy`の`source_tracker || target_tracker`)、両方が`any`はエラー、コピー元=コピー先のペアはスキップ。コピー先は従来どおり明示選択必須(Redmineのコントローラも空のコピー先を拒否)。テスト: `WorkflowEditTest.php` |
| 「使用中ステータスのみ表示」フィルタ | done(2026-07-22) | Redmineの`WorkflowsController#find_statuses`相当。選択中トラッカーに既存の遷移(`old_status_id <> new_status_id`)があるステータスのみをデフォルトで表示するチェックボックスを追加(デフォルトON)。遷移が未定義のトラッカーは全ステータス表示にフォールバック |
| クローズ可否のフィルタ(未完了サブタスク/ブロック関連の考慮) | done(2026-07-22) | `WorkflowService::allowedTransitions()`が`Issue::isClosable()`(未クローズの子課題、または自分をブロックする未クローズ課題があるか)で管理者含め全ユーザーのクローズ系ステータス遷移をフィルタ(Redmineの`Issue#closable?`/`new_statuses_allowed_to`と同じ規則)。現在のステータス自体は常に選択可能なまま維持。**さらに(2026-07-22)**: `Issue::isReopenable()`(祖先を`parent_id`で遡り、直近の親に限らずいずれかが未クローズでない=クローズ中なら再オープン不可、Redmineの`Issue#reopenable?`と同じ規則)+`WorkflowService::excludeUnreopenableStatuses()`(`excludeUnclosableStatuses()`と対称の実装)を追加し、閉じた親(または祖先)を持つサブタスクはオープン系ステータスへ遷移できないよう制限。現在のステータス自体は常に選択可能なまま維持(既存のクローズ側ガードと同じ保護) |

### カスタムフィールド(課題)

| 機能 | 状態 | 備考 |
|---|---|---|
| モデル・フォーマットレジストリ・トラッカー/プロジェクト適用・ロール可視性 | done | — |
| フィールド形式のカバレッジ | partial(2026-07-29訂正) | **訂正**: Redmine本家の実際の形式数は`lib/redmine/field_format.rb`で13種(String/Text/Link/Int/Float/Date/List/Bool/Enumeration/User/Version/Attachment/Progressbar)、"~12"という従来の記載はやや不正確だった。**Progressbarを追加(2026-07-29)**: `App\CustomFields\Formats\ProgressbarFormat`(0〜100にバリデーションされる整数、ストレージは`IntFormat`と同じ`value_int`)。~~Redmine本家の`ratio_interval`属性(値選択を固定刻みの`<select>`にする管理用オプション、既定値`Setting.issue_done_ratio_interval`)は意図的に対象外~~ → **`issue_done_ratio_interval`と`ratio_interval`を実装(2026-09-20)**: 設定「課題トラッキング」に刻み(5%/10%、既定10)を追加、`DoneRatioSteps`が`(0..100).step(interval)`を返し、課題フォームの進捗率(従来はスライダー`step=10`ハードコード)・一括編集・ステータスの既定進捗率(従来は数値入力)が`<select>`に。進捗率型カスタムフィールドに`custom_fields.ratio_interval`列(5/10、空欄は設定の既定)とフォームの選択欄を追加し、入力も`<select>`。刻みは選択肢の粒度のみで保存値の妥当性には影響しない(刻みに合わない既存値は課題フォーム/ステータスフォームでは選択肢に残り、カスタムフィールド入力では空表示のまま保存値は保持される)。テスト: `DoneRatioIntervalTest.php`。表示も進捗率グラフィックではなく他の数値形式と同じ素のテキスト(本アプリはネイティブの`done_ratio`自体も`issues/show.blade.php`でプレーンな「N%」テキスト表示のみでグラフィカルなプログレスバーを持たないため、それに合わせた)。**残り3形式**: User/Version(`Redmine::FieldFormat::RecordList`、選択肢がプロジェクトコンテキスト(メンバー/バージョン一覧)に依存する)は、本アプリの`FormatContract::options(CustomField $field)`が`CustomField`のみを受け取り呼び出し元の対象オブジェクト(Issueなど、ひいてはそのプロジェクト)を渡せない設計のため、正しく実装するには全フォーマット実装(既存9クラス、Progressbarを除く)にまたがるインターフェース変更が必要と判明 — 単独のwell-scoped項目には収まらないため見送り。Attachment(ファイルアップロード形式)はSpatie MediaLibrary連携が絡む大型スコープのため同じく見送り |
| regexp/min/max/default_value | done(2026-07-29) | `default_value_mode`(`fixed_date`/`date_offset`)列を追加(Redmineの同名カラム相当、実チェックアウト`/Users/sesoko/Desktop/workspace/redmine`の`app/models/custom_field.rb`/`app/views/custom_fields/formats/_date.html.erb`で仕様を確認 — この2つがRedmine自体が持つ全モードで、他に追加モードは存在しない)。`date`形式のフィールドに限り管理フォームで「絶対日付」/「相対日付(今日からの日数)」を選択可能(相対日付は符号付き整数の日数を`default_value`列にそのまま格納、負数で過去日を表現)。新規`CustomField::defaultValue()`(Redmineの`CustomField#default_value`ゲッターオーバーライド相当)が読み取り時に`今日+オフセット`を解決 — 保存時ではなく参照時に計算するため、オフセットは常に「課題作成日基準」で正しく機能する。課題フォームの新規課題プリフィル(`applyCustomFieldDefaults()`)をこのメソッド経由に変更。`date`以外の形式では`default_value_mode`は常にnullで保存(保存済みレコードのフォーマット変更ができない既存の制約と合わせ、将来的な取り違えを防止)。REST API(`CustomFieldResource`)にも`default_value_mode`を追加(生の格納値をそのまま返却、解決済みの日付はWeb UI側のみ) |
| 検索対象(`searchable`)の実効性 | done(2026-07-21) | プロジェクト内検索でstring/textカスタムフィールド値がLIKE検索される |
| 保存後のフォーマット変更禁止・多重度変更時のクリーンアップ | done(2026-07-22) | Redmineの`CustomField#field_format=`(保存済みレコードへの代入を黙って無視)と`handle_multiplicity_change`を移植。フォーマット固定は二重防御: フォーム側は編集時にセレクタを固定表示し`save()`が既存レコードの値を使用(`customized_type`と同じパターン)、モデル側は`updating`イベントで`field_format`のダーティ変更を元値に差し戻すバックストップ。多重度クリーンアップは`updated`イベントで`multiple`がtrue→falseに変わった時のみ、対象オブジェクトごとに最新(最大id)の値だけを残して削除(RedmineのEXISTS相関サブクエリをEloquentの`whereExists`でそのまま再現) |
| CustomFieldEnumeration(選択肢の位置/有効フラグ、削除時再割当) | done(2026-07-22) | **調査の結果判明**: Redmineの「リスト」形式は本アプリの`list`形式(単純な文字列配列`possible_values`)と同じ仕組みで、`CustomFieldEnumeration`は実際には別の独立したフィールド形式`enumeration`(`Redmine::FieldFormat::EnumerationFormat`)専用のテーブルだった。新規`enumeration`形式を追加: `custom_field_enumerations`テーブル(`custom_field_id`, `name`, `position`, `active`)+`CustomFieldEnumeration`モデル(`App\Models\Enumeration`とは別物)。値は選択肢のIDを`value_string`に保存(`FormatContract::castValue()`に`CustomField`を渡すようインターフェースを拡張し、IDから選択肢名への解決を可能に — 既存7形式は素通しの引数追加のみ)。カスタムフィールド管理フォームに選択肢の追加/改名/有効無効切替UIを追加。**削除時再割当**: 選択肢の「削除」は即時実行(フォーム保存を待たない独立アクション)、削除前に置き換え先選択肢を選べるドロップダウンを表示し、既存の`custom_field_values`を置き換え先へ一括更新(置き換え先未選択の場合はRedmineと異なり値を確実にnullへクリア — Redmine本家は置き換え先未指定だと存在しないIDを指したまま放置される)。**並べ替えUIを追加(2026-07-24)**: `CustomFieldEnumeration`は元々`spatie/eloquent-sortable`(`position`列、`custom_field_id`単位にスコープした`buildSortQuery()`)を実装済みだったため、モデル層の変更は不要。カスタムフィールド編集フォームの各選択肢行に▲▼ボタンを追加し、`swapOrderWithModel()`で即時(フォーム保存を待たない、削除と同じ即時実行パターン)に位置を入れ替え。ドラッグ&ドロップ(本家)ではなくボタン方式(このアプリの他の管理画面に前例のない新規パターンだが、新規JS依存を避けるため)。未保存の新規行(まだDBに存在せず`position`を持たない)には表示せず、既存選択肢のみ対象 |
| 表示列・CSV列としてのカスタムフィールド | done(2026-07-22) | 長年の意図的見送りを解消。`availableColumns()`がネイティブ列+プロジェクト適用対象の課題カスタムフィールド(`cf_{id}`キー、フィルタエンジンと同じ規約)を提供し、表示列チェックボックス・テーブル表示・CSVエクスポートのすべてで選択可能に。複数値フィールドは「, 」結合で表示。`castValue()`/`options()`の基盤整備(同日実施)により実装障壁が低下していた。カスタムフィールドでの**並べ替え**は引き続き不可(`CustomFieldFilter::isSortable()=false`、列見出しクリックは無害なno-op)。**並べ替えに対応(2026-09-20)**: 列見出しのクリックと2・3列目の並べ替えで単一値のカスタムフィールドを並べ替えできる。`CustomFieldFilter::applySort()`が`custom_field_values`への相関サブクエリ(値の列は形式ごと=数値は数値順、日付は日付順)で並べ、空欄は昇順で先頭・降順で末尾。複数値フィールドは対象外(Redmine同様)。「フィルタに使う」が無効なフィールドも並べ替え可能(`QueryFilterEngine::applySort()`の`$sortOnlyFields`)。**Redmineとの差分**: Redmineは数値の空欄を0として並べるが、本アプリは空欄を最小値として扱う。保存済みクエリ/ダッシュボードブロックでフィルタ対象外のフィールドの並べ替えは未対応。テスト: `IssueCustomFieldSortTest.php` |

### 一括編集・インポート・エクスポート

| 機能 | 状態 | 備考 |
|---|---|---|
| 一括編集(ステータス/優先度/担当者/バージョン/進捗率+共通コメント) | done | 各課題ごとに認可チェック・Journal記録 |
| ステータス一括編集の選択制約 | done(2026-09-20) | 選択課題それぞれの許可遷移(`allowedTransitions`)の共通部分だけを提示し、適用時も全課題で遷移可否を再検証。トラッカー/カテゴリ(プロジェクト内のみ)/開始日・期日/非公開(`setPrivateOn`を課題ごとに認可)/親課題(`manageSubtasks`、選択課題自身とその子孫は不可)を一括編集に追加。カスタムフィールドの一括編集は A1-06b。テスト: `IssueBulkEditFieldsTest.php` |
| 課題一覧の右クリックコンテキストメニュー | done(2026-09-20) | 行の右クリック(Alpine)で、未選択の行なら選択をその行に置換、選択済みなら選択全体を対象にメニュー表示。ステータス(選択課題共通の遷移先)/トラッカー/優先度/対象バージョン/担当者(自分・なし)/カテゴリ/進捗率は`contextUpdate()`が一括編集と同じ検証・認可(`applyBulkEdit()`)で適用し、他のフォーム入力値は破棄する。編集(単一のみ)/一括編集・コピーはフォームへのアンカー、削除は既存の一括削除。`edit_issues`が無ければ非表示。カスタムフィールド/ウォッチャー等は A1-05b。テスト: `IssueContextMenuTest.php` |
| 工数一覧の右クリックメニュー | done(2026-09-20) | 課題一覧と同じ操作(未選択行は選択を置換、選択済みは選択全体)。編集(単一)/一括編集(複数)、作業分類(`contextUpdateActivity()`が`applyBulkEdit()`の検証・認可を通す)、削除。`update`できない行は右クリック不可。Redmineにある工数カスタムフィールドのサブメニューは未対応(A8-06はこれ以上分割せず、工数カスタムフィールドの一括編集自体が本アプリに無いため)。テスト: `TimeEntryContextMenuTest.php` |
| 一括コピー・一括プロジェクト間移動・一括削除 | done(2026-07-23) | 一括プロジェクト間移動(`move_issues`+移動先`add_issues`、`IssueService::moveToProject()`)、一括削除(`delete_issues`)、一括コピー(`copy_issues`+複製先`add_issues`、`IssueService::copy()`、関連カスタムフィールド値も複製先トラッカーに応じて複製)を実装。コピー元リンク(`copied_to`関連)は元から自動作成済み。**添付ファイル/ウォッチャーの複製を追加(2026-07-23)**: `IssueService::copy()`に`copyAttachments`/`copyWatchers`引数(共にRedmine同様デフォルトtrue)を追加、一括コピーフォームにも対応するチェックボックス(共に既定でチェック済み、Redmineの一括編集フォームの`copy_attachments`/`copy_watchers`チェックボックスと同じデフォルト)を追加。添付はSpatie MediaLibraryの`Media::copy()`でファイルごと複製、ウォッチャーは`status=active`のユーザーのみ複製(Redmineの`visible_watcher_users.select{active}`相当、複製先での自動ウォッチ(作成者/担当者)との重複は`firstOrCreate`で吸収)。**子課題の複製を追加(2026-09-20)**: `IssueService::copy(copySubtasks:)`が子孫を上から順に、コピーした親の下へ複製する(一括コピーフォームの「子課題も複製」、Redmine同様既定オン)。子課題は自分のトラッカーを保つため、コピー先が使わないトラッカーの子は配下ごとスキップ。対象バージョンは「オープンかつコピー先から参照可能」な場合のみ保持、カテゴリは同一プロジェクトのときのみ、担当者は有効なコピー先メンバーのみ保持。閲覧できない(非公開等)子課題はコピーしない。子課題には`copied_to`関連を作らない(Redmine同様)。テスト: `IssueSubtaskCopyTest.php` |
| CSVインポート(列マッピング・バックグラウンド処理・進捗表示) | done | `ImportIssuesJob`, `IssueImport` |
| マッピング可能な列 | partial(2026-07-23) | カテゴリ/対象バージョン/親/非公開フラグを追加(`ImportIssuesJob::mapRowToAttributes()`、`IssueService::create()`が元々`category_id`/`fixed_version_id`/`parent_id`/`is_private`を`fillable`経由で受け付けていたため配線のみ)。カテゴリ/対象バージョンはプロジェクト内の名前一致で解決、一致しなければ行を失敗させず未設定のまま(担当者のメール解決と同じ寛容な扱い)。親課題は`#番号`で同一プロジェクト内から解決し、見つからなければ行を失敗として記録(Redmineと異なり自動作成はしない)。非公開フラグは`set_issues_private`権限をインポート実行ユーザーが持つ場合のみ反映(手動フォームが権限のないユーザーにチェックボックス自体を出さないのと同じ扱い、`FILTER_VALIDATE_BOOLEAN`で1/true/はい等を解釈)。**カスタムフィールド/遅延付き関連は引き続き対象外**(カスタムフィールドは`Issue::relevantCustomFields()`がロール可視性判定に`auth()->user()`を参照するためキュージョブ内での認可コンテキストの扱いを別途設計する必要があり、単独のwell-scoped項目には収まらないため見送り) |
| カテゴリ/バージョンの自動作成、`unique_id`による遅延親子/関連解決 | partial(2026-07-24) | **カテゴリ/バージョンの自動作成を追加**: Redmineの`IssueImport#create_categories?`/`#create_versions?`相当(オプトインのチェックボックス AND `manage_categories`/`manage_versions`権限の両方が必要、Redmine本家と同じ二重ゲート)。CSVインポート画面でカテゴリ/対象バージョンの列をマッピングし、かつ権限を持つ場合のみチェックボックスを表示。マッチしないカテゴリ/バージョン名が見つかった場合、行を失敗させず新規作成して紐付け(トランザクションでは囲まない — Redmine本家も同様に、後続のIssue自体の保存が失敗しても自動作成されたカテゴリ/バージョンはロールバックされない)。`unique_id`による遅延親子/関連解決(2パスインポート設計が必要な別スコープ)は引き続き未実装のまま |
| CSVエクスポート | done(2026-07-24) | 選択列のみ。エンコーディング(UTF-8/Shift_JIS、UTF-8はBOM付き)と区切り文字(カンマ/セミコロン/タブ)を選択可能に(Redmineの`Redmine::Export::CSV`相当)。**関連/添付/Watcher列を追加(2026-07-24)**: `DISPLAY_COLUMNS`(表示列チェックボックス・オンスクリーン表として構成・CSVエクスポートの3者で共有される単一のキー集合)に`relations`/`attachments`/`watchers`を追加。値の結合区切りはRedmine本家の`csv_value`/`csv_content`の実装差をそのまま踏襲(不統一だが本家との出力互換を優先): 関連は`", "`(`IssueRelation#to_s`形式の`"<ラベル> #<相手のID>"`をカンマ区切りで連結)、添付/Watcherは`"\n"`(セル内で1行1件)。関連ラベルは`issues/show.blade.php`の`RELATION_LABELS`と同じ日本語表記だが、ジャーナル履歴表示用の逆方向キー変換(`RELATION_JOURNAL_KEYS`)は不要なため独立して複製(ジャーナル表示とCSV表示は別のユースケースで、フラットなfrom/to両側の連結だけで足りる)。N+1回避のため、選択列に応じて`relationsFrom`/`relationsTo`・`media`(添付ファイル)・`watchers.user`を条件付きeager loading。CFの並べ替え不可と同様、これら3列も並べ替え候補の2/3列目セレクトには表示される([`DISPLAY_COLUMNS`を共有する既存の設計]が、`QueryFilterEngine`にフィールド未登録のため選択しても実質的にno-op(既存のCF列と同じ挙動、クラッシュはしない) |
| PDFエクスポート・Atomフィード | partial(2026-07-30訂正) | **訂正**: 本行はAtomフィードも含め「missing」のまま放置されていたが、実際にはAtomフィード(`issues.atom`、`IssueAtomController`)は既に実装・テスト済みだった(`tests/Feature/Issues/IssueAtomFeedTest.php`で確認、一覧画面にAtomリンクも設置済み)。~~フィルタ/ソート/グループ化状態を反映しない固定「最近更新されたオープン課題」ビューである点は`IssueAtomController`のdocコメントに既述の意図的な簡略化。~~ → **フィルタ/ソートを反映(2026-09-20)**: 一覧のURL状態(`statusFilter`・`activeFilterKeys`/`filterOperators`/`filterValues`・最大3段の`sortKey*`)を`ListQueryString`経由で読み、一覧画面のAtomリンクも現在の状態を引き継ぐ(`atomUrl`)。Redmine同様、フィルタとソートが`feeds_limit`に入る課題を決め、エントリは更新日の新しい順に表示。不正なパラメータは無視、可視性スコープは常に適用。**未対応**: グループ化の反映(Redmineも反映しない)。テスト: `IssueAtomFeedTest.php` ~~PDFエクスポート~~ → **done(2026-07-30)**、詳細は§0.5「ユーザー判断待ち」表の該当行参照。2026-09-19注記: doneなのは課題単体PDF(`issues.pdf`)のみで、フィルタ/列を反映した一覧PDFは未実装(バックログA1-29)。**課題一覧のPDFエクスポートと`issues_export_limit`を追加(2026-09-20、A1-29)**: 一覧の現在のフィルタ/並べ替え/選択列を反映したA4横のPDF(`exportPdf()`、`pdf.issues`)。CSVとPDFはどちらも設定「課題一覧のエクスポート件数の上限」(既定500、上限5000)で先頭から打ち切り、超過時は一覧に注意を表示する。**Redmineとの差分**: 上限の最大値(5000)は本アプリの安全弁。長文列(説明/最新コメント)は各行の下に600文字まで。テスト: `IssueListPdfExportTest.php`。以下は従来の記録 |

---

## 2. プロジェクト・管理機能・認証

対象: Projects, Roles & Permissions, Members, Groups, Custom Fields(全対象種別), Enumerations, Settings, User管理, LDAP, 2FA, 登録モード。

### Projects

| 機能 | 状態 | 備考 |
|---|---|---|
| プロジェクト一覧 | partial(2026-07-22) | 名前/識別子の検索とステータス(アクティブ/クローズ/アーカイブ済み)フィルタを追加、いずれか使用時は全プロジェクトを対象にした25件/ページのページネーション表示に切替(`can('view')`を通した後にコレクション側でページ分割するため、非表示プロジェクトが件数に混入しない)。**さらに(2026-07-22)**: フィルタ未使用時の表示を「ルートのみのフラット表示」から「全プロジェクトをネストセットのツリー順(`withDepth()`+`defaultOrder()`)でインデント表示」に変更、子プロジェクトのツリー表示を実装。インデント幅はGanttビューの`8 + depth * 16`px方式を踏襲(`16 + depth * 16`px、`px-4`の既定16pxを深さ0の基準値とする)。深さは真の(可視性を考慮しない)絶対深度のため、親が非表示なプロジェクトの子はインデントされた状態で親なしに表示され得る意図的な簡略化(Redmine本家はより精緻な可視祖先相対インデント)。フィルタ使用時は引き続きフラット・アルファベット順のまま(検索は文脈より一致結果を優先する設計判断) **ツリー表示の是正(2026-09-20)**: 従来の2つの簡略化(絶対深度でのインデント/フィルタ使用時のフラット・アルファベット順)を解消。`Project::defaultOrder()`のツリー順を常に使い、`display_level`(一覧内に存在する祖先の数、Redmineの`project_tree`と同じ)でインデント — 親が閲覧不可/フィルタで除外された場合は最上位として表示され、検索結果もツリー形状を保つ。テスト: `ProjectListFilterTest.php` A9-07/A14-07(2026-09-20): ヘッダーにプロジェクトジャンプボックス(`<x-project-jump-box>`、`details`+Alpineの即時絞り込み)を追加。「最近使ったプロジェクト」(個人設定`recently_used_projects`件、既定3、0で無効。プロジェクトページのGETで`RecordRecentProject`ミドルウェアが記録、変更時のみ保存)・「ブックマーク」(ブックマーク済みは最近から除外)・「すべてのプロジェクト」(所属する有効なプロジェクト、最大200件)。Redmineの`ProjectJumpBox`に相当。
| プロジェクト作成 | partial(2026-07-22)→**グローバル権限`add_project`を追加(2026-09-20、A3-01)**: トップレベルのプロジェクト作成は、管理者に加え、いずれかの役割(どのプロジェクトのメンバー役割か、グループ経由か、設定した「非メンバー」ビルトイン役割)が`add_project`を持つログイン済みユーザーも可能(`AuthorizationService::canGlobally()`、`ProjectPolicy::create`)。作成者は`new_project_user_role_id`(未設定なら最初の付与可能な役割)のメンバーになる(従来どおり)。匿名は不可。REST `POST /projects`とプロジェクト一覧の「新規プロジェクト」ボタンも同じ判定。テスト: `AddProjectPermissionTest.php`。以下は従来の記録: 管理者専用(ポリシーで一般ユーザーは常に `false`)。作成時のデフォルトモジュール/トラッカー/**公開設定**を設定「プロジェクト」タブ(`default_projects_modules`/`default_projects_tracker_ids`/`default_projects_public`)から適用するよう追加(Redmineの`Project#initialize`と同じく新規作成フォームの初期値のみで、作成時にプロジェクトごと上書き可能)。トラッカー設定が未設定の場合は全トラッカーにフォールバック、公開設定が未設定の場合はRedmineの既定値と同じ`true`にフォールバック |
| ホームページ(`projects.homepage`) | done(2026-09-20) | Redmineの`projects.homepage`(最大255文字)を追加。プロジェクト編集フォームの入力欄、概要画面のリンク表示(`Project::homepageUrl()`が http/https/ftp/ftps/mailto のみリンク化、`javascript:`等はテキスト表示 — Redmineの`uri_with_safe_scheme?`相当)、REST API(`ProjectResource`と作成/更新リクエスト)に対応。**未対応**: プロジェクト一覧クエリの列(バックログA2-03)。テスト: `tests/Feature/Projects/ProjectHomepageTest.php` |
| プロジェクト編集 | done | 名前/識別子/説明/公開設定/モジュール/プロジェクトカスタムフィールド |
| サブプロジェクト | done(2026-07-22) | プロジェクトフォームに親プロジェクト選択欄を追加(自分自身/子孫は選択肢から除外しサイクルを防止)し、作成・再配置ともUIから可能に。プロジェクト詳細に`add_subprojects`権限保持者向け「サブプロジェクトを追加」リンクを追加(`?parent_id=`付きで作成フォームへ、`ProjectPolicy::createSubproject`で認可)。トップレベルプロジェクト作成自体は引き続き管理者専用のまま |
| 有効モジュール | done | `Project::syncModules()` |
| アーカイブ/アーカイブ解除 | done(2026-07-22) | 詳細画面にボタンを配線(管理者専用、`ProjectPolicy::archive`)。`AuthorizationService::can()`がRedmineの`Project#allows_to?`と同様、アーカイブ中プロジェクトへの全操作を拒否(管理者はGate::before経由で従来どおりバイパス)。`ProjectPolicy::view()`もis_publicより優先してアーカイブを弾くため一覧・詳細から実質不可視化 |
| クローズ/再オープン | done(2026-07-22) | 詳細画面にボタンを配線(`close_project`権限、ステータスバッジ表示)。`Permission::$readOnly`フラグ(view_*/browse_repository等に付与)を追加し、クローズ中はモジュール権限のうち読み取り専用以外(課題作成・編集・工数記録等)を拒否。~~`close_project`/`edit_project`等プロジェクト管理系(module未指定)権限は対象外とし、クローズ中でも再オープンや設定変更は可能なままにする実装上の判断~~ → **Redmineの`:read => true`分類に一致(2026-09-20)**: クローズ中に許可されるのは`readOnly`フラグの権限のみに変更(従来は「module未指定は全て免除」)。Redmineの`preparation.rb`で読み取り扱いの`edit_project`/`close_project`/`delete_project`/`select_project_modules`は引き続き可能(再オープン・設定変更・削除ができる)。**新たにクローズ中は不可**: `manage_members`・`add_subprojects`・`manage_public_queries`(従来は過剰に許可されていた)。**新たにクローズ中も可**: `edit_own_issue_notes`・`delete_own_messages`(Redmineが読み取り扱い)。管理者は従来どおり全て可(本アプリの`can()`は管理者を最初に許可、Redmineは管理者もクローズ中の書き込みを拒否する — 意図的な差分として据え置き)。テスト: `ProjectStatusTest.php` |
| プロジェクト削除 | done(2026-07-30) | Redmine本家の`ProjectsController#destroy`(`/Users/sesoko/Desktop/workspace/redmine`の`app/controllers/projects_controller.rb`・`app/models/project.rb`の`deletable?`・`lib/redmine/sudo_mode.rb`まで確認)を参照実装として移植。**認可**: `ProjectPolicy::delete()`に`$project->isLeaf()`を追加(元々`delete_project`権限チェックのみだった) — Redmine本家の`deletable?`(`user.admin? => true` / それ以外は`delete_project`権限 && `leaf?`)と同じ非対称ルール。管理者は`Gate::before`の既存の全許可バイパスにより、この`isLeaf()`制約自体を経由せず子孫を持つプロジェクトも削除可能(Redmine本家の管理者無条件許可と同義)。**カスケード削除**: 本アプリが既に依存している`kalnoy/nestedset`パッケージ(`Project`は元々`NodeTrait`を使用済み)の`deleted`イベントフックが子孫プロジェクトを自動削除し`_lft`/`_rgt`の隣接リスト値も再採番するため、当初懸念していた「nested set cascadeの設計」自体が不要だった(vendorソース`vendor/kalnoy/nestedset/src/NodeTrait.php`の`deleteDescendants()`まで確認して判明)。各プロジェクト配下のテーブル(issues/versions/wiki_pages/repositories/members等、全20テーブル)は元々すべて`project_id`に`cascadeOnDelete()`が設定済みで、DB外部キーレベルでの一括削除が保証されている。**advisorレビューで見つかった検証不足**: 当初「子プロジェクトも削除される」ことを親子1階層のみのテストで確認していたが、`projects`テーブルの`parent_id`外部キー自体は`nullOnDelete()`(削除時にNULL化、カスケード削除ではない)であるため、1階層だけのテストでは「kalnoy側のカスケードが効いている」のか「単に`parent_id`がNULL化されて孤児になっただけ」なのか区別できないという指摘を受けた。孫プロジェクトまで含む3階層構成で`descendants()->count()`(中間値)を削除前に直接アサートしてツリーが本当にネストしていることを確認した上で、孫まで消えることを検証するテストを追加(`nullOnDelete()`は孫には推移しないため、孫が消えることは`kalnoy`側の真のカスケードでしか説明できない)。**残存する設計上の注意点**: `projects.parent_id`の`nullOnDelete()`は、`kalnoy/nestedset`のモデルレベルカスケード(`deleted`イベントで子孫を先に`delete()`)により通常の削除フローでは実質到達しないデッドコード相当になった(`mail_notification`列のDBデフォルト値が`creating()`フックにより到達不能になったのと同種の状況、詳細は本ファイル302行目「ユーザー」節参照) — 実害はないため据え置くが、将来「なぜNULL化なのに孫が消えるのか」と混乱しないよう明記しておく。**Web UI**: `projects/show.blade.php`に`deleteProject()`アクションを追加。Redmine本家の`require_sudo_mode`(直近のパスワード再確認)+`params[:confirm] == identifier`(プロジェクト識別子の入力一致)の二重確認を再現。パスワード再確認ロジックは既存`profile/index.blade.php`の`requirePasswordConfirmation()`と全く同じ実装が複製されていたため、新規`App\Support\Auth\RequiresPasswordConfirmation`トレイトに切り出して両方から共有(重複除去、既存の`profile.index`側テストで非破壊を確認済み)。`deleteConfirmationInput`はクライアント改変可能なpublicプロパティのため、アクション内部で毎回サーバー側比較(Livewireの標準方針通り)。**REST API**: `DELETE /projects/{project}`を追加(`api.projects.destroy`)。Redmine本家はAPIリクエストに対して`confirm`パラメータもsudo modeも無条件でスキップする(`api_request?`で早期`true`、`Redmine::SudoMode::SudoRequestFilter#before`も同様)ため、API版は`Gate::authorize('delete', $project)`のみで確認入力を要求しない(APIキー/トークン自体が確認の代わり、という設計をそのまま踏襲)。テストはWeb UI(9件: 管理者削除・カスケード確認・サブプロジェクト連鎖削除・非admin leafのみ許可・非admin非leaf拒否・無権限拒否・識別子不一致拒否・sudo未確認リダイレクト・非表示確認)とAPI(4件)の実読み取り経路E2Eで検証、DBから実際に消えていることを`Model::find()===null`で直接確認(戻り値やHTTPステータスだけでなく実データの消失を確認) |
| プロジェクトのコピー | **対象外と正式決定**(2026-07-30) | 詳細は§0.5「ユーザー判断待ち」表の「プロジェクト削除/コピー」行参照。Redmine本家`Project#copy`系(9メソッド・約265行、`copy_issues`の二周走査によるidリマップ+双方向IssueRelation付け替えを含む)の規模を実測した上でMercurial/CVS/Bazaar/Filesystemアダプタと同格の大型据え置きと判断 |
| ブックマーク | done(2026-07-21) | `project_bookmarks`テーブル+`User::bookmarkedProjects()`。詳細画面とプロジェクト一覧の★ボタンでトグル、一覧に「ブックマークのみ表示」フィルタ |
| プロジェクト別 Enumeration(工数種別等)の上書き | done(2026-07-22) | Redmineと同じくTimeEntryActivityのみ対象(`Project#activities`/`create_time_entry_activity_if_needed`相当)。`enumerations`に`project_id`/`parent_id`を追加し、新規`projects/{project}/activities`画面(`edit_project`権限)でプロジェクトごとに有効/無効をトグル(Redmine同様リネームは不可、グローバル値の名前をそのまま複製)。状態がグローバル既定と一致する場合は上書き行を作らず/削除し、`Project::activities()`が実効的な一覧を解決。工数記録フォーム・課題フォームの工数記録欄・工数一覧の絞り込みドロップダウンをすべてこの実効一覧に差し替え |
| メンバー管理 | done(2026-07-22) | ~~グループをメンバーとして追加できない~~ → **done**(2026-07-21)。「ユーザー/グループ」切替式フォームでグループもロール付きで追加可能に。既存メンバーのロール編集は「編集」リンクでフォームにプリフィルして更新可能(~~グループメンバーは編集フォームの対象外、削除→再追加のみ~~ → **グループメンバーもロールを編集可能(2026-09-20)**: 編集時は対象グループ名を読み取り専用で表示し、管理可能ロールの制限・「最低1ロール」検証は通常メンバーと同じ。テスト: `ProjectMemberManagementTest.php`)。~~メールアドレス完全一致でのみ追加(候補選択なし)~~ → **done**(2026-07-22): 名前/メールの部分一致で候補を絞り込むデバウンス付き検索(`wire:model.live.debounce.300ms`、既存の`projects/index.blade.php`の検索と同じ規約)+クリックで選択するドロップダウンに変更(Redmineの`autocomplete_for_user`相当)。候補は既存メンバーを除外 |
| 課題カテゴリ | done (2026-07-21) | プロジェクト設定画面から管理(§ Issue Categories参照) |

### ロール・権限

| 機能 | 状態 | 備考 |
|---|---|---|
| ロール一覧/作成/編集(権限チェックボックス) | done | Anonymous/Non-member向けの権限フィルタも正しく機能 |
| ロール削除 | done | — |
| ロールのコピー作成 | done(2026-07-21) | 一覧の「コピー」リンクが`?copy_from=<id>`付きで新規ロールフォームを開き、名前(「〜のコピー」接尾辞)と権限をプリフィル。builtin種別はコピーされない(このフォーム自体がbuiltinを一切設定しないため) |
| 課題の閲覧範囲(全て/デフォルト/自分のみ) | done(2026-07-21) | ロール編集フォームにセレクトを追加。詳細は §Issues本体参照 |
| 工数エントリ閲覧範囲 | done(2026-07-21) | `Role.time_entries_visibility`(all/default/own)。課題閲覧範囲と同一パターンで`TimeEntryPolicy::view`と一覧クエリに適用 |
| ユーザー閲覧範囲 | done(2026-07-30) | `Role.users_visibility`(all/members_of_visible_projects)。詳細は§0.5「ユーザー閲覧範囲」行参照 |
| 「課題に割当可能」フラグ | done(2026-07-21) | `Role.assignable`(デフォルトtrue) + `Project::assignableUsers()`。課題フォーム/一覧の一括編集/カテゴリの既定担当者セレクトに適用。バリデーション自体は緩めたままで、既にnon-assignableなメンバーへ割当済みの課題を編集しても既存の割当は保持される(意図的) |
| ロールの既定作業分類(`roles.default_time_entry_activity_id`) | done(2026-09-20) | Redmineの`Role#default_time_entry_activity`を追加(削除時NULL化のFK)。ロール編集フォームに共有・有効な作業分類の選択欄(Anonymousロールでは非表示、Redmineの`roles/_form.html.erb`と同じ)。`Project::defaultActivityId(?User)`がRedmineの`TimeEntryActivity.default_activity_id`と同順で解決: 利用可能な作業分類が1つならそれ→ユーザーのメンバーロール(position昇順)の既定→プロジェクト固有の既定→全体の既定。ロールが指す共有分類は、プロジェクトが上書きしていればその上書き行に解決、無効化していればスキップ。工数入力フォームと課題編集画面のインライン工数欄の初期値に適用。テスト: `tests/Feature/TimeEntries/DefaultActivityTest.php` |
| 管理可能ロールの制限 | done(2026-07-22) | Redmineの`Role#managed_roles`(自己参照多対多)+`all_roles_managed`フラグ+`Member#set_editable_role_ids`相当。新規`role_managed_role`ピボットテーブルと`roles.all_roles_managed`(既定true)を追加。`AuthorizationService::managedRolesFor()`が管理者=全ロール、`manage_members`権限を持つロールのうち`all_roles_managed`があれば全ロール、無ければ`managedRoles()`の和集合を解決。プロジェクトのメンバー管理画面はこの範囲外のロールをチェックボックスとして表示せず、保存時も範囲外のロールIDは`Rule::in()`で拒否した上で、既存メンバーが元々持っていた範囲外ロールは編集時も維持(サブセット管理者が権限外ロールを意図せず剥奪できないようにする)。ロール管理フォームにも「全ロールを管理可能」チェックボックス+管理可能ロール選択を追加 |
| 権限一覧レポート・一括更新マトリクス | done(2026-07-22) | `roles/report.blade.php`(`/roles/report`)で全ロール×全権限のマトリクスを表示し、1回の保存で全ロールの権限を一括更新。各ロールで実際に付与可能な権限(匿名/非メンバー組み込みロールは`PermissionRegistry::assignableTo()`でサブセットに制限)以外はチェックボックス自体を表示せず、保存時もサーバー側で許可リストを再計算するため、無効な権限をクライアント側から注入しても反映されない |

### グループ

| 機能 | 状態 | 備考 |
|---|---|---|
| グループ CRUD | done | — |
| グループメンバー管理 | done(2026-07-22) | ~~メール完全一致のみ、オートコンプリートなし~~ → プロジェクトメンバー管理と同一パターンの名前/メール部分一致検索+選択ドロップダウンに変更(既存グループメンバーは候補から除外) |
| グループをプロジェクトにロール付きで割当 | done(2026-07-21) | `AuthorizationService::memberRolesFor()`は元々グループ経由のロールを解決していた(未使用だったのみ)。プロジェクトのメンバー管理画面からグループを追加できるようにUIを配線 |
| グループ用カスタムフィールド | done(2026-07-22) | `CustomizableType::Group`を追加。グループはプロジェクト/ロールを持たない管理者専用リソース(`GroupPolicy`が全メソッドdeny、管理者のみ`Gate::before`経由)のため、Issue/Project/Versionと異なりロール別可視性フィルタは不要 ― `Group::relevantCustomFields()`は単純に対象タイプの全カスタムフィールドを返す。`groups/form.blade.php`で入力/保存(既存パターンを踏襲) |

### カスタムフィールド

| 機能 | 状態 | 備考 |
|---|---|---|
| Issue用カスタムフィールド | done | トラッカー/プロジェクト範囲、ロール可視性、必須/複数値 |
| Project用カスタムフィールド | done | ロール可視性は `Project::relevantCustomFields()` で反映 |
| Version用カスタムフィールド | done(2026-07-22) | `CustomizableType::Version` を追加。`Version::relevantCustomFields()` はVersion自身がロール/メンバーを持たないため所属`project`経由でロール可視性を解決。`versions/form.blade.php` で入力/保存(`projects/form.blade.php`と同一パターン) |
| User/DocumentCategory用 | done(2026-07-23) | `CustomizableType`は Issue/Project/Version/Group/TimeEntryActivity/Document/DocumentCategory/**User**の8種。TimeEntryActivityに続きDocumentCategoryを追加(Redmineの`TimeEntryActivityCustomField`/`DocumentCategoryCustomField`相当) ― `Enumeration`モデルはIssuePriority/TimeEntryActivity/DocumentCategoryの3種を1テーブルで表現するため、`relevantCustomFields()`を`match($this->type)`でTimeEntryActivity→`CustomizableType::TimeEntryActivity`、DocumentCategory→`CustomizableType::DocumentCategory`と振り分け(IssuePriorityは対象外、Groupと同様ロール可視性フィルタも不要)。両タイプの相互非流出(TimeEntryActivity用フィールドがDocumentCategoryフォームに現れない等)をテストで確認。`enumerations/form.blade.php`は完全に型非依存の共通フォームのため変更不要。**User用(2026-07-23追加)**: Groupと同一パターン(`HasCustomFields`+`customizableType()`+`relevantCustomFields()`、ユーザー管理は管理者専用リソースのためロール可視性フィルタ不要)。`MorphMapServiceProvider`に`'user' => User::class`を追加(Userは従来どの多態関連にも参加していなかったため新規追加)、`users/form.blade.php`に入力欄を追加(`groups/form.blade.php`と同一パターン) |
| フィールド形式 | partial(2026-07-22) | string/text/int/float/date/bool/list/enumeration/**link**の9種(2026-07-22にlinkを追加)。`App\CustomFields\Formats\LinkFormat`はStringFormatと同じ保存/検証(Redmine同様、保存時の追加URL検証は行わない)だが、表示側で新規Bladeコンポーネント`<x-custom-field-value>`を介してクリック可能なリンクとしてレンダリング(スキームなしの値には`http://`を自動付与、`LinkFormat::href()`)。この表示コンポーネントは課題詳細・文書詳細の2箇所で共用。~~一覧画面(課題一覧の列表示)ではリンク化せずプレーンテキストのまま(意図的なスコープ限定)~~ → **課題一覧の列でもリンク化(2026-09-20)**: `issues/index.blade.php`のCF列セルが`<x-custom-field-value>`で描画(`customFieldCellValues()`)、危険なスキームは`LinkFormat::href()`で無害化、複数値のLinkフィールドはテキストのまま、CSVは従来どおりプレーン。テスト: `IssueCustomFieldColumnTest.php`。user/version/attachment は引き続き未対応(選択ウィジェットが別途必要なため) |
| custom_field_enumerations(選択肢の管理された一覧) | done(2026-07-22) | 上の「カスタムフィールド(課題)」節の同名行を参照。新規`enumeration`フィールド形式として実装、クエリ/フィルタエンジン(`CustomFieldFilter`)にも対応(選択肢IDでフィルタ、ラベルは選択肢名) |
| default_value/regexp/searchable/editable・visible フラグ、「全プロジェクト対象」 | done(2026-07-24) | 「全プロジェクト対象」は元々実装済み(対象プロジェクト未選択時は`CustomField::appliesToProject()`が全プロジェクト扱い)。`default_value`(新規課題作成時に自動入力、`prefillFromCopySource`の後に適用され上書きしない)・`regexp`(保存時に不正な正規表現を検証)・`searchable`をフォームに追加(2026-07-22、モデル/カラムは既存、フォーム未配線だった)。**`editable`フラグを追加(2026-07-24)**: 新規`editable`カラム(既定true)+`CustomField::editableBy(?User $user)`(自分自身が`editable`か、または操作者が管理者なら常にtrue、Redmineの`CustomField#editable?`+管理者バイパス相当)。7箇所すべての`<x-custom-field-input>`呼び出し(課題/プロジェクト/バージョン/グループ/ユーザー/文書/列挙値フォーム)の`:disabled`に反映(課題フォームは既存のワークフロー`read_only`判定とOR結合)。**サーバー側でも新規`CustomField::filterEditableValues()`により、編集不可フィールドへの送信値を`save()`で保存前に必ず除去**(disabled属性はあくまで表示上のヒントであり、細工したLivewireリクエストによる直接のプロパティ変更は防げないため、7箇所すべてのフォームで`customFieldValues`をこのフィルタ経由に統一。ワークフローの`read_only`判定は既存の別機構でサーバー側強制が元々ないが、`editable`は新規実装のためサーバー側強制まで含めて実装)。**`visible`フラグは別途カラム追加不要と判明**: 既存の`custom_field_role`ピボット+`CustomField::visibleToRoles()`(空ピボット=全ロールに表示)がRedmineの`visible`真偽値+`custom_fields_roles`の2つを1つの表現に統合済み(Redmine側も`visible=true`保存時に`roles`を自動クリアする設計になっており、実質的に同一メカニズム)。管理フォームの「閲覧可能ロール(未選択=全ロールに表示)」チェックボックス群が実質的にこの機能のUI。旧チェックリスト行の「visibleフラグ未実装」という記載は誤り(調査時に見落とされていた)だったため訂正 **`is_filter`(フィルタとして使用)を追加(2026-09-20)**: `custom_fields.is_filter`列・フォームのチェックボックス・API(`is_filter`)・`IssueFilterFieldRegistry`(プロジェクト内/横断の両方)が`is_filter`のフィールドだけをフィルタ候補にする。**Redmineとの意図的な差分**: Redmineの既定は`false`だが、本アプリは従来すべてのカスタムフィールドがフィルタ可能だったため既定を**true**にして既存フィールドの挙動を維持。フィルタでなくなったフィールドも列としての選択・値の保持は従来どおりで、保存済みクエリのそのフィルタ条件は無視される。テスト: `CustomFieldIsFilterTest.php` |

### Enumerations(優先度・工数種別・文書カテゴリ)

| 機能 | 状態 | 備考 |
|---|---|---|
| モデル(position/is_default/active) | done | `Enumeration` + `EnumerationType`、各所で消費されている |
| 管理画面 | done(2026-07-21) | `enumerations/{index,form}.blade.php`。`/enumerations/{type}` を `EnumerationType` のネイティブ暗黙Enumルートバインディングで解決し、タブ切り替えで3種別を編集 |
| 種別ごとに既定値を1つだけに制限 | done(2026-07-21) | `Enumeration::makeDefault()` |
| 使用中の値の削除防止 | done(2026-07-21) | IssuePriority(`issues.priority_id`)・TimeEntryActivity(`time_entries.activity_id`)は使用中なら削除不可。DocumentCategoryは `nullOnDelete()` のためガード不要 |

### アプリケーション設定(Redmine 7.0.0 の120キー中、本アプリ実装は48キー — 2026-09-19機械照合、バックログA-5)

| Redmineのタブ | 状態 | 備考 |
|---|---|---|
| 全般 | partial(2026-07-30) | `app_title`, `default_issues_per_page`に加え、**`welcome_text`・`activity_days_default`を追加(2026-07-30)**。`welcome_text`: プロジェクト一覧(ホーム)画面の先頭に表示するMarkdown対応のウェルカムメッセージ。既存の`WikiMarkdownRenderer::render()`をそのまま再利用するにあたり、シグネチャの`Project $project`必須引数を`?Project $project = null`に変更(プロジェクト横断のホーム画面はどの単一プロジェクトにもスコープされないため)——`[[Page]]`リンク・`{{include}}`・`{{child_pages}}`マクロは、プロジェクト無しでは解決しようがないため、既存の`$page === null`時の「リテラルテキストのまま残す」というフォールバック方針をそのまま踏襲(`{{child_pages}}`は元々`$page === null`で即座に何もしない実装だったため無改修、`[[Page]]`用の`WikiLinkInlineParser`は`$project !== null`のときだけ登録、`{{include}}`は`$project === null`なら`extractIncludeMacros()`が即座に元テキストを返す)。`{!! !!}`での出力は既存のWikiページ本文と同じ`html_input => escape`設定に守られるため、管理者が入力したHTMLがそのまま出力される信頼境界はWikiページと同一(新規のリスクではない)。テストは`tests/Unit/Support/Markdown/WikiMarkdownRendererTest.php`(プロジェクト無しでの素のMarkdown・`[[Page]]`のリテラル化・`{{include}}`のリテラル化・`{{toc}}`は無関係なので正常動作、の4件)+`tests/Feature/Projects/ProjectsWelcomeTextTest.php`(表示確認+未設定時は非表示、の2件)。**`activity_days_default`**: `activity/index.blade.php`・`activity/global-index.blade.php`が独立に持っていたハードコードの7日をこの設定値に統一(既定値7、Redmine本家の既定10ではなく本アプリの従来のハードコード値をそのまま既定値として維持——ユーザー判断待ち表の他の設定項目と同じ「導入で既存挙動を変えない」方針)。**advisorレビューで発見・修正**: `app/Http/Controllers/ActivityFeedController.php`(プロジェクトの`activity.atom`)も独立に`private const DAYS = 10`をハードコードしていたのを同じ設定に統一したが、これによりAtomフィードの未設定時の既定ウィンドウが「本家の10日」から「本アプリのHTML画面と揃えた7日」に**意図的に縮小**される(HTML画面とAtomフィードで異なるハードコード値を持たせない一貫性を優先した判断)。この意図的な差分をテストで固定(`tests/Feature/Activity/ActivityAtomFeedTest.php`に「8日前のエントリ=10日窓なら含まれるが7日窓なら除外される」ケースを追加、設定変更で窓が広がることの確認テストと合わせて検証)。`per_page_options`(ページサイズを選ばせる`<select>`自体がどの一覧画面にも存在せず、UIから新規に作る必要がある)・`search_results_per_page`(検索結果に現状ページネーション自体が無く、ゼロから追加が必要)は、いずれも単なる設定値追加では済まない別機能のため今回は対象外、次の判断点として据え置き A2-07(2026-09-20): `per_page_options`(既定25,50,100)・`search_results_per_page`(既定10)を追加。`PageSize`+`SelectsPageSize`+`<x-per-page-select>`で課題・グローバル課題・グローバルお知らせ・プロジェクト一覧に「表示件数」選択(URL `per_page`、選択肢に無い値は無視)、検索(プロジェクト内/グローバル)は`pagedResults`でページ分割。既定の件数は`default_issues_per_page`(課題)/25/10のまま=Redmineの「先頭の選択肢」とは異なる。工数一覧・文書・プロジェクトお知らせは未ページ分割のためA2-07bへ。 A5-01(2026-09-20): `host_name`(ホスト[:ポート][/パス])・`protocol`(http/https)を追加。設定時、コンソール起動時と各キュージョブ実行前に`URL::forceRootUrl`/`forceScheme`で反映され、キューで作るメール等のリンクが公開アドレスになる(未設定は従来どおり`APP_URL`)。Webリクエスト内はブラウザが実際に来たアドレスのまま。 |
| 管理 → 情報(`admin/info`) | done(2026-09-20) | `GET /admin/info`(`admin.info`、管理者のみ、ヘッダーに「情報」リンク)。アプリ/Laravel/PHP/DB/OSのバージョン、環境(APP_ENV・デバッグ・キャッシュ/セッション/メール/ファイル保存先)、チェックリスト(`storage/*`・`bootstrap/cache`の書き込み可否、`git`/`svn`コマンド、ImageMagick(なければimagick拡張/GDで代替表示)、PHP拡張)、キュー(接続・待機中/失敗ジョブ数。`sync`のときは注記)を表示。スケジューラの稼働状況は判定材料がないため未対応。`SystemInfo`。 |
| 管理 → デフォルト設定のロード | done(2026-09-20) | `GET /admin/default_configuration`(管理者のみ、情報ページからリンク)。言語(日本語/English)を選んで、標準のロール(マネージャー/開発者/報告者+組み込み2種)・トラッカー・課題ステータス・優先度・作業分類・ワークフローを投入。トラッカー/ステータス/優先度/組み込み以外のロールのどれかが既にあると読み込めない(名前が混ざるのを避けるためRedmineと同じ趣旨)。`DefaultConfigurationSeeder`(旧`DatabaseSeeder`の初期データ部分を切り出し、ワークフローは内部キーで組むのでどちらの言語でも同じ)。カスタムフィールド/デモデータは含めない。A14-01(初期セットアップ)は別途`blocked`のまま。 |
| 工数管理(`timelog_*`) | done(2026-09-20) | `timelog_required_fields`(課題/コメント)・`timelog_accept_0_hours`・`timelog_max_hours_per_day`・`timelog_accept_future_dates`・`timelog_accept_closed_issues`を設定画面「工数管理」に追加。`TimeLogConstraints`を`TimeEntryService`の作成/更新で検査するため、Web フォーム・一括編集・REST API・CSVインポート・コミットキーワードの全経路に効く(Redmineの`TimeEntry#validate_time_entry`と同じく、時間の検査は時間が変わるとき、未来日の検査は日付が変わるときだけ)。既定はRedmine同様すべて許可=**従来は0時間を常に拒否していたが既定で許可に変わる**。課題フォームの作業時間記録は課題の保存前に検査し、拒否時は課題を変更しない。コミットキーワードの工数が拒否された場合はその工数だけ記録しない。 |
| Atomフィードの最大件数(`feeds_limit`) | done(2026-09-20) | Redmineの`Setting.feeds_limit`(既定15)。設定「全般」に追加(1〜500)、`ActivityFeedController::limit()`が活動・課題・お知らせ・フォーラムの4フィードすべてで共有(従来は各コントローラが`LIMIT`定数15をハードコード)。不正な保存値でも最低1件。**未対応**: Redmineが同設定を使うプロジェクト一覧/工数のAtomフィード(本アプリに該当フィードなし)。テスト: `tests/Feature/Activity/FeedsLimitTest.php` |
| Atomキー(フィードリーダー向け認証) | done(2026-09-20) | `users.atom_key`(40桁hex、初回利用時に生成)と、プロジェクトの活動・課題・お知らせ・フォーラムのAtomに`?key=`で認証する`atom.key`ミドルウェアを追加。キーはそのリクエストだけを認証しセッションを残さず、有効なユーザーのみ、本人の閲覧権限の範囲だけ読める。プロフィールにキー表示とリセット(旧キーは即無効)、各Atomリンクには自動でキーを付与。REST APIキーとは別物。Redmineの`rss_key`に相当。 |
| 課題の変更履歴Atom(`/issues/changes`) | done(2026-09-20) | `GET /projects/{p}/issues/changes.atom`と全プロジェクト版`GET /issues/changes.atom`(Redmineの`JournalsController#index`)。各journalのコメントと変更点(`進捗率: 0 → 50`)を新しい順に最大25件(Redmine同様の固定値)、閲覧できる課題のみ・非公開コメントは閲覧権限/本人のみ。`?key=`(Atomキー)で購読可、課題一覧にリンクを追加。保存済みクエリ指定(`query_id`)は未対応。 |
| 整形済みテキストのキャッシュ(`cache_formatted_text`) | done(2026-09-20) | Redmineの`Setting.cache_formatted_text`(既定オフ、`lib/redmine/wiki_formatting.rb:91`で2KB超のみ対象)。設定「全般」にチェックボックス、`WikiMarkdownRenderer::render()`が最外周の呼び出しだけを`Cache::remember`(キーは本文・プロジェクト・ページ・添付ファイルのシグネチャ)。**Redmineより保守的な差分**: 本アプリの描画は本文外のデータに依存するため、`{{include}}`/`{{child_pages}}`を含む本文は常に対象外、結果は1時間(TTL)で失効(Redmineに有効期限はない)。`#123`・`[[Page]]`の参照先が作成/削除された直後は最大1時間古い表示が残りうる(Redmineと同種の制約、設定画面の注記にも明記)。再帰呼び出し(`{{collapse}}`本体・取り込み)は`renderMarkdown()`で単独ではキャッシュされない。テスト: `tests/Feature/Wiki/FormattedTextCacheTest.php` |
| 表示(日付/時刻形式、テーマ、週始まり、サムネイル) | partial(2026-07-23) | `start_of_week`(週始まり: 日/月/土)を実装、カレンダー画面(プロジェクト内`calendar.index`/全プロジェクト`calendar.global-index`)のグリッド開始曜日とヘッダーの曜日ラベル行の両方に反映(`Carbon::startOfWeek()`/`endOfWeek()`に直接渡せるようRedmineの ISO値(1/6/7)ではなくCarbonの日定数(0/1/6)で保存)。日付/時刻形式・テーマ・サムネイルサイズは本アプリにi18n/テーマ切替基盤が無いため引き続き対象外 A5-04/A14-06(2026-09-20): 設定`new_item_menu_tab`(0=なし/1=「新しい課題」リンク/2=「+」ドロップダウン、既定2)を追加。プロジェクト内のどのページでもヘッダーに、閲覧者が作成権限を持つ項目(課題・カテゴリ・バージョン・工数・お知らせ・文書・Wikiページ・ファイル)だけを表示(`<x-new-item-menu>`)。Redmineはプロジェクトメニューのタブだが、本アプリにはプロジェクトメニューがないためヘッダーに置いた。 A4-11(2026-09-20): アバター(`<x-avatar>`、`UserAvatar`)を追加。設定`gravatar_enabled`(既定オフ)+`gravatar_default`(標準/mm/identicon/monsterid/wavatar/retro/robohash/イニシャル)。オフのときは名前から決めた色のイニシャル円(外部通信なし)、オンのときはメールハッシュのGravatar(閲覧者のブラウザが直接取得、referrer なし、`initials`指定時は404でイニシャルに戻る)。課題詳細(作成者・担当者・履歴)とメンバー一覧に表示。Redmine 6.0のアップロード式アバターは未対応(要件不明のため分割せず対象外) A8-07(2026-09-20): `timespan_format`(小数`1.50`/時:分`1:30`、既定は小数)を追加。`Hours::format()`(Redmineの`format_hours`)を課題(詳細・一覧の列/合計)・工数一覧(列/合計)・グローバル工数一覧・バージョン・プロジェクト概要の時間表示に適用。入力側(工数フォームの`1:30`入力)は未対応。
| 認証(ログイン必須、セルフ登録、パスワードポリシー、2FA必須設定、セッションタイムアウト、自動ログイン、REST API有効化) | partial(2026-07-23訂正) | **訂正**: 本行も他の設定タブ同様「missing」のまま放置されていたが、セルフ登録モード・メールドメイン許可/拒否リストは本セッション以前〜今回で実装済み(詳細は下部「ユーザー管理・認証」節の該当行を参照)。**セッションタイムアウトを追加(2026-07-23)**: Redmineの`Setting.session_timeout`(アイドルタイムアウト、`User.verify_session_token`相当)を実装。新規`App\Http\Middleware\EnforceSessionTimeout`が`routes/web.php`の`auth`ミドルウェアグループに追加登録され、リクエストごとにセッションの`last_activity_at`を更新、設定した分数を超えて操作が無かった場合はログアウト+セッション再生成の上ログイン画面へリダイレクト(Redmineの選択肢と同じ無効/1/2/4/8/12/24/48時間のプリセット)。~~Redmineが別途持つ`session_lifetime`(操作有無に関わらずログインからの絶対上限)は同種の仕組みを別の基準時刻に適用するだけのため対象外(意図的なスコープ限定)~~ → **`session_lifetime`を実装(2026-09-20)**: `EnforceSessionTimeout`がセッションの`session_started_at`(そのセッションで最初に認証済みリクエストを見た時刻、ログアウトでセッションごと消える)を基準に、最終操作からのタイムアウトとは独立に絶対上限を判定(どちらか一方の超過で失効)。開始時刻はリクエストで更新されないため操作で延長できない。選択肢はRedmineの`session_lifetime_options`と同じ(無効/4・8・12時間/1・7・30・60・365日)。設定導入前から続いているセッションは、導入後に最初に見た時点から数え始める。テスト: `SessionTimeoutTest.php`。**パスワード最小文字数を追加(2026-07-24)**: `password_min_length`設定(Redmineの同名設定相当、既定値8)。`AppServiceProvider::boot()`で`Password::defaults(fn () => Password::min((int) Setting::get('password_min_length', 8)))`を登録 — `Password::defaults()`のクロージャは呼び出しのたびに再評価される(Laravel側の仕様)ため、キャッシュ経由の`Setting::get()`と組み合わせても管理者の変更が再デプロイなしで即座に反映される。新規登録(Fortifyの`PasswordValidationRules`は元々`Password::default()`を使用済みのため無改修で反映)・管理者によるユーザー作成(`users/form.blade.php`の固定`min:8`を`Password::default()`に置換)・本人によるパスワード変更(`profile/index.blade.php`の固定`min:8`を同様に置換)の3経路すべてに一貫して適用。~~Redmine本家の文字種別必須設定(`password_required_char_classes`、大文字/小文字/数字/記号を個別に選択可能)は、Laravelの`Password`ルールが提供するのは`mixedCase()`のような粗い単位のトグルのみで4分類を独立選択できる形に素直に対応しないため対象外~~ → **実装(2026-09-20)**: `RequiredPasswordCharacterClasses`規則(Redmineの`Setting::PASSWORD_CHAR_CLASSES`と同じ4分類・パターン、記号は印字可能な非英数字ASCII)を`Password::defaults()`の`->rules([...])`に追加(Laravelの`Password`に追加ルールを渡せるため素直に対応できた — 従来の対象外理由は不正確)。設定「認証」に4つのチェックボックス、選んだ文字種はそれぞれ独立に必須。登録・管理者フォーム・パスワード変更・リセットのすべてに適用され、空値は必須/nullable規則に任せる。既存パスワードは次に変更するときから対象。テスト: `PasswordPolicyTest.php`。パスワード有効期限(`password_max_age`)は本アプリに`passwd_changed_on`相当のカラムもログイン時の強制変更フローも無く、単なる設定値追加では済まない別機能のため対象外。**`autologin`・`rest_api_enabled`・`twofa`(2FA必須設定)を追加(2026-07-30)**: いずれも詳細は下部「ユーザー判断待ち」表および「API認証」節を参照。**`login_required`も同日中に追加実装完了** — 詳細は本表の`login_required`単独行を参照 A11-15(2026-09-20): `jsonp_enabled`(既定オフ、設定画面のREST API欄)。オン時、APIへのGETに`callback`(または`jsonp`)を付けると成功レスポンスを`callback(...)`で包み`application/javascript`+nosniffで返す。callbackは英数字・`_`・`.`のみ(空になれば無視)。Redmineと異なり**GETのみ**対象(エラー・POST等は包まない)。
| プロジェクト(デフォルト公開設定、デフォルトモジュール、識別子連番化、新規プロジェクトの既定ロール) | done(2026-07-24) | デフォルト公開設定(`default_projects_public`)・デフォルトモジュール(`default_projects_modules`)・デフォルトトラッカー(`default_projects_tracker_ids`)を実装(詳細は§2 Projects「プロジェクト作成」行を参照)。識別子連番化: `sequential_project_identifiers`設定(Redmineの同名設定相当)。`Project::nextIdentifier()`が直近作成プロジェクト(id降順)の識別子を1つ進める — Redmine本体はRubyの汎用`String#succ`(末尾が数字なら数字側を繰り上げ、非数字ならアルファベット側を繰り上げてゼロ幅の桁上がりも行う)を使うが、本実装は「末尾の数字を+1(元の桁数を維持、桁上がり時のみ拡張)」のみ忠実に再現し、末尾が非数字の場合は単純に`1`を追記する簡略版(実用上ほぼ全てのケースをカバーし、稀なアルファベット桁上がりのみ非対応、という意図的な簡略化)。Redmine同様、新規プロジェクトフォームの初期表示では適用せず(`Project#set_default_values`と同じく保存時のみ、識別子欄が実際に空欄の場合のみ適用) — 本アプリの識別子欄は名前入力から自動スラグ化されるJS/Livewireの`updatedName()`が既にあるため、この設定が実際に効くのはユーザーが明示的に識別子欄を空にした場合のみ(Redmine自身も同じ制約を持つ、JSの自動生成を上書きしないための設計)。**新規プロジェクトの既定ロールを追加(2026-07-24)**: `new_project_user_role_id`設定(Redmineの同名設定相当)。`Project::defaultMemberRole()`(設定値 → 未設定/無効なら最初のgivableロールへフォールバック、Redmine本体の`Project.default_member_role`と同じ挙動)+`Project::addDefaultMember(User $user)`(Redmineの`Project#add_default_member`相当、`Member`を作成しロールを1つ付与)。プロジェクト作成フォーム(`projects/form.blade.php`の`save()`)の新規作成パスに、**管理者以外**の作成者にのみ適用(Redmine本体の`ProjectsController#create`と同じ`unless User.current.admin?`条件、管理者は元々全プロジェクトを閲覧できるためメンバー登録自体が無意味)。**本アプリ固有の前提**: `ProjectPolicy::create()`が管理者以外を一律拒否する既存の設計(このセッションより前からの確立済みスコープ決定)のため、この機能が実際に発火するのは非管理者によるサブプロジェクト作成パス(`createSubproject`権限経由)のみ — トップレベルのプロジェクト作成自体が非管理者には到達不可能 |
| ユーザー | partial(2026-07-29) | Redmineの本タブは7項目(`max_additional_emails`/`email_domains_allowed`/`email_domains_denied`/`unsubscribe`/`default_users_hide_mail`/`default_notification_option`/`default_users_no_self_notified`/`default_users_time_zone`/`default_users_auto_watch_on`)。うち`email_domains_allowed`/`email_domains_denied`は既に実装済み(§2「認証」行参照)、**`default_users_no_self_notified`を追加(2026-07-29)**: 新規ユーザーの`no_self_notified`(自分自身の変更を通知しない)初期値、既定は`true`(Redmine本家の`config/settings.yml`の`default: 1`と一致)。自己登録・管理者による作成・オンザフライLDAPプロビジョニングの3経路すべてで明示的にシード、プロフィール画面で本人が個別に上書き可能(詳細は§1「メール通知(課題)」参照)。**`default_notification_option`を追加(2026-07-29)**: 新規ユーザーの`mail_notification`初期値、既定は`only_assigned`(Redmine本家の`config/settings.yml`の`default: 'only_assigned'`と一致、これまで本アプリの`User`モデルの`$attributes`にハードコードされていた`only_my_events`から変更)。Redmineの`User#set_mail_notification`(`before_create`コールバック、値が空の場合のみ既定値を適用)と同じ設計を`User::booted()`の`creating()`イベントフックとして実装 — 個別の`User::create()`呼び出し箇所(自己登録・管理者作成・オンザフライLDAPプロビジョニング・テストファクトリ)に配線を分散させず、モデルレベルの単一箇所で全経路を確実にカバーする方式に変更(`no_self_notified`実装時にadvisorから指摘された「複数の書き込み経路のうち一部だけ配線してしまう」リスクを踏まえた設計判断)。**注意点2点**: (1) この変更は新規作成ユーザーにのみ適用され、既存ユーザーの`mail_notification`列の値(`only_my_events`)はそのまま — 新規/既存ユーザーで既定ティアが異なる状態になる。(2) `users`テーブルの`mail_notification`列自体のDBカラムデフォルト値(`only_my_events`、マイグレーション`2026_07_29_085847_add_mail_notification_to_users_table`)は、Eloquentの`creating()`フックが常に挿入前に値をセットするため到達不能なデッドコードになった(生SQL経由の直接INSERTなど本アプリが行わない経路でのみ意味を持つ) — 実害はないため据え置き。**`unsubscribe`(自己アカウント削除)を追加(2026-07-30)**: Redmine本家の`User#own_account_deletable?`(user.rb:830-834、`Setting.unsubscribe`が有効 かつ 「自分が管理者なら他にactiveな管理者が最低1人いる」の2条件)をそのまま`User::deletable()`として実装、ロック済みの管理者は「他のactive管理者」に数えない点まで一致させた(テストで直接境界を確認)。Redmine本家は削除確認前に`require_sudo_mode`(パスワード再確認)を要求するため(my_controller.rb:30-31)、本アプリでも既存の`RequiresPasswordConfirmation`トレイト(2FA無効化等と共用)を再利用。**設計上の大きな逸脱**: Redmine本家は`User#destroy`(`before_destroy :remove_references_before_destroy`)で、Issue/Journal/Query/TimeEntry/WikiContent/News等の作成者を単一の共有`User.anonymous`プレースホルダへ`update_all`で付け替えてから実際に行を物理削除する。本アプリはこの付け替え+物理削除ではなく、**該当ユーザーの行自体をその場で匿名化**(`App\Services\AccountDeletionService`: 氏名/メール/ログインIDをスクラブし`UserStatus::Deleted`に変更、行は残す)する方式を採用 — 理由は、本アプリの作者系FK(`issues.author_id`/`journals.user_id`/`queries.user_id`/`time_entries.user_id`/`wiki_pages.author_id`/`news.author_id`/`boards.author_id`)が現状すべて素の`RESTRICT`制約(`onDelete`指定なし)で、Redmine方式を再現するには7本のFKすべてに`nullOnDelete`等のスキーマ変更と、`->author->name`/`->user->name`を素で参照している16ファイル・29箇所のBladeビューへのnullセーフ対応が必要になり影響範囲が大きい一方、行をその場で匿名化する方式ならスキーマ変更もビュー側の変更も不要(既存の全画面が無改修で動作)。副作用として、複数の削除済みユーザーがRedmine本家のような単一の共有Anonymousではなく、それぞれ別個の(匿名化済みの)行のまま区別される — 履歴上「誰が書いたか」が個別に追える点はむしろ本家より情報量が多いままとも言える。プロジェクトメンバーシップ・グループ所属・プロジェクトブックマーク・非公開クエリ・ウォッチは削除時に実削除(Redmine本家も非公開クエリ・ウォッチは実削除する設計と一致)、公開/ロール限定クエリ・作成済みIssue/Journal/TimeEntry等の内容はそのまま保持(作成者は匿名化済みの同じ行を指し続ける)。削除済みユーザーは`UserStatus::Deleted`になり`isActive()`が`false`を返すため既存のログインゲートで自動的にログイン不可、`Project::assignableUsers()`等のメンバーシップ経由のクエリからも(メンバーシップ行自体を削除するため)自動的に除外される。管理者向けユーザー一覧(`users.index`)は削除済みユーザーを一覧から除外(Redmine本家は行自体が消えるため同じ見え方になる)。`Setting::unsubscribe`の既定値は`true`(Redmine本家の`config/settings.yml`の既定と一致、本アプリの他の設定タブでよくある「既定false」パターンからの意図的な非逸脱)。プロフィール画面の「アカウントを削除」セクションは`deletable()`がtrueの場合のみ表示されるが、Livewireのクライアント側状態は改ざん可能なため`deleteAccount()`アクション自体も`deletable()`とパスワード再確認の両方をサーバー側で再検証(テストで、削除不可な管理者が直接アクションを呼んでも拒否されること・パスワード再確認なしでは削除されないことの両方を確認)。`max_additional_emails`(追加メールアドレス機能自体が本アプリに無い)・`default_users_hide_mail`・`default_users_time_zone`(本アプリはタイムゾーン機能自体が無い)・`default_users_auto_watch_on`は引き続き未着手のまま。**advisorレビューで確認した安全性(実装変更なし)**: 作成者は保持する設計のため「削除済みユーザーが自分の作成したIssueのメール通知を(存在しないアドレスへ)受け取り続けないか」を`App\Support\Mail\NotificationRecipients::resolve()`で確認 — 候補者集合自体がプロジェクトメンバー∪ウォッチャーからのみ構築される設計(`OnlyOwner`ティアも「候補者のうち作成者と一致する者」を絞るだけで、作成者専用の別経路で候補者を追加することはない)のため、削除時にメンバーシップ/ウォッチを実削除する本実装では削除済みユーザーはそもそも候補者集合に入らず、加えて`->where('status', UserStatus::Active)`の明示フィルタも二重に効く。**settings画面のドライバイ修正**: `self_registration`セレクトの直下に残っていた「メール確認によるアカウント有効化は…未対応です」という注記文が、前日(2026-07-30)の`email`モード実装で事実と矛盾する状態のまま取り残されていたため、今回削除 A4-13(2026-09-20): ユーザー個人設定(`users.preferences` JSON、`UserPreferences`)をプロフィールの「個人設定」に追加。配線済み: `comments_sorting`(課題の履歴の並び順)、`hide_mail`(公開プロフィールのメール非表示。本人と管理者には表示。**既定はオフ=従来どおり表示**、Redmineは既定オン)、`textarea_font`(課題/コメント/Wikiの入力欄)、`warn_on_leaving_unsaved`(課題・Wikiフォームで未保存離脱警告)、`default_issue_query`(全体クエリのみ、URLに条件がなければ課題一覧をそのクエリで開く)、`auto_watch_on`(値のみ保存、消費はA4-14)。設定`default_users_hide_mail`/`default_users_auto_watch_on`で新規ユーザーの既定を指定。`notify_about_high_priority_issues`・`history_default_tab`・`recently_used_projects`・`default_project_query`は消費先が本アプリにない/別機能のためA4-13bへ。。**登録時のカスタムフィールド(2026-09-20、A4-07)**: 設定`show_custom_fields_on_registration`(既定オフ)がオンなら編集可能なユーザーのカスタムフィールドを、必須の項目は設定に関わらず常に登録フォームに表示(`CreateNewUser::registrationCustomFields()`、`<x-registration-custom-field>`)。形式検証は管理画面と同じ。フォームに出していない項目のIDを直接POSTしても保存されない。テスト: `RegistrationCustomFieldsTest.php` |
| 課題トラッキング(進捗率算出方式、プロジェクト間関連/サブタスク許可、既定表示列) | partial(2026-07-23訂正) | **訂正**: 本行は従来「missing」のまま放置されていたが、実際には進捗率算出方式(`issue_done_ratio`)・プロジェクト間関連許可(`cross_project_issue_relations`)・親子集計系(`parent_issue_priority`/`_dates`/`_done_ratio`)・重複クローズ連動(`close_duplicate_issues`)は本セッション以前から`settings/index.blade.php`に実装済みだった(他の行の更新時に本行の訂正が漏れていた)。**既定表示列を追加(2026-07-23)**: `issue_list_default_columns`設定(Redmineの`Setting.issue_list_default_columns`相当)を実装、`issues/index.blade.php`(プロジェクト内一覧)・`issues/global-index.blade.php`(全プロジェクト一覧、`project.present? ? default : [:project] \| default`と同じくRedmine同様`project_id`を自動先頭付与)双方の`mount()`でURL未指定時のみ適用。カスタムフィールドは列の既定値として選択不可(インストールごとに変わるため対象外、Redmine本体は選択可能) **関連課題・サブタスクの表示列(2026-09-20)**: Redmineの`related_issues_default_columns`(既定 status/assigned_to/start_date/due_date/done_ratio、tracker・subjectは常時表示のため選択肢から除外)と`display_related_issues_table_headers`(既定オフ)。設定「課題トラッキング」に列チェックボックスと見出し行のトグルを追加、課題詳細画面のサブタスクと関連課題を表に変更し題名+選択列を表示(`RelatedIssueColumns`)。表示列に必要な関連は先読みしてN+1を回避。**簡略化**: 列の並び順は固定(選択順の変更は不可)、カスタムフィールド列は対象外。テスト: `RelatedIssueColumnsTest.php` |
| メール通知(送信元、ヘッダ/フッタ、通知イベント種別) | partial(2026-07-29) | `notified_events`(通知するイベント種別、現状は課題のみ実配信)・`mail_from`・`plain_text_mail`・`emails_footer`を実装。「自分自身の変更を通知しない」は当初このタブのグローバル設定として実装したが、Redmine本家に相当するのはユーザーごとの`no_self_notified`設定のため、下部「ユーザー」タブ行の`default_users_no_self_notified`(新規ユーザーの既定値)+プロフィール画面の個別設定に置き換え済み(詳細は§1「メール通知(課題)」参照) **`emails_header`と`show_status_changes_in_mail_subject`を追加(2026-09-20)**: ヘッダ文は課題・Wiki・Newsの全通知メール(HTML/テキスト)の先頭に(署名`emails_footer`と対称、空なら出力しない)。件名のステータスはRedmineの`Mailer#issue_add`/`#issue_edit`に合わせ、新規課題は常に`(ステータス)`付き、更新はそのステータスを変更した更新のみ(従来は常に付与)、設定オフで両方とも付けない(既定オン)。テスト: `IssueMailNotificationTest.php`。**未対応**: ヘッダ/署名のMarkdown整形(プレーンテキスト表示)、`default_users_hide_mail`(ユーザー個人設定基盤A4-13が前提) |
| 受信メール | partial(2026-07-22) | 有効フラグ+既定プロジェクト/トラッカー/ステータスに加え、`mail_handler_preferred_body_part`(プレーンテキスト/HTML優先、HTML側は`strip_tags`でプレーン化)・`mail_handler_body_delimiters`(1行1パターンの完全一致で本文を切り捨て、返信の引用部分除去用)・`mail_handler_excluded_filenames`(カンマ区切りのワイルドカードパターンで添付を除外)を追加。Redmineの`unknown_user`/`no_permission_check`/APIキー経由受信は引き続き意図的に対象外(既存の514行目を参照) |
| 添付ファイル(最大サイズ、許可/禁止拡張子) | done(2026-07-21) | `attachment_max_size`/`attachment_extensions_allowed`/`attachment_extensions_denied`。`App\Support\Attachments\AttachmentValidationRules`を全6箇所のアップロードフォーム(Issue/News/Wiki/Message/Document/Files)で共通利用。最大サイズは`media-library.max_file_size`をハード上限として超過不可 |
| リポジトリ(有効SCM、自動フェッチ、コミットキーワード) | done(2026-07-22〜24) | 自動フェッチ(`autofetch_changesets`設定)を実装。`AutofetchRepositoryChangesetsJob`を15分ごとにスケジュール登録し、有効時は全リポジトリへ`RepositorySyncJob`をディスパッチ(`RepositorySyncJob`自体の`ShouldBeUnique`によりリポジトリ単位で重複実行は防止済み)。**有効SCM切り替え・コミットキーワード設定を追加(2026-07-24)**: `enabled_scm_types`設定(チェックボックス、既定は両方有効)でリポジトリ作成フォームの「種別」選択肢を制限(既存リポジトリ自身の種別は後から無効化されても選択肢に残り続け、編集をブロックしない)。~~`commit_fixing_keywords`設定(カンマ区切り文字列)~~ → **`commit_fixing_keyword_rules`(キーワードごとの対象ステータス割り当て)に置き換え(2026-07-24)**、詳細は「コミットメッセージのキーワード連動」の行を参照 A5-12/A10-02(2026-09-20): コミットルールに`done_ratio`(進捗率)と`if_tracker_id`(トラッカー限定)を追加(同じキーワードの最初に一致するルールが有効、状態だけ/進捗だけのルールも可)。設定`commit_ref_keywords`(既定`*`=キーワードなしの#idも関連付け。Redmineの既定は`refs,references,IssueID`だが従来動作を保つため`*`)と`commit_cross_project_ref`(既定オン=従来どおり。Redmineの既定はオフ)を追加し、オフ時は同一/親/子プロジェクトの課題のみ関連付け・更新・工数記録の対象。コミットログのエンコーディング/履歴表示件数はA5-12bへ。 A5-12b(2026-09-20): `repository_log_display_limit`(履歴の表示件数、既定100)・`repositories_encodings`(UTF-8以外のファイル内容/差分/blame/ツリーを候補順にUTF-8へ変換。バイナリは対象外)・`commit_logs_encoding`(コミットログの変換候補。**Redmineはリポジトリ単位の`log_encoding`だが、本アプリは全体設定のみ**)・`commit_logs_formatting`(既定オン=コミットログをMarkdown整形。従来はプレーンテキスト表示)を設定「リポジトリ」に追加。`CodesetConverter`をGit/Svnアダプタに配線 A10-01(2026-09-20): リポジトリ単位の`log_encoding`/`path_encoding`を追加(フォームで指定、空欄は全体設定、Gitアダプタのログ/ツリー変換に配線)。リモートURL+ユーザー/パスワード(`url`/`login`/`password`)は「ローカルパス限定」の安全境界を外すためA10-01bとして`blocked(要承認: docs/design/gap-A10-01b.md)` A10-05/A5-07(2026-09-20): `diff_max_lines_displayed`(既定1500、超過分を切り詰めて注記。変更/比較画面)・`file_max_size_displayed`(既定512KB、超過はインライン表示せずダウンロードのみ)・`thumbnails_size`(既定100px、以降のアップロードに適用)を追加(0で無制限)。`bulk_download_max_size`は一括ZIP(A7-10)と同時に A10-03/A13-06(2026-09-20): リポジトリ管理Webサービス`GET /sys/projects`(有効プロジェクト+既定リポジトリ`{id,url}`)・`GET|POST /sys/fetch_changesets?id=`(同期ジョブをキュー投入、id省略で全件、不明は404)を追加。設定`sys_api_enabled`(既定オフ)+`sys_api_key`(「キーを生成」ボタン付き)で保護し、キー比較は`hash_equals`、CSRF除外。権限`commit_access`を登録(Redmineと同様にアプリ内では強制せず、外部のreposman/Apache連携が参照する用)。`POST /sys/projects/{id}/repository`(リポジトリ作成)はローカルパス限定の安全境界のため対象外。

### ユーザー管理・認証

| 機能 | 状態 | 備考 |
|---|---|---|
| 管理: ユーザー/グループのプロジェクトメンバーシップ(`principal_memberships`) | done(2026-09-20) | ユーザー編集・グループ編集画面に「プロジェクト」パネル(`ManagesPrincipalMemberships`+`<x-principal-memberships>`)を追加。所属プロジェクトとロールの一覧、追加(有効なプロジェクトのうち未所属のもの)・ロール変更・削除。ロールは`givable`のみで1つ以上必須、他人の行は操作不可(404)、管理者のみ+プロジェクトの`manageMembers`も確認。グループは`group_id`のメンバーとして登録(ユーザーへのロール継承は既存のグループ解決に従う)。 |
| ユーザー一覧の右クリックメニュー | done(2026-09-20) | 行のチェックボックスと右クリック(`openContextMenu`)で選択し、一括ロック/解除・削除(`AccountDeletionService`による匿名化)・グループに追加/選択全員が所属するグループから外す。自分自身のロック/削除と、他に有効な管理者がいなくなる管理者の削除は黙って除外する(Redmineは自分のみ除外)。**Redmineとの差分**: 管理者による他ユーザーの削除はこのアプリに従来無く、この行で初めて可能になった。テスト: `UserContextMenuTest.php` |
| 管理者向けユーザー一覧クエリ(`UserQuery`) | done(2026-09-20、保存クエリは対象外) | ユーザー一覧にフィルタ(ステータス/認証方式/所属グループ/名前/ログインID/メール/管理者/登録日/最終ログイン)、表示列の選択、列見出しでの並べ替え、CSVエクスポート(選択列・フィルタ結果、UTF-8 BOM付き)。`UserFilterFieldRegistry`+`UserGroupFilter`が`QueryFilterEngine`を再利用。一覧は表形式になり、右クリックメニューと選択は従来どおり。**Redmineとの差分**: 既定はフィルタなし(Redmineは有効ユーザーのみ)、保存クエリ・ユーザーのカスタムフィールド・2要素認証方式の列/フィルタ・ページングは未対応。共通の改善: フィルタの「〜を含む」が大文字小文字を区別しなくなり(PostgreSQL、`%`/`_`は文字として扱う)、値が空のままの条件は無視される(従来は`>=`等で例外)。テスト: `UserListQueryTest.php` |
| プロジェクト権限の細分化(`select_project_publicity`/`manage_project_activities`/`view_members`) | done(2026-09-20、A3-02/A3-11/A13-05) | 公開/非公開の選択(プロジェクトフォームのチェックボックスとREST。権限が無ければ値は無視され、新規は既定`default_projects_public`、新規作成では作成者に与えられる既定ロールの権限で判定)、作業分類設定ページ(`ProjectPolicy::manageActivities`)、メンバーの一覧/参照(`MemberPolicy::viewAny/view`=REST。作成・更新・削除は`manage_members`のまま)を独立した権限にした。既存ロールには移行で旧権限(`edit_project`/`manage_members`)の保持者へ付与(`grant_project_permission_stand_ins`)。**未対応**: `save_queries`・`search_project`(A13-05b)、Redmineのプロジェクト概要のメンバー欄(本アプリには無い)。テスト: `ProjectPermissionSplitTest.php` |
| ユーザーのCSVインポート(`UserImport`) | done(2026-09-20、A4-01) | `/users/import`(管理者のみ)。列の対応付け(ログインID/名前/メールアドレス必須、パスワード、言語、管理者、認証方式、ステータス、ユーザーのカスタムフィールド)、`ImportUsersJob`が行ごとにユーザーフォームと同じ検証(ログインの形式と大文字小文字を区別しない重複、メールの重複とドメイン制限、パスワードの強度)で作成し、失敗行はエラー一覧に残して残りを続行する。パスワード空欄は認証方式(LDAP)を指定した行だけ可。引用符内の改行とBOMに対応。状況は`/users/imports/{id}`。**Redmineとの差分**: 名前は1列(firstname/lastnameの別列は無い)、`must_change_passwd`(A4-03)とインポート後の「アカウント情報を通知」は未対応、文字コードはUTF-8のみ。テスト: `UserImportTest.php` |
| 追加メールアドレス(`email_addresses`、`max_additional_emails`) | done(2026-09-20、A4-02) | `email_addresses`テーブル(追加分のみ。主アドレスは`users.email`のまま)。プロフィールで追加/削除/通知のオン・オフ、上限は設定「ユーザー」の`max_additional_emails`(既定5、0で追加不可)。追加アドレスにもメール形式・ドメイン制限・大文字小文字を区別しない重複(他人の主/追加アドレスとの衝突、自分の主アドレスとの重複)を検証。通知メール(課題・Wiki・お知らせ)は主アドレス+通知オンの追加アドレスすべてに届く(`User::routeNotificationForMail()`)。受信メールの送信者は追加アドレスでも照合される。アカウント削除時に追加アドレスも削除。**Redmineとの差分**: 追加アドレスを主アドレスに昇格する操作と、アドレス確認メールは無い。テスト: `AdditionalEmailTest.php` |
| パスワード有効期限・次回ログイン時の変更要求(`password_max_age`/`must_change_passwd`) | done(2026-09-20、A4-03) | `users.passwd_changed_on`/`must_change_passwd`列。設定「認証」のパスワードの有効期限(無効/7/30/60/90/180/365日)、管理者のユーザー編集の「次回ログイン時にパスワードの変更を要求する」。該当するローカルアカウントは、`EnforcePasswordChange`ミドルウェアにより、プロフィール以外のページを開くとプロフィールへ戻される(パスワード変更で解除。プロフィール/Fortifyの変更・再設定のいずれでも)。パスワードが変わるたびに期限の起点を更新(`User`の`saving`フック)。LDAP等の外部認証は対象外。**既存アカウントの起点は移行時点**(有効期限をオンにしても一斉に期限切れにならない。Redmineは起点なしを期限切れ扱い)。ユーザーCSVインポートにも`must_change_passwd`列。テスト: `PasswordExpiryTest.php` |
| 工数・優先度のカスタムフィールド(`TimeEntryCustomField`/`IssuePriorityCustomField`) | done(2026-09-20、A8-02。API・一括編集・横断一覧・レポート軸は A8-02b/A8-04) | `CustomizableType`に`TimeEntry`/`IssuePriority`を追加(モーフマップも)。`TimeEntry`が`HasCustomFields`を使い、管理画面で対象に選べる。工数フォームに入力欄(必須/形式検証、閲覧ロールで非表示、編集不可の項目は変更不可)、プロジェクトの工数一覧に`cf_{id}`列(表示列選択・CSV)。優先度の管理フォームにも入力欄。**未対応**: REST API(課題のAPIも現状CFを出していない)、工数の一括編集、全体の工数一覧の列、レポートの軸(A8-04)。テスト: `TimeEntryCustomFieldTest.php` |
| 管理者によるユーザー一覧/作成/編集 | done | `users/{index,form}.blade.php`, `UserPolicy`(admin-only) |
| アカウントロック/アンロック | done | 一覧画面からワンクリックで切替。`AuthenticateUser` がログイン時に `UserStatus::Locked` を実際にブロックする(この画面の実装と同時に修正した既存バグ) |
| 管理者による強制パスワードリセット | done(2026-07-22) | ユーザー編集フォームに「パスワードリセットメールを送信」ボタンを追加(Laravel/Fortify標準の`password.reset`通知フローを`Password::sendResetLink()`経由で起動)。編集フォームでパスワード欄に直接新しい値を入力する既存の方法も引き続き利用可能。LDAP連携ユーザーへの送信は拒否(ローカルパスワードを持たないため) |
| 自分自身のロック防止 | done | 一覧画面の切替アクションは自分自身に対して403を返す(唯一の管理者が自分をロックして誰も解除できなくなる事態を防止)。編集フォーム自体は自分の`is_admin`/`status`変更を意図的に許可(Redmineと同様、誤操作ではなく意図的操作として扱う) |
| LDAP認証ソース CRUD | done | ホスト/ポート/TLS/base_dn/direct-bind or search+bind/属性/onthefly登録/タイムアウト |
| LDAP接続テストボタン | done(2026-07-22) | Redmineの`AuthSourceLdap#test_connection`相当を`LdapAuthenticator::testConnection()`に実装(接続を開き、検索用アカウントが設定されていればバインドも検証)。認証ソース編集フォーム(既存レコードのみ、Redmine同様保存済みの設定でテスト)に「接続をテスト」ボタン+成功/失敗メッセージを追加。新規作成フォームでは非表示(保存前にテストする対象が無いため) |
| ~~LDAPカスタムフィルタ/属性→カスタムフィールドマッピング~~ → **done**(2026-07-30、行タイトルの後半は訂正) | login/name/mailの属性マッピング自体は既存(`attr_login`/`attr_name`/`attr_mail`)。今回追加したのは検索フィルタ | **行タイトルの訂正**: 「属性→カスタムフィールドマッピング」という機能はRedmine本家(`/Users/sesoko/Desktop/workspace/redmine`)を`app/models/auth_source*.rb`・`app/views/auth_sources/_form_auth_source_ldap.html.erb`まで確認したが存在しない — 本家が持つのは`attr_login`/`attr_firstname`/`attr_lastname`/`attr_mail`という固定4属性の名前マッピングのみで、任意のLDAP属性をユーザーのカスタムフィールドに流し込む機能自体がRedmine本体にはない。行タイトル自体が当初から誤りだったと判断し是正した。Redmine本家の`AuthSourceLdap#attr_login`/`attr_firstname`/`attr_lastname`/`attr_mail`(属性名マッピング)は、本アプリの`User`モデルがfirstname/lastnameを分割保持しない設計のため元々`attr_login`/`attr_name`(cn一括)/`attr_mail`の3属性で対応済みだった(チェックリストの「login/name/mailのみ」という記載は実装の抜けではなく仕様差異の説明だった、と改めて確認)。**今回追加(2026-07-30)**: `AuthSource.filter`(新規`filter`カラム、nullable)= Redmine本家の`AuthSourceLdap#filter`(`base_filter`経由で全検索にAND条件として付加、例: 特定グループのメンバーのみログイン許可)相当。`LdapAuthenticator::baseQuery()`で`LdapRecord\Query\Builder::rawFilter()`により検索時に常時AND付加(search-then-bind・direct bindの両モードの検索クエリに適用)。フォーム側で開き括弧/閉じ括弧の対応と個数一致のみをチェックする簡易構文検証を追加(Redmine本家は`Net::LDAP::Filter.construct`によるフルパース、本アプリはRFC4515パーサーを新規導入するほどの規模ではないため簡易版に留めた)。**テスト設計上の注意点**: `LdapRecord\Laravel\Testing\DirectoryEmulator`(既存LDAPテストが使うフェイクディレクトリ)は`rawFilter()`が生成する`Raw`フィルタ型を認識せず内部のEloquent変換ロジックで黙って無視する(`LdapRecord\Laravel\Testing\EmulatesQueries::applyFilterToEloquentQuery()`を確認、`ConditionFilter`/`GroupFilter`/`Not`のみ対応)ため、フェイク経由のE2Eテストではフィルタが実際に効いているかを検証できない — 実際に試して「フィルタを設定したのに絞り込まれない」誤検知に気づき、代わりに`LdapAuthenticator::baseQuery()`をリフレクション経由で呼び出し、`LdapRecord\Query\Builder::getUnescapedQuery()`でコンパイル済みのLDAPフィルタ文字列(`(&(memberOf=...)(uid=jdoe))`)を直接アサートする形に変更(まさに「最終結果でなく中間値を直接検証する」という標準方針の実例) |
| LDAPオンザフライ登録 | done | — |
| 二要素認証(TOTP) | done(2026-07-23) | Fortify経由。**管理者による強制無効化ボタンを追加(2026-07-23)**: `users.form`(ユーザー編集画面)に対象ユーザーが2FA有効時のみ表示される「二要素認証を無効にする」ボタンを追加、既存の`sendPasswordReset()`と同じ`update`権限で保護(セルフサービス版の`disableTwoFactor()`が使うパスワード再確認は他人のアカウント操作には適用されないため不使用)、Fortifyの`DisableTwoFactorAuthentication`アクションをそのまま対象ユーザーに適用 |
| パスキー/WebAuthn | done(Redmineにはない機能) | — |
| パスワードリセット | done | — |
| 登録モード(無効/手動承認/メール確認/自動) | **done**(2026-07-22、メール確認モードは2026-07-30に追加) | Redmineの`Setting.self_registration`相当を`self_registration`設定として実装。「無効」は`Fortify::registerView`が登録ページ自体をログインへリダイレクト(直接POSTされた場合も`CreateNewUser`側でバリデーションエラーとして拒否、二重の防御)、「手動承認」は`UserStatus::Registered`(元々定義されていたが未使用だった値)でロック状態のアカウントを作成し、ユーザー管理画面に「承認待ち」バッジ+「承認」ボタンを追加、「自動」は従来通り即座に有効化。**メール確認モード(`email`)を追加(2026-07-30)**: 当初「送信メール基盤が本アプリに無いため対象外」としていたが、その後Issue/Wiki/Newsのメール通知基盤が整備済みになったため再検討し実装。Redmine本家の`AccountController#register_by_email_activation`(`Token`モデル、`register`アクション、既定有効期限1日)を参照実装として移植 — 本アプリはTokenテーブルを新設せず、Laravelの署名付きURL(`URL::temporarySignedRoute('account.activate', now()->addDay(), ['user' => $user->id])`)をRedmineのToken相当として利用(改ざん検知・有効期限ともに標準機能でカバーされ、専用テーブル不要)。新規`App\Notifications\ConfirmAccountRegistration`(`MailMessage`ベース、Issue/Wiki/News通知と異なりリンク1つの単純な内容のため専用Mailable+Bladeビューを作らず`Illuminate\Notifications\Messages\MailMessage`を直接使用)がリンク付きメールを送信、新規`App\Http\Controllers\AccountActivationController`(`signed`ミドルウェアで署名検証)が`GET /account/activate/{user}`でトークン相当を検証しアカウントを有効化。**実装中に発見・修正した既存バグ(「手動承認」モードにも影響)**: FortifyのRegisteredUserControllerは`CreatesNewUsers::create()`の戻り値の`status`を一切見ずに`$guard->login($user)`を無条件実行するため、「手動承認」モードで作成された`UserStatus::Registered`(ロック状態)のアカウントも、実際にはブラウザセッションが認証済みのまま張られてしまっていた(単発リクエストに限らず、`$guard->login()`が発行するセッションCookieはそのセッションが期限切れになるかログアウトされるまで有効なため、影響範囲は「登録直後の1リクエスト」ではなく「そのブラウザで開いたままのセッション全体」だった)。`AuthenticateUser`の`isActive()`チェックは別途行われる「次回以降の」ログイン試行にしか効かず、登録時の自動ログインをそもそも経由しないため素通りしていた。新規`App\Http\Responses\RegisterResponse`(`Laravel\Fortify\Contracts\RegisterResponse`を`FortifyServiceProvider::register()`で上書きバインド)で、作成直後のユーザーが`isActive()`でなければ即座に`Auth::logout()`+セッション無効化を行うよう修正 — メール確認モードのみならず既存の手動承認モードの穴も同時に塞いだ。テストは最終的なstatus値だけでなく実際のセッション認証状態(`auth()->check()`)を直接アサートすることでこのバグを検出(登録直後の`assertRedirect()`先を確認するだけでは見逃していた)。署名付きURLの改ざん/期限切れ拒否、有効化後にログイン可能になること、既にactiveなユーザーへの再訪問は404、**有効化後に管理者がロックしたアカウントに対して同じ署名付きリンクを再訪問してもロック状態を上書きできない(リプレイ耐性)**、の各境界ケースもテスト済み。なお`RegisterResponse`のJSON応答分岐(`wantsJson()`)は非activeユーザーでも201を返す(HTMLパスのみ案内メッセージを付与)— RedmineのAPI登録は元々別経路のため直接対応するものはなく、現状は未検証のまま許容している |
| 自己登録メールドメイン許可/拒否リスト | done(2026-07-23) | Redmineの`EmailAddress.valid_domain?`/`Setting.email_domains_allowed`/`email_domains_denied`相当。設定画面に`email_domains_allowed`/`email_domains_denied`(カンマ区切り、先頭`.`でサブドメイン一致)を追加、`CreateNewUser`アクションに拒否リスト優先→許可リストがあればホワイトリストとして機能、の判定ロジックを実装(`attachment_extensions_allowed`/`_denied`と同じカンマ区切りリストのパターンを踏襲)。~~**注**: Redmine本体はこの検証を`EmailAddress`モデル経由で全ユーザーのメール変更(管理者による作成/編集含む)に適用するが、本アプリでは自己登録時のみに意図的に縮小~~ → **全経路に適用(2026-09-20)**: `EmailDomainPolicy`/`AllowedEmailDomain`規則を、自己登録・管理者のユーザー作成/編集・プロフィール・Fortifyのプロフィール更新・REST `PUT /my/account`のすべてに適用。**アドレスが変わらない編集は対象外**(後から制限を足しても無関係な編集が止まらない)。なお`.`始まりの項目はサブドメインのみに一致しベアドメインには一致しない(Redmineの`domain_in?`どおり、従来のdocコメントの記述は不正確だった)。テスト: `CaseInsensitiveUniquenessTest.php` |
| パスワード再設定(lost password)と`lost_password`設定 | done(2026-09-20) | **既存の不具合を発見・修正**: FortifyのパスワードリセットルートはFeatureとして有効だったが`requestPasswordResetLinkView`/`resetPasswordView`が未設定で、`/forgot-password`と`/reset-password/{token}`が500を返していた(管理者の「リセットメール送信」のリンクも同様に壊れていた)。`auth/forgot-password.blade.php`・`auth/reset-password.blade.php`を追加しFortifyに登録、ログイン画面に「パスワードをお忘れの場合」リンクを追加。**設定`lost_password`**(既定オン、Redmineの`Setting.lost_password`): オフで本人による再設定リクエスト(`password.request`/`password.email`)をログインへリダイレクト(`EnforceLostPasswordSetting`、Fortifyグローバルミドルウェア)、リンクも非表示。管理者発行の既存トークンによる`password.reset`/`password.update`は意図的に継続。**ガード**: ロック中・外部認証(LDAP)ユーザーにはリセットメールを送らず(`User::sendPasswordResetNotification()`)、発行後にロックされたユーザーのトークンでの再設定も拒否(`ResetUserPassword`)。テスト: `tests/Feature/Users/LostPasswordTest.php` |
| ログイン欄の必須化(@mentionミニフェーズ前提1) | **done**(2026-07-30) | `login`列自体は2026-07-21のLDAP対応追加時点で既に存在していた(nullable/unique)が、LDAP連携アカウント以外には常にnullで、全ユーザー必須にする仕組みが無かった。**今回の変更**: (1) 新規マイグレーション(`2026_07_30_040000_make_login_mandatory_on_users_table.php`)が既存の空login行をメールのローカル部から生成(サニタイズ後、Redmineのフォーマット規則外の文字は`-`に置換、衝突時は`-2`/`-3`…で連番、ローカル部が空/サニタイズ後に空になる場合は`user{id}`にフォールバック)して埋めた上で列を`NOT NULL`化。`RefreshDatabase`は空テーブルにしかマイグレーションを実行しないため、この後方互換バックフィルパスは`up()`/`down()`を手動挿入行に対して直接呼ぶテスト(`LoginBackfillMigrationTest.php`、衝突ケース・空ローカル部ケース含む4件)で検証。(2) `User::LOGIN_LENGTH_LIMIT`(60)と`User::LOGIN_FORMAT_REGEX`(`/^[a-zA-Z0-9_\-@.]+$/`、Redmine本家`user.rb`の`/\A[a-z0-9_\-@.]*\z/i`相当)をモデル定数として新設し、login書き込み経路3箇所すべて(自己登録`CreateNewUser`・管理者フォーム`users/form.blade.php`・LDAPオンザフライ登録`AuthenticateUser::provisionFromDirectory()`)がこの単一の真実源を参照する設計にした——LDAP経路はフォームバリデーションを経由しないディレクトリ由来の生入力(実際にはログインフォームにユーザーが直接入力したテキストがそのままLDAP bind DNにもlogin列にも使われる)であり、当初は未検証のまま`User::create()`に渡していたが、advisorレビューで「フォーム側の2箇所にしか制約が無い」と指摘され、フォーマット/文字数を満たさない場合は`provisionFromDirectory()`が(ディレクトリが認証を受理するかを試す前に)`null`を返しプロビジョニング自体を拒否するよう修正(Redmine本家がUserモデルバリデーション失敗時に暗黙に登録を失敗させる挙動に相当)。ガードを一時的に外して2件の新規テストが実際に失敗することを確認した上で復元。管理者フォームは`auth_source_id`の有無に関わらず常にlogin欄を表示・必須化(従来はLDAP連携時のみ表示)。~~大文字小文字を区別しない一意性判定(Redmine本家は行う)は、既存の`email`列の一意性判定が同じく大文字小文字を区別する実装のままであることに合わせ、意図的に対象外とした~~ → **`login`と`email`の一意性を大文字小文字無視に(2026-09-20)**: `UniqueUserValueIgnoringCase`規則(`lower(col) = ?`)を登録・管理者フォーム・プロフィール・Fortify更新・APIの全経路に適用。**検証層のみでDBに`lower()`のユニークインデックスは追加していない**(既存データに`Admin`と`admin`が並存するとマイグレーションが失敗するため)。テスト: `CaseInsensitiveUniquenessTest.php`。**ドライバイ修正**: `AccountDeletionService`が匿名化時に`login`を`null`に戻していたが、NOT NULL化により不可能になったため、`email`と同じプレースホルダ文字列(`deleted-user-{id}-{random8}`)を再利用する形に変更(既存の`AccountDeletionTest.php`のうち、nullを期待していた3件のテストをプレースホルダの一意性/元の値との不一致を検証する形に更新)。UserFactoryにも`login`を追加(`fake()->unique()->userName()`)、既存の登録/管理者フォーム関連テスト十数件がloginをsetしていなかったため追加修正。REST API(`Api/V1/MyAccountController`/`UpdateMyAccountRequest`)は`login`をそもそも検証ルールに含んでいない(name/emailのみ、`rules()`実体で確認済み)ため対象外、`profile/index.blade.php`(セルフサービスのプロフィール編集)も`login`への言及なし(grepで確認済み)のため同様に対象外。**2回目のadvisorレビューで追加指摘・修正した2点**: (1) `2026_07_21`のマイグレーションで追加した`login`列のunique制約が、今回の`->change()`(`NOT NULL`化)後も生き残っているかがテストされていなかった(アプリ層の`Rule::unique('users')`はDB制約の有無に関係なく通るため無関係)——`LoginBackfillMigrationTest.php`に重複login挿入が`QueryException`になることを直接確認するテストを追加。(2) `LOGIN_FORMAT_REGEX`がコメントでは本家の`\z`(文字列末尾、改行不可)相当と説明していたが、実際の正規表現はPHPの`$`(末尾に改行1つまで許容)のままだったため、`"admin\n"`のような値がすり抜けてDB上は`"admin"`とは別の一意な行として保存されうる状態だった(グローバルミドルウェア`TrimStrings`がHTTP経由の値は事前に整形するため実害は無いが、コメントと実装が食い違っていた)。`D`修飾子を追加して整合させ、正規表現自体を直接検証する`tests/Unit/Models/UserLoginFormatRegexTest.php`(HTTP経由だと`TrimStrings`に阻まれて再現できないため、あえてリクエストを経由しない直接テストとした)を新設。両修正とも、該当箇所を一時的に外して新設テストが実際に失敗することを確認した上で復元済み |
| 公開プロフィール画面(@mentionミニフェーズ前提2) | **done**(2026-07-30) | Redmine本家の`UsersController#show`(users_controller.rb)を移植。新規ルート`GET /users/{user}`(`users.show`、既存の`/users/create`・`/users/{user}/edit`より後に登録——前者は文字列リテラル、後者は`{user}`より1段深いセグメントのため登録順に関わらず衝突しないが、慣例に合わせ既存の3行の後ろに追加)。**認可の設計**: 既存の`UserPolicy::view()`(管理者専用、`Gate::before`バイパス以外は常にfalse)を流用せず、新規`User::isVisibleTo(?User $viewer): bool`をRedmine本家`Principal#visible?`(`Principal.visible(user).find_by(id:) == self`)の直接移植として追加——理由は、`UserPolicy`のクラスdocが明言する通りその`view`は「アカウント管理」向けの権限であり、「他人のプロフィールを覗ける範囲」という別の概念(ユーザー閲覧範囲機能で実装済みの`users_visibility`ロール設定と地続き)を混同すべきでないため。`isVisibleTo()`は`User::scopeVisibleTo()`(ユーザー閲覧範囲機能で実装済み、2026-07-30)をそのまま再利用しつつ、管理者は`status`を問わず常にtrue、非管理者は対象ユーザーが`Active`でない場合は即false、という判定をラップ——`scopeVisibleTo()`自体は意図的にstatusでフィルタしない設計(呼び出し側の責務、`members.blade.php`の`userCandidates()`が`->where('status', Active)`を個別に追加しているのと同じパターン)のため、これに倣った。認可が通らない場合はGate::authorize()の403ではなく`abort_if(..., 404)`——Redmine本家の`render_404`と一致させるための意図的選択(存在の有無自体を非公開ユーザーに漏らさない)。ガードを一時的に外し、「非公開ユーザーへの直接アクセス」「ロックユーザーへの非管理者アクセス」の2テストが実際に200を返す(404にならない)ことを確認した上で復元。**表示内容**: ログインID・メール・登録日・最終ログイン日時、担当/報告課題数(未クローズ/合計)、プロジェクトメンバーシップ一覧(ロール付き)、直近10件のアクティビティ。グループ所属は本人または管理者のみ表示(Redmine本家の`(User.current == @user || User.current.admin?) && @user.groups.any?`と同一条件)。**意図的なスコープ縮小**: (1) Redmine本家がユーザー設定の`hide_mail`で個別にメール非表示を選べる機能は、本アプリに対応するpreference機構が無いため未移植(常にメール表示)。(2) カスタムフィールド値の表示(`@user.visible_custom_field_values`)は未移植。(3) 課題件数・プロジェクト一覧はリンク付き絞り込み(Redmine本家は`issues_path(assigned_to_id: ...)`等)ではなく単純な数値/一覧表示——本アプリの課題一覧フィルタが`QueryFilterEngine`の内部filters配列形式に依存しており、単純なURLクエリパラメータでの事前絞り込みリンクを作るには別途URL⇔filters変換ロジックが必要になるため据え置き。**副作用として実施した基盤拡張**: `App\Support\Activity\ActivityEntry`に`authorId`(nullable int、末尾のデフォルト付き名前付き引数のため既存の全呼び出し箇所は無改修でコンパイル可)を追加し、Issue/IssueJournal/Message/News/TimeEntry/Wikiの6プロバイダが実際のUser FK(`author_id`または`user_id`、各モデルのリレーション名から一意に確定するLaravel既定の命名規則を`Schema::getColumnListing()`で実カラム名まで確認済み)を設定するよう変更——「最近の活動」セクションが対象ユーザーの投稿だけに絞り込むために必要だった。Document(著者列自体が存在しない)とChangeset(committerは生のSCM文字列で必ずしもUserにマッピングされない)の2プロバイダは`authorId`を設定できず`null`のまま——この2種のイベントはプロフィールの「最近の活動」に決して現れない、という制約として明記(見落としではなく設計上の既知の欠落)。新規テスト`tests/Feature/Activity/ActivityGlobalIndexTest.php`に、`authorName`だけでなく`authorId`という生の中間値を直接アサートする回帰テストを1件追加(活動フィード自体の見た目は変わらないため、この値が壊れても他のテストでは検出できない)。**課題件数の可視性境界で発見した設計上の考慮点**: 当初`issueCounts()`をactivityと同じ`visibleProjects()`(`ProjectPolicy::view`ベース、公開プロジェクトなら無条件true)でスコープしていたが、これは`issues.global-index`が実際に使っている`can('viewAny', [Issue::class, $project])`(`view_issues`権限まで確認)より緩く、「公開プロジェクトだが課題モジュール無効/`view_issues`未付与」なケースで本来見えないはずの課題件数が漏れる経路になり得た。新規`issueVisibleProjects()`(`visibleProjects()`をさらに`view_issues`権限で絞り込み)を追加してissueCounts()専用に使用、activityの方は各`ActivityProvider::entries()`内部が個別に`view_*`権限を確認する既存設計(activity.global-index と同じ役割分担)のため据え置き。**advisorレビューで追加指摘・修正した2点**: (1) ページ自体は実装したが、どこからもリンクされておらず直接URLを打たない限り到達不能だった(UIフィールドを機能より先に追加してはいけない、の逆パターン——機能をリンクより先に追加してしまっていた)。管理者専用の`users/index.blade.php`(ユーザー名)と`projects/members.blade.php`(メンバー一覧の氏名、ただし`isVisibleTo()`がfalseになる相手——非管理者閲覧者から見たロック中のメンバー等——はリンクせずプレーンテキストのまま)の2箇所にリンクを追加。後者はガードを一時的に外し、ロック中メンバーへのリンクが実際にレンダリングされてしまうことを確認した上で復元。(2) `recentActivity()`が`Carbon::createFromTimestamp(0)`から`now()`までの全期間を対象にしていたが、`ActivityProvider::entries()`は各プロバイダとも著者フィルタもLIMIT句もSQL側に持たない(生の`->get()`で全件具現化してからPHP側で絞り込む)設計のため、可視な全プロジェクトの全期間の課題/Journal/News等を毎回スキャンしてから10件に絞る、という高コストなクエリになっていた。`activity.global-index`が既定7日窓を採用しているのと同じ理由でコストを抑えるため30日窓(`now()->subDays(30)`)に変更——Redmineの`Fetcher`(著者フィルタ・LIMITをSQLに押し込む設計)との「直近10件」の完全一致は失われる(31日以上activityが無いユーザーは空表示になる)が、この妥協は`activity.global-index`が最初から受け入れているものと同種として許容。境界のテスト(30日超のエントリが除外されることを確認)を追加。テストはE2Eで15件(自分自身/管理者/既定all可視性ロール/`members_of_visible_projects`ロールでの許可・拒否/ロックユーザー拒否/課題件数の権限スコープ/プロジェクト一覧の可視性/グループ表示の可視性2件/アクティビティの著者絞り込み/30日窓の境界/管理者一覧からのリンク/メンバー一覧からのリンク・非リンクの2件) |

---

## 3. コンテンツモジュール(Wiki / フォーラム / お知らせ / 文書 / ファイル)

### Wiki

| 機能 | 状態 | 備考 |
|---|---|---|
| ページ CRUD | done | — |
| 階層(親子) | done | — |
| 親の付け替え | done(2026-07-21〜24) | 同一プロジェクト内は循環参照を除外した `parent_id` 変更(編集フォーム)。**プロジェクト間移動を追加(2026-07-24)**: Redmineの`WikiPage#wiki_id` safe_attribute(`handle_rename_or_move`/`handle_children_move`)相当を`WikiPageService::moveToProject()`に実装。表示画面に「別のプロジェクトへ移動」フォームを追加(`IssueService::moveToProject()`と同じUI配置パターン)。移動元・移動先の両方で`rename_wiki_pages`権限が必要(Redmine本家の`safe_attributes=`と同じ二重チェック)。移動したページ自身の親は常にクリア(移動先に同じ親が存在する保証がないため)。~~**Redmine本家との差分**: 本家は子ページも再帰的に移動先へ連れていく(`handle_children_move`)が、本アプリは`IssueService::moveToProject()`が既に確立した「子は最上位に切り離す」簡略化を踏襲(カテゴリ/対象バージョンのような他のプロジェクト間移動と同じ簡略化ポリシー)~~ → **子ページも再帰的に移動(2026-09-20)**: `WikiPageService::moveChildren()`が子・孫を親子関係を保ったまま移動先へ運び、各ページに旧タイトルのリダイレクトを残す。移動先に同名(大文字小文字を区別しない)のページがある子は、Redmineの`update_all parent_id nil`と同じく元のプロジェクトに最上位ページとして残り、その子孫もついていく。テスト: `WikiPageMoveTest.php`。リダイレクトは`wiki_redirects`に`redirects_to_project_id`列を追加してプロジェクト横断対応(移動元プロジェクトに旧タイトルのリダイレクトが残り、移動先プロジェクト+ページを指す)、`WikiLinkInlineParser`もクロスプロジェクトリダイレクトを解決するよう更新 |
| バージョン履歴 | done | 追記型 `wiki_page_versions` テーブル |
| 任意の2バージョン間の差分表示 | done(2026-07-22) | Redmineの`WikiPage#diff`+`Redmine::Helpers::Diff`(単語単位・空白区切りでのLCS差分、追加/削除を`<span>`でマーク)相当。`App\Support\Diff\WordDiffer`に標準的なLCS動的計画法で実装(依存追加なし)。新規`wiki.diff`ルートで`from`/`to`のバージョン番号を受け取り(URL上の順序に関わらず古い方を`from`に正規化)、履歴画面に「旧」「新」のラジオボタン列と「選択したバージョンを比較」リンクを追加。ページサイズに対しO(語数from×語数to)の時間/メモリ計算量(Redmine本家も同様の量) |
| Wiki権限(`view_wiki_edits`/`delete_wiki_pages_attachments`/`manage_wiki`)とWiki全体削除 | done(2026-09-20) | `view_wiki_edits`: 履歴・過去版・差分・注釈(と`GET /wiki/{id}?version=`、Wikiの活動)の閲覧に必要。`delete_wiki_pages_attachments`: Wikiの添付削除。`manage_wiki`: `wiki.pages`の「Wikiを削除」(全ページを履歴・添付ごと削除。各ページは通常の削除経路を通るためWebhookも発火)。マイグレーションで`view_wiki_pages`保有ロールに`view_wiki_edits`、`edit_wiki_pages`保有ロールに`delete_wiki_pages_attachments`を自動付与(従来動作を維持)。`manage_wiki`は自動付与しない。 |
| Annotate/Blame | done(2026-07-22) | Redmineの`WikiAnnotate`相当を`App\Support\Wiki\WikiAnnotator`に実装。各バージョンが差分ではなく全文を保持している前提で、対象バージョンからv1まで逆方向に1バージョンずつ隣接ペアを行単位でLCS差分し、インデックス再マッピング配列で「現在のどの行に対応するか」を遡って追跡(単発のv(n-1)対vn比較では不十分 — Redmine本家のアルゴリズムをそのまま移植)。既存の`WordDiffer`(Wiki差分表示用)のLCSコア部分を`App\Support\Diff\LcsDiffer`に切り出し、単語単位・行単位の両方から共有(既存の差分機能のテストは無変更で通過を確認済み)。新規`wiki.annotate`ルート+Volt画面で行番号/バージョン/著者/内容を表形式表示(同一バージョン/著者の連続行はセルを空白にする「バンディング」表示、Redmineの`annotate.html.erb`と同じ)、`wiki/history.blade.php`の各バージョン行に「(注釈)」リンクを追加 |
| バージョンの復元(revert) | done(2026-07-22) | Redmine自体に専用の「復元」アクションはなく、過去バージョンの本文を編集フォームに読み込んで保存し直すことで新しいバージョンとして記録する(`WikiController#edit`+`content_for_version`)方式。同様に過去バージョン閲覧画面(`wiki.version`)に「このバージョンを復元」リンクを追加、`wiki.edit`へ`?version=N`付きで遷移し、`wiki/form.blade.php`の`mount()`がそのバージョンの本文をプリフィル(`update`権限が必要) |
| バージョン単体の削除 | done(2026-07-22、2026-09-20にRedmineの規則へ是正) | `wiki/history.blade.php`の`deleteVersion()`。**2026-09-20の是正**: (1) 認可: 従来は`edit_wiki_pages`(`update`)で許可していたが、Redmineの`destroy_version`は`preparation.rb:129`で**`delete_wiki_pages`権限**にマップされ、加えて`editable?`(保護されていないページ、または`protect_wiki_pages`)も要求する — 編集権限だけの利用者が履歴を消せた食い違いを解消(`canDeleteVersions`)。(2) 最新バージョンの削除: 従来は不可(「現在バージョン=最も番号が大きい履歴行」という設計上の理由)だったが、Redmineの`page_update_after_destroy`と同じく**削除でき、ひとつ前のバージョンが最新になる**(本アプリは最新=最大番号の行なので行を消すだけで済む)。(3) 残り1件の削除: 従来は不可だったが、Redmineと同じく**ページ自体を削除**(確認ダイアログに明記、Wiki一覧へ遷移)。テスト: `WikiPageVersionTest.php`。**注意(挙動変更)**: 編集権限のみのロールは履歴を削除できなくなった |
| リネーム時のリダイレクト | done(2026-07-22) | Redmineの`WikiRedirect`+`WikiPage#handle_rename_or_move`相当。新規`wiki_redirects`テーブル(`project_id`, `title`, `redirects_to`)を追加、`WikiPageService::update()`がリネーム時に旧タイトル→新タイトルのリダイレクトを作成し、旧タイトルを指していた既存のリダイレクトも新タイトルへ付け替え(多段リネームのチェーン追従)。編集フォームに「旧タイトルへのリンクをリダイレクトする」チェックボックス(既定オン、Redmineの`redirect_existing_links`相当)。本アプリはWikiリンクをタイトルではなく生成時にIDへ解決する設計(`WikiLinkInlineParser`)のため、Redmineのような実際のURLリダイレクトではなく、リンク解決時に直接一致が無ければ`wiki_redirects`にフォールバックする形で実装。ページ削除時は指しているリダイレクトも`WikiPage`の`deleting`イベントで削除 |
| 保護ページ | done | `is_protected` + ポリシー |
| デフォルト保護ページ(Sidebar等) | done(2026-07-22) | Redmineの`WikiPage::DEFAULT_PROTECTED_PAGES`(`%w(sidebar)`)を移植。`WikiPage`モデルの`creating`イベントで、タイトル(大文字小文字を区別しない)が`sidebar`に一致する新規ページを強制的に`is_protected = true`にする ― `protect_wiki_pages`権限を持たない作成者であってもRedmine同様に強制される(フォーム側の保護チェックボックスは同権限を持つ場合のみ表示されるが、それとは独立にモデル層で常時適用)。**さらに(2026-07-22)**: 「Sidebar」ページの内容を実サイドバーとして描画する機能(Redmineの`_sidebar.html.erb`相当)を実装。新規Bladeコンポーネント`<x-wiki-sidebar>`がプロジェクトの「Sidebar」ページ(大文字小文字を区別せず検索)を`WikiMarkdownRenderer`で描画、Redmine本家と同じスコープで`wiki.show`/`wiki.index`/`wiki.date-index`の3画面のみに表示(history/diff/annotate/編集フォームおよびWikiモジュール外の画面は対象外)。各ビューを2カラムのflexレイアウトに変更(メインコンテンツ+サイドバー列) |
| ~~開始ページ設定~~ → **done(2026-07-30)** | **設計はユーザーが3択(新規`wikis`テーブル/`wiki_pages.is_start_page`フラグ/暗黙のルート順)から選択**: 忠実路線の新規`wikis`テーブル案を採用(`project_id`一意+`start_page`文字列、Redmine本家の`Wiki`モデル相当)。**ルーティング再設計**: `wiki.index`(`/projects/{project}/wiki`)の意味を「ページ一覧」から「開始ページへのリダイレクト」に付け替え(Redmine本家の`WikiController#show`のid無し呼び出し=`Wiki#find_or_new_page(nil)`相当)、旧来の一覧機能はファイルごと`resources/views/livewire/wiki/pages.blade.php`に複製し新ルート`wiki.pages`(`/projects/{project}/wiki/pages`)へ退避——`route('wiki.index', ...)`という同じルート名を維持したまま挙動だけ差し替えたため、`projects/show.blade.php`の「Wiki」タブ(意味的にも「一覧」ではなく「開始ページ」を指すのが正しい)は無改修のまま新動作に自動的に追従。一覧に「戻る」性質を持つ内部リンク3箇所(`wiki/date-index.blade.php`のタイトル順に戻る・`wiki/show.blade.php`の削除後リダイレクト・`wiki/form.blade.php`の新規作成キャンセルリンク)は明示的に`wiki.pages`へ付け替え。この付け替えに伴い`Livewire::test('wiki.index', ...)`を呼んでいた既存テスト4ファイル(`WikiExportTest`/`WikiBulkPdfExportTest`/`WikiSidebarTest`/`WikiPageTest`)も`wiki.pages`に一括改名(挙動は変更していないため全件無改修でグリーン)。**この付け替えがLivewireの`mount()`内`$this->redirect()`一発で実現できたのは、本アプリのWikiページURLがRedmine本家のようなタイトルベースではなく元々`{wikiPage}`の数値IDベース route model binding だったため**——本家の`/wiki`は開始ページ本文をその場で直接描画するが、本アプリはタイトルからページを解決して`wiki.show`へリダイレクトする方式に意図的に簡略化(本アプリの既存URL設計との整合を優先)。**開始ページの指定UI**: `wiki/form.blade.php`に「このページを開始ページに設定する」チェックボックスを追加、Redmine本家の`WikiPage#is_start_page`と同じく`rename_wiki_pages`権限でガード(`canRename`を流用、新規ポリシーメソッドは追加せず)。既に開始ページになっているページでは`@disabled`表示(本家の`:disabled => @page.is_start_page`と同じ、外すには別のページ側でチェックを入れ直す一方通行の設計——本家同様、外すだけで「開始ページなし」の状態には戻せない)。保存時、`$data`から`is_start_page`を分離してから`WikiPageService::update()/create()`へ渡す(WikiPage自身のカラムではないため)、タイトル変更と同時にチェックした場合は変更後の新タイトルを`start_page`に保存(リネーム後の`$page->title`を参照する順序を明示的にテストで固定)。`Wiki`行自体は`Project::wikiOrCreate()`で遅延作成(プロジェクト作成時ではなく、開始ページへのリダイレクトか開始ページ指定チェックボックスの保存時に初めて必要になった時点)——Redmine本家の`Wiki.create_default`(Wikiモジュール有効化時に即時作成)とは異なるが、本アプリにはモジュール有効化の単一フックが無いための現実的な代替。**advisorレビューで発見・記録した既知のエッジケース**: `login_required`無効時、開始ページがまだ存在しないWikiで匿名ユーザーが`/wiki`にアクセスすると`wiki.create`(タイトルpre-fill付き)へリダイレクトされるが、`wiki.create`自体はゲスト到達可能ルート一覧に含まれていない(ページ作成は元々「課題一覧・Wiki閲覧」のスコープ外のため)ため、本来閲覧可能なはずの公開プロジェクトで403ではなくログイン画面へのリダイレクトに遭遇してしまう既知の粗さ——`tests/Feature/Authorization/LoginRequiredTest.php`にこのチェーン全体(`wiki.index`→`wiki.create`のリダイレクト先URL→そこが`auth`ミドルウェアでログインへ再リダイレクトされること)を2ホップとも直接アサートするテストとして固定済み。テストは`tests/Feature/Wiki/WikiStartPageTest.php`に8件(`wikiOrCreate()`の遅延作成/冪等性、開始ページ存在時のリダイレクト先、不在時の作成フォームへのプレフィル付きリダイレクト、チェックボックスでの指定、リネームと同時指定時の新タイトル保存、`rename_wiki_pages`権限が無い場合はチェックボックス自体が非表示、外しても既存の開始ページ設定が消えないこと)、`LoginRequiredTest.php`に2件追加(開始ページありの場合の正常リダイレクト、開始ページ無しの場合の上記エッジケース) |
| **マクロエンジン全体** | done(2026-07-24) | `#123` 課題メンションと `[[ページ]]` リンクに加え、`{{toc}}`を実装(2026-07-22)。専用のDOM後処理クラスを新規に書く代わりに、既存の依存関係である`league/commonmark`が同梱する`TableOfContentsExtension`+`HeadingPermalinkExtension`をそのまま利用(新規パッケージ追加なし)。`{{toc}}`単独の行を、その位置のまま見出しのネストされた`<ul>`に置換(Redmineと同じ「書いた場所に展開される」挙動、常に先頭に固定されるわけではない)。`HeadingPermalinkExtension`は全見出しに`id`属性を付与するために必須だが、`insert: after`+`symbol`/`title`を空文字にすることで見出しへの目に見えるパーマリンクアイコン挿入は回避(副作用として、見出しごとに中身が空の非表示`<a>`タグが1つ増える — TOCの実装に必要な既存パッケージの制約によるトレードオフ)。**`{{child_pages}}`を追加(2026-07-24)**: `{{toc}}`と異なりこのマクロを理解する既存パッケージが無いため、`WikiMarkdownRenderer::render()`にHTML後処理として自前実装(`resolveInlineAttachmentImages()`と同じDOM後処理パターン、`html_input=escape`のため生テキストへのHTML注入は不可な点に注意しコミットメッセージにも明記)。単独行の`{{child_pages}}`を、そのページの子ページへのリンク一覧(`wiki.show`の既存「子ページ」セクションと同じくタイトル昇順・フラット表示)に置換。`render()`に新規オプション引数`?WikiPage $page`を追加(末尾のため既存呼び出し元は非破壊)、Wiki関連6箇所(一覧・詳細表示・詳細HTML出力・バージョン表示・編集プレビュー・サイドバー)に配線、課題側3箇所(説明・コメント)は`$page`未指定のまま(Redmineの「Wikiページ上でのみ使用可」という制約と同じ扱い、リテラルテキストとして残る)。Redmine本家の`{{child_pages(Foo)}}`(別ページ指定)・`{{child_pages(depth=2)}}`(再帰段数指定)・`parent=1`オプションは意図的に対象外(本アプリの既存「子ページ」UI自体がフラット1階層のみで再帰非対応のため、それに合わせたスコープ)。**`{{collapse}}`を追加(2026-07-24)**: `{{toc}}`/`{{child_pages}}`と異なり本文が複数行の未レンダリングMarkdownそのものであるため、HTML後処理だけでは完結できない — CommonMark変換前に生テキストから`{{collapse}}`〜`}}`ブロックを正規表現で抽出してプレースホルダ段落に置換(`html_input=escape`のためHTMLプレースホルダは使えず、プレーンテキストのプレースホルダを使用)、抽出した本文は`render()`自身を再帰呼び出しして先にHTML化した上でブロック情報として保持、CommonMark変換後にプレースホルダ段落を`<details><summary>ラベル</summary>本文HTML</details>`へDOM置換(`{{child_pages}}`と同じ`HtmlFragment`パターン、`importNode()`で子孫ノードごと差し込み)。Redmine本家はjQueryによる表示/非表示トグル(2つのリンク+隠しdiv)だが、本アプリはネイティブHTML5の`<details>/<summary>`要素を採用しJS不要で同等の「既定で折りたたみ、クリックで展開」を実現。`{{collapse(ラベル)}}`で表示ラベルを指定可能(省略時は既定文言)。~~**既知の制限**: 同じマクロのネスト(`{{collapse}}`の中に`{{collapse}}`)は正規表現の非貪欲マッチが最初に見つかった`}}`で本文を閉じてしまうため正しく入れ子にならない~~ → **ネスト対応(2026-09-20)**: 正規表現を、開始行と`}}`行の深さを数える行スキャナ(`collapseOpening()`/`collapseClosingIndex()`)に置換し、任意の深さで入れ子にできる。閉じられていないブロックはリテラルのまま(他の本文は失われない)。**併せて既存の不具合を修正**: 前後の本文と空行なしで隣接した`{{collapse}}`は、プレースホルダが段落に混ざって`COLLAPSE-MACRO-PLACEHOLDER-0`という文字列がそのまま表示されていたため、プレースホルダの前後に空行を入れて独立した段落にした。テスト: `WikiCollapseMacroTest.php`。旧記載: (内側の開始タグがリテラルテキストとして残るのみでクラッシュや内容欠落はしない、安全側に縮退する設計として意図的に許容、テストで実際の縮退挙動を明示的に検証)。**`{{include}}`は実装済み**(`WikiMarkdownRenderer::extractIncludeMacros()`/`replaceIncludeMacros()`、`{{collapse}}`と同じDOM後処理パターン): 単独行の`{{include(ページ名)}}`を対象ページの本文HTMLに置換、`$includedPageIds`を再帰呼び出しに引き回して循環インクルード(AがBを含みBがAを含む)を検出・防止(`tests/Feature/Wiki/WikiIncludeMacroTest.php`)。Redmine本家の`{{include(project:Page)}}`(他プロジェクト参照形式)は対象外(ステータス欄がpartialのまま「未実装」と記載されていたのは本文更新漏れ、行実装自体は完了しており今回訂正)。**別件で確認(2026-07-22)**: 通常のMarkdown箇条書き(`- item`)がアプリ全体で箇条書き記号(disc)無しで表示される既存のTailwindスタイリング上の問題を発見(本機能の変更が原因ではなく、既存の`prose`クラス設定の問題)。**マクロを追加(2026-09-20、A7-01)**: `App\Support\Markdown\WikiMacros`(登録可能なマクロ表。プラグインは`register()`で追加)に`{{hello_world}}`/`{{macro_list}}`/`{{recent_pages(N)}}`/`{{issue(id, tracker=false, subject=false, status=false)}}`/`{{thumbnail(file, size=, title=)}}`を追加し、`{{child_pages(ページ名, depth=N, parent=1)}}`のオプション(再帰の深さ・親の表示・別ページ指定)と`{{include(プロジェクト:ページ)}}`(別プロジェクトのページ。そのページの閲覧権限が無ければ「見つかりません」)に対応。段落単独の行だけが対象、閲覧権限は表示するユーザーで判定、動的な出力を持つマクロはフォーマット済みテキストのキャッシュ対象外。**Redmineとの差分**: 文中(インライン)での使用は不可、`{{issue}}`のproject/description等のオプションの一部と`{{child_pages}}`の`parent`以外の細かいオプションは未対応。テスト: `WikiMoreMacrosTest.php` |
| セクション単位編集 | done(2026-07-22) | Redmineの`Redmine::WikiFormatting::SectionHelper`(セクション分割)+`StaleSectionError`(競合検知)を移植。新規`WikiSectionSplitter`(ATX/Setext見出し・フェンスコードブロックを認識してMarkdownを前/対象セクション/後の3分割、見出し出現順の1始まりインデックス)。表示画面(`wiki/show.blade.php`)は`update`権限を持つ閲覧者にのみ、レンダリング後のHTMLの各見出しへ`WikiSectionEditLinkInjector`(DOMDocumentで見出しを文書順に走査し「編集」リンクを注入 — Volt SFCのPHPブロックに`<?xml encoding="utf-8"?>`という`?>`を含む文字列リテラルを直書きすると、そこでブロックが打ち切られてコンパイルエラーになるため独立クラスに分離)経由で`?section=N`リンクを付与。編集フォーム(`wiki/form.blade.php`)は`?section=`があれば`mount()`で該当セクションのみを本文欄にプリフィルし、読み込み時点のセクション本文のSHA-256ハッシュを保持。`save()`は保存直前に最新の全文を再取得してハッシュを再検証し、一致しなければ他ユーザーの競合編集とみなして保存を中止しエラー表示、一致すれば`WikiSectionSplitter::updateSection()`で全文へ差し戻す |
| プレビュー | done(2026-07-22) | 編集フォームに「プレビュー」トグルボタンを追加、`WikiMarkdownRenderer`で本文テキストエリアの現在値を保存せずにレンダリング(Wiki表示画面と同じレンダラーを再利用)。既存ページ編集時はインライン画像参照もそのページの既存添付ファイルに対して解決(このフォーム送信で選択中だが未アップロードのファイルはまだMediaレコードが無いため対象外) |
| PDF/HTML/TXT/ZIPエクスポート | partial(2026-07-23) | ページ単体のTXT/HTMLエクスポートを実装(Redmineの`WikiController#show`の`format=txt`/`format=html`相当)。新規`export_wiki_pages`権限を追加(Redmineの同名パーミッション、`:read => true`相当で`readOnly: true`として登録、Redmine自体が`:public`指定なしのためデフォルトのMember階層のまま)。TXTは現行バージョンの生Markdownをそのまま返却、HTMLは`WikiMarkdownRenderer`の出力を最小限のHTML文書(charset/title付き)でラップ。ファイル名はページタイトルから生成(`/`・`\`のみサニタイズ)。**Wiki全体のZIP一括エクスポートを追加(2026-07-23)**: `wiki.index`に「ZIP(TXT)」「ZIP(HTML)」リンクを追加(Redmineの`WikiController#export`相当、`export_wiki_pages`権限で保護、新規`WikiPagePolicy::exportAll()`)。プロジェクト内の全ページを既存の単体エクスポートと同じ形式(生Markdown/最小HTML文書)でPHP標準の`ZipArchive`(新規Composer依存なし)にまとめて1ファイルとしてダウンロード、エントリ名はページタイトルベース(`wiki_pages`の`[project_id, title]`一意制約によりプロジェクト内で衝突しない)。~~PDF出力(新規依存が必要なため対象外)は引き続き未実装のまま~~ → **done(2026-07-30)**: Wikiページ単体PDF・Wiki一括PDFとも実装済み(§0.5「PDF出力」行参照、2026-09-19訂正) A7-15(2026-09-20): ZIP内のファイル名をRedmineの`archived_wiki_page_filename`に揃えた(`\`と`/?%*:|"'<>`・改行を`_`に置換、衝突は`名前(1).txt`)。A7-07(ZIPに添付を含める)はRedmineにない機能のため対象外(C-26)。
| 日付インデックス表示 | done(2026-07-22) | Redmineの`WikiController#date_index`相当。新規`wiki.date-index`ルートで、各ページの現在バージョンが書かれた日付(`currentVersion->created_at`)でグルーピングし新しい日付順に表示。Wiki一覧画面から「日付順に表示」でアクセス可能 |
| ページのWatch | done(2026-07-23) | `WikiPage`に`watchers()`(ポリモーフィック`Watcher`)を追加、`view_wiki_pages`権限で自己Watch/Unwatch可能。~~他ユーザーの追加/削除UIはまだなし(Issueの`manageWatchers`相当は未実装)~~ → **done**: `IssuePolicy::manageWatchers()`/`issues/show.blade.php`の実装をそのまま移植。Redmineにはウォッチャー管理専用の権限が無いため(Issueの`add_issue_watchers`のような専用権限がWiki側には存在しない)、既存の`edit_wiki_pages`権限をゲートとして採用。ウォッチャー追加候補はプロジェクトメンバーに限定(Issueと同じ`Rule::exists('members', 'user_id')`保護)。ブラウザで実際に他メンバーをウォッチャーとして追加できることを確認済み |
| 添付ファイル | done(2026-07-21) | `WikiPage implements HasMedia`。フォームからアップロード、詳細画面で表示/削除(`update`権限) |

### フォーラム(Boards / Messages)

| 機能 | 状態 | 備考 |
|---|---|---|
| Board CRUD・並べ替え | done | — |
| ネストしたBoard | done(2026-07-21) | `boards.parent_id`追加。フォーム側で自己参照/循環を防ぐ親セレクト(Wikiページと同じパターン)、一覧はトップレベル+ネストした子を表示 |
| トピック作成/返信 | done | — |
| Sticky | done | — |
| ロック | done | `MessagePolicy::reply` で返信禁止を実装 |
| トピックの別Boardへの移動 | done(2026-07-21) | `edit_messages`保持者(`MessagePolicy::manageFlags`)がトピック詳細から別フォーラムへ移動可能。返信は独自の`board_id`を持つため、トピックと返信の両方を一括更新 |
| トピックのWatch | done(2026-07-23) | トピックのみWatch可能(返信は対象外、`MessagePolicy::watch`)。~~他ユーザーの追加/削除UIはまだなし~~ → **done**: Wikiページで実装した`manageWatchers`パターン(`edit_messages`権限をゲートに使用、Redmineに専用権限が無いため)をそのまま移植。ウォッチャー追加候補はプロジェクトメンバーに限定。ブラウザで実際に他メンバーをウォッチャーとして追加できることを確認済み |
| 引用返信 | done(2026-07-21) | トピック/返信それぞれに「引用」ボタン、返信入力欄に`>`引用形式をプリフィル |
| 添付ファイル | done | **訂正(2026-07-22)**: 従来「`Message`が`HasMedia`未実装」として`missing`と報告されていたが、実際には`Message implements HasMedia`が既に存在し、トピック作成/返信フォームでのアップロード、詳細画面での一覧表示・説明文編集・削除、`BoardTest`でのテストカバレッジまで全て揃っていた(過去に別行の一括作業で実装されテスト済みだったが、この行自体の更新が漏れていた) |
| 返信のページネーション | done(2026-07-21) | 25件/ページでページネーション。見出しの件数は`total()`で全件数を表示 |
| Atomフィード | done(2026-07-22) | Redmineの`BoardsController#show`(`format.atom`)相当。新規`boards.atom`ルート(`GET /projects/{project}/boards/{board}.atom`、`{board}`ルートより前に登録 — `boards.show`の無制約パラメータが先に`5.atom`を丸ごと飲み込んでしまうのを防ぐため`whereNumber`+登録順で回避)。ボード内の全メッセージ(トピック・返信とも)を新着順に最大15件(既存の`activity.atom`と同じ既定値)配信、返信は`{board.name}: {topic.subject}`のタイトルでトピックURLへリンク(Redmineの`Message#acts_as_event`と同じ形式)。既存の`ActivityEntry`DTOとAtomテンプレート構造を`activity-atom.blade.php`から流用(`feeds.board-atom`) |

### News

| 機能 | 状態 | 備考 |
|---|---|---|
| CRUD・概要/本文 | done | — |
| コメント | done | `NewsComment` |
| 添付ファイル | done | `News implements HasMedia` |
| Watch・作成者自動Watch | done(2026-07-21〜24) | `News::watchers()`+トグルボタン、作成時に作成者を自動Watch。**他ユーザーのWatcher管理UIを追加(2026-07-24)**: Wiki(`WikiPagePolicy::manageWatchers()`)/フォーラム(`MessagePolicy::manageWatchers()`)と同一パターンで、`NewsPolicy::manageWatchers()`(`manage_news`権限でゲート、Redmineに専用の権限が無いため`update`/`delete`と同じ権限を再利用)+ プロジェクトメンバーからの追加・削除UIを実装 |
| ~~メール通知~~ → **done**(2026-07-30) | 詳細は§1「メール通知(News)」行参照。本行は更新漏れで`missing`のまま放置されていた(他の設定タブ行でも過去に複数回発生した既知のパターン、詳細は§0.5冒頭の訂正メモ参照) |
| プロジェクト横断のNews一覧 | done(2026-07-22) | Redmineの`NewsController#index`(`project_id`無し、`News.visible`スコープ)相当。新規`/news`ルート(`news.global-index`、初のプロジェクト非スコープなグローバル画面)を追加、ヘッダーナビゲーションに「お知らせ」リンクを配置。`view_news`権限の可視性判定(プロジェクトのアーカイブ/クローズ/モジュール有効性含む)はSQLの単一WHERE句では表現できないため、`projects.index`と同じ「全件取得→`can('view', $news)`でメモリ内フィルタ→`LengthAwarePaginator`で手動ページネーション」方式を踏襲(10件/ページ、Redmineの既定値と同じ)。プロジェクト列を追加表示 |
| Atomフィード(プロジェクト単位) | done(2026-07-22) | Redmineの`NewsController#index`(`format.atom`)相当。新規`news.atom`ルート(`GET /projects/{project}/news.atom`)+`NewsAtomController`。プロジェクト内の全お知らせを新着順に最大15件配信(`ActivityFeedController::LIMIT`共通)、`boards.atom`と同じ`ActivityEntry`DTO+共有`feeds.atom`テンプレートを流用。`news.index`画面に「Atom」リンクを追加 |

### Documents

| 機能 | 状態 | 備考 |
|---|---|---|
| CRUD | done | — |
| カテゴリ | done | `Enumeration` 経由 |
| 添付ファイル | done | `Document implements HasMedia` |
| カテゴリ/日付/タイトル/作成者でのグルーピング・並べ替え | partial(2026-07-22) | Redmineの`DocumentsController#index`(`sort_by`パラメータ)を移植。`documents.index`に`#[Url]`束縛の`sortBy`(既定`category`)を追加、カテゴリ別(既定・未分類は空文字キーで先頭)/更新日別(新しい日付グループが先頭)/タイトル先頭文字別にグルーピング・並べ替え。~~「作成者」でのグルーピング(Redmineは各文書の最新添付ファイルのアップロード者でグルーピング)は本アプリの添付ファイルがアップロード者を記録していないため対象外~~ → **実装(2026-09-20、A7-09)**: アップロード者を`media.custom_properties.uploaded_by`に記録する仕組みを追加。**記録は1か所**(`AppServiceProvider`の`Media::creating`フック — リクエストのユーザー、APIガードも含む)なので、課題・Wiki・フォーラム・News・文書・ファイル・API(`PendingUpload`経由の`move()`でも保持)のどの経路も漏れず、リクエスト外(受信メール)は送信者を明示指定、なければ匿名。`AttachmentUploader`(`idOf`/`userOf`/`usersFor`)で取得。文書一覧に「作成者」並べ替えを追加(最新の添付のアップロード者名でグループ化、添付なし・不明は「(不明)」グループ、メディアは先読み)。**注意**: 実装前にアップロードされたファイルはアップロード者が不明のまま。テスト: `AttachmentUploaderTest.php` |
| カスタムフィールド | done(2026-07-22) | `CustomizableType::Document`を追加(Issue/Project/Version/Group/TimeEntryActivityに続き4回目の同一パターン適用)。`Document`に`HasCustomFields`トレイト+`customizableType()`+`relevantCustomFields()`(Versionと同じ、プロジェクトのロール経由で可視性解決)を実装、`documents/form.blade.php`に入力欄、`documents/show.blade.php`に表示欄(Issue詳細と同じ`customFieldDisplayValues()`パターン)を追加。カスタムフィールド管理画面の「対象」選択肢は`CustomizableType::cases()`を直接列挙しているため追加のUI変更は不要 |

### Files モジュール

| 機能 | 状態 | 備考 |
|---|---|---|
| バージョンへのファイル添付 | done | — |
| **プロジェクトレベルのファイル**(バージョン非依存) | done(2026-07-21) | `Project implements HasMedia`。アップロードフォームの「バージョン」に「プロジェクト全体(バージョンなし)」を追加、`ProjectPolicy::manageFiles`(`manage_files`権限)で保護 |
| ファイル名/日付/サイズ/ダウンロード数での並べ替え | done(2026-07-22) | `files/index.blade.php`にRedmineの`FilesController#index`の`sort_clause`相当のUIを追加。4項目のボタンをクリックで昇順/降順切替、プロジェクト直下・各バージョンの両方の一覧に同一のソート順を適用(Redmine同様、コンテナごとに個別のソート順は持たない)。添付は`Media`(Eloquentコレクション)のためDBの`ORDER BY`ではなくPHP側の`sortBy()`で実装 |
| 複数ファイル同時アップロード | done | — |

### 添付ファイル(横断)

| 機能 | 状態 | 備考 |
|---|---|---|
| エンティティごとの複数添付 | done | Spatie MediaLibrary、対象は Issue/Version/News/Document/WikiPage/Message(2026-07-21〜) |
| サムネイル/画像変換 | done(2026-07-22) | Redmineの`Attachment#thumbnail`相当。添付ファイルを持つ全モデル(Issue/WikiPage/News/Document/Message/Project/Version)に`registerMediaConversions()`で100x100の`thumb`変換を登録(共通ロジックは`App\Concerns\HasThumbnails`、`InteractsWithMedia`の同名no-op実装と衝突するため`insteadof`で明示解決)。非画像ファイルはMedia Library側が自動的にスキップ(`ImageGenerator`が対応しないため)。専用ルート`attachments.thumb`+`AttachmentThumbnailController`でプライベートディスク越しに配信(`attachments.show`と同じ`view`権限境界、強制ダウンロードではなくインライン表示用)。`<x-attachment-thumbnail>`コンポーネントを課題・Wiki・ファイル(プロジェクト/バージョン)・News・Document・フォーラムの添付一覧すべてに配線 |
| 添付ファイルの説明文 | done(2026-07-22) | Redmineの`Attachment#description`相当を`media.custom_properties.description`として実装。課題・Wikiページ・プロジェクト/バージョンファイル・News・Documentに続き、フォーラム(Message、トピック・返信とも)にも対応し、添付ファイルを持つ全エンティティで完了。返信は`WithPagination`で分割表示されるため、`mount()`時点で全返信を(表示ページに関わらず)一括取得して`attachmentDescriptions`を事前充填 |
| 添付ファイルの一括ダウンロード/一括編集 | done(2026-09-20) | `GET /attachments/{type}/{id}/download`(課題/お知らせ/文書/Wiki/フォーラム投稿の添付をZIP化。同名は`name(1).ext`、閲覧権限が必要、合計が設定`bulk_download_max_size`(既定102400KB、0で無制限)を超えると拒否)と、ファイル名・説明を一括編集する`attachments.edit-all`(`update`権限。ファイル名は実ファイルとサムネイルごと改名、パス区切り/制御文字/禁止拡張子は拒否)。リンクは課題・Wiki・お知らせ・文書の各詳細に表示(フォーラム投稿は画面上のリンクなし。ルートのみ対応)。 |
| 添付ファイルの表示画面(`attachments#show`) | done(2026-09-20) | `GET /attachments/{id}/preview`(`attachments.preview`)を追加(既存の`/attachments/{id}`は従来どおりダウンロード)。拡張子から種別を判定(Redmineの`MimeType.of`)し、画像=インライン、PDF=埋め込み、text/`*.diff|patch`=本文(`file_max_size_displayed`以下、`repositories_encodings`で変換、diffは行種別で着色)、その他=詳細とダウンロードリンクのみ。同じレコードの前へ/次へ。`GET /attachments/{id}/inline`は画像/PDFだけを`Content-Disposition: inline`+サンドボックスCSP+nosniffで返す(HTML等は返さない)。閲覧権限は元レコードに従う。課題・Wiki・お知らせ・文書・フォーラム・ファイルの添付一覧に「表示」リンク(表示できる種別のみ)。サムネイルのリンク先は従来のダウンロードのまま。 |
| ダウンロード数カウント | done(2026-07-21) | `AttachmentController`が`media.custom_properties`の`download_count`をダウンロードごとにインクリメント。`<x-download-count>`コンポーネントで各添付ファイル一覧に表示 |
| Wiki/フォーラム投稿への添付 | done(2026-07-21) | Wiki/フォーラム投稿(`Message`、トピック・返信とも)ともに対応 |
| 本文中のインライン画像参照(`attachment:file.png`) | done(2026-07-22) | Redmineの実装(`InlineAttachmentsScrubber`)は`attachment:`という独自プレフィックス構文ではなく、通常のMarkdown画像記法でファイル名だけを裸で書いた場合(`![](screenshot.png)`)にレンダリング後のHTMLを走査し、同一オブジェクトの添付ファイルからファイル名(大小文字区別なし)で解決する後処理。本アプリも`WikiMarkdownRenderer`に同等の後処理(DOMDocument走査)を追加し、Wikiページ本文・過去バージョン表示の両方で対応。スキームやパスを含むURL、非画像拡張子は対象外(Redmineと同じ拡張子ホワイトリスト)。実装当時は課題説明文がそもそもMarkdownレンダリングされていなかったためWikiのみのスコープだったが、その欠落は「課題本文・コメントのMarkdownレンダリング」行(2026-07-22)で解消済み、インライン画像参照も課題側で同様に有効 |

---

## 4. クエリ・レポート・工数管理・ダッシュボード横断機能

**見出しの結論: マイページを除き、プロジェクト横断ビューが一切存在しない。** `routes/web.php` の非プロジェクトルートは `/my/page` と `/profile` のみ。Issues/TimeEntries/Activity/Calendar/Gantt/Search は全て `/projects/{project}/...` 配下限定。

### クエリ/フィルタ/レポートエンジン

| 機能 | 状態 | 備考 |
|---|---|---|
| 保存済みクエリ(フィルタ/列/ソート/グループ) | done | `App\Models\Query` |
| 公開/非公開 | done(2026-07-22) | Redmineの`Query::VISIBILITY_PRIVATE/ROLES/PUBLIC`を移植。`queries.is_public`(boolean)を`visibility`(文字列enum、既存データは`true→public`/`false→private`で移行)に置き換え、`query_role`ピボットテーブルを追加。新規`manage_public_queries`権限を登録し、`saveQuery()`はこの権限を持たない場合サーバー側で強制的に`private`へフォールバック(Redmineの`QueriesController#new/#create`と同じ、クライアント側のフォーム自体も非保持者には選択肢を出さない二重の防御)。`visibleTo()`はロール判定を含めて全面書き換え(匿名ユーザーにも対応)、`savedQueries()`はロール交差判定がSQL述語1本で表現できないため`projects.index`と同じ「取得後にメモリ内フィルタ」方式に変更。課題一覧・工数一覧の両方の保存済みクエリ機能に適用 |
| プロジェクト横断クエリ | partial(2026-07-22) | `queries.project_id`は元々nullable(未使用)だった列を配線。`Query::visibleIn()`をプロジェクトの保存済みクエリ一覧+`project_id IS NULL`のグローバルクエリのUNIONに拡張(Redmineの`global_or_on_project`スコープ相当)、新規`Query::visibleGlobally()`を`issues.global-index`の保存済みクエリ一覧に接続。グローバルクエリの保存/読込を`issues.global-index`に追加(`saveQuery`/`loadQuery`/保存フォームは`issues.index`と同じ`<x-saved-query-save-form>`コンポーネントを再利用)。`Query::resolveVisibility()`はプロジェクト無しの保存では`manage_public_queries`を判定するプロジェクトが無いため、管理者のみ非公開以外を選択可能(Redmineの`User#allowed_to?(action, nil)`がグローバル権限オプション無しでは常にfalseを返す挙動と一致)。`Roles`可視性のグローバルクエリは「いずれかのプロジェクトで一致するロールを持つメンバーシップがあるか」で判定する新規`AuthorizationService::hasAnyMembershipWithRoles()`を追加(Redmineの`user.memberships.joins(:member_roles)`相当、単一プロジェクト内のロール交差ではなく全プロジェクト横断)。**工数一覧(2026-07-22追加)**: `time-entries.global-index`にも同一パターンを配線(グローバル保存/読込に加え、このページ独自の`groupBy`もクエリの`group_by`に保存/復元される点が課題一覧との違い)。`time-entries.index`側の`loadQuery()`もプロジェクト固有クエリに加えてグローバルクエリを読み込めるよう拡張 |
| 列選択 | partial(課題、2026-07-22)/partial(工数、2026-07-22) | 課題: カスタムフィールドも列として選択可能に(上の「表示列・CSV列としてのカスタムフィールド」行参照)。CF列での並べ替えは2026-09-20に対応(上の行参照)。工数: `issues.index`と同じ`DISPLAY_COLUMNS`+チェックボックスUIパターンを移植(日付/担当者/作業分類/課題/コメント/時間の6列から選択、既定は全列表示)。表示テーブル・CSVエクスポートとも選択列に追従、保存済みクエリの`column_names`にも反映(既存の`saveQuery`/`loadQuery`が空配列を書き込んでいた不備も合わせて解消)。課題側と同様、カスタムフィールドは列として選択不可、列の並べ替え(ドラッグでの順序変更)も対象外 A2-01(2026-09-20): 課題・工数の両一覧で選択列の並び順を◀▶ボタンで変更可能に(`ReordersColumns`トレイト + `<x-column-order>`、2列以上選択時のみ表示)。テーブル見出し・CSV・保存クエリの`column_names`が並び順に追従。ドラッグ&ドロップは未実装(意図的に外した)。 A2-02(2026-09-20): 既定クエリの3段階(ユーザー個人設定→`projects.default_issue_query_id`→設定`default_issue_query`)を`DefaultIssueQuery`で解決し、URLに条件のない課題一覧をそのクエリで開く。プロジェクト/全体の既定は公開クエリのみ(全体は全体クエリのみ)。プロジェクト設定と管理設定に選択欄を追加、クエリ削除で既定は解除 A1-16(2026-09-20): 課題一覧の列に親課題・更新日・終了日・最終更新者(最新journalのユーザー、なければ作成者)・非公開・説明・最新のコメント(公開コメントのみ)を追加(`Issue::lastJournal()`/`lastNotesJournal()`、CSVにも反映)。説明と最新のコメントは行の下にブロック行で表示(Redmineの`inline: false`)。更新日・終了日は並べ替え・絞り込みも可(`updated_at`/`closed_on`をフィルタ登録)。`id`は既に常時表示、`project`列はプロジェクト単位の一覧では意味がないため対象外。
| グルーピング | partial(2026-07-23) | ステータス/トラッカー/優先度/担当者に加え、単一値のカスタムフィールド(`multiple: false`)でもグルーピング可能に。件数はSQL `GROUP BY`による全件集計(2026-07-21、ネイティブ列)/`custom_field_values`への`LEFT JOIN`(2026-07-23、カスタムフィールド。ON句にフィールド絞り込みを含めることで、値未設定の課題も「未設定」グループとして残るようにしている)。表示行自体は現在のページ内のみ(意図的、パフォーマンス上の理由)。複数値カスタムフィールド(`multiple: true`)はJOINによる`COUNT(*)`の水増しを避けるためグルーピング対象外(グループ化選択欄にも表示しない) |
| 合計/集計 | done(2026-07-22) | 課題: 一覧に予定工数/実績工数の合計を表示(Redmineの`issue_list_default_totals`相当、両方固定表示で設定化はしない)。全体合計はフィルタ適用後の全件をSQL集計(ページ内ではない)、グループ化時は各グループ見出しに件数+予定/実績合計を表示(`groupTotals`を件数のみ→件数/予定/実績の連想配列に拡張、実績はtime_entriesをJOINしたグループ別SUM)。工数: 従来通り全体+グループ別の時間合計 A2-09(2026-09-20): 設定化。`issue_list_default_totals`(予定工数/実績工数/残り工数(予定)から選択、既定は従来どおり予定+実績、空なら合計行なし)と`time_entry_list_defaults`(`column_names`=工数一覧の初期列、`totalable_names`に`hours`を含めると時間の合計を表示。既定は全列+合計)。`ListDefaults`。数値カスタムフィールド・合計列(`total_*`)の合計は未対応。
| 相対日付フィルタ(「過去N日以内」等) | done | — |
| フィルタ演算子(=,≠,in,contains,empty,between,≥/≤) | done | — |
| カスタムフィールドでのフィルタ | done(課題)/n/a(工数) | — |
| 複数列ソート | done(2026-07-22) | `QueryFilterEngine::applySort()`は元々`[key, direction]`配列を受け取れる設計だったが呼び出し側が1列分しか渡していなかった。課題一覧に2列目・3列目の選択欄を追加(Redmine同様最大3列)。列見出しクリックは引き続き1列目のみ変更。保存済みクエリの`sort_criteria`も3列分を保存/復元 |
| CSVエクスポート | partial(2026-07-23) | 課題・工数とも対応、両方ともエンコーディング(UTF-8/Shift_JIS)・区切り文字(カンマ/セミコロン/タブ)オプション付き(工数側は2026-07-23に課題側と同一パターンで追加。UTF-8にはBOM付与、Shift_JISはBOMなしで`mb_convert_encoding`変換、不正な値はUTF-8/カンマへフォールバック)。PDFはなし。Atomは別行(本節冒頭「Atom フィード」行)で対応済み — 現在の絞り込み状態は反映しない固定フィード |
| 課題レポート(トラッカー/ステータス別集計) | done(2026-07-23) | Redmineの`ReportsController#issue_report`相当。`issues/report.blade.php`(`/projects/{project}/issues/report`)でトラッカー/優先度/カテゴリ/対象バージョン/担当者/作成者の6軸×ステータス別件数グリッドを表示。カテゴリ/バージョン/担当者は「なし」行も集計。各セル(次元×ステータス)と行の「合計」列をRedmineの`aggregate_link`(`reports/_details.html.erb`)相当としてクリック可能なリンクに変更、直上の「課題一覧のURLクエリ文字列によるフィルタ初期化」機構で該当次元(+セルの場合はステータスも)を絞り込んだ課題一覧へ遷移(件数0のセルはリンク化しない、Redmine同様)。「なし」行(カテゴリ/バージョン/担当者未設定)は`=`ではなく`empty`演算子でリンク。サブプロジェクト集計・CSV出力は引き続き意図的に対象外(Redmine自体もこのグリッドとは別のドリルダウン専用ページで折れ線/棒グラフとCSVを提供しており、本アプリは1画面に統合する設計を既に選択済みのため、その差分は対象外のまま)。**詳細ページ・サブプロジェクト・可視性(2026-09-20、A2-06)**: 各集計に「詳細」リンク(`/projects/{p}/issues/report/{detail}`、`issues.report-details`)を追加し、ステータス別に加えて未完了/完了/合計の列と合計行、CSV(UTF-8 BOM付きまたはShift_JIS、区切り文字選択)を出す。設定`display_subprojects_issues`がオンなら、閲覧できるサブプロジェクトの課題も集計し「サブプロジェクト別」の集計が加わる。集計は`App\Support\Reports\IssueReport`に集約。**不具合修正**: 従来のレポートは課題の可視性(非公開課題など)を無視して全件を数えていたため、閲覧できる課題だけを数えるようにした。**Redmineとの差分**: 行は本アプリ従来どおり全ステータスを列に出す(Redmineは使われているステータスのみ)、グラフは対象外。テスト: `IssueReportDetailsTest.php` |
| 課題一覧のURLクエリ文字列によるフィルタ初期化 | done(2026-07-23) | `App\Concerns\InteractsWithQueryFilters`(課題一覧/工数一覧/ガントで共有)の`filterOperators`/`filterValues`に`#[Url]`を追加(`activeFilterKeys`は元々`#[Url]`済みだったが、肝心の演算子/値が欠けていたため実質機能していなかった)。Redmineの`f[]`/`op[]`/`v[][]`クエリ文字列によるフィルタ済みディープリンクと同等の仕組みが3画面すべてで有効に。利用例: ロードマップの課題数リンク(直上の行)、課題レポートのセル/合計リンク(本節末尾の「課題レポート」行) |

### 工数管理

| 機能 | 状態 | 備考 |
|---|---|---|
| TimeEntry CRUD | done(2026-07-23) | 一括編集・一括削除に対応。`time-entries/index.blade.php`にIssue一覧のbulk edit/deleteと同じパターン(チェックボックス列+選択件数パネル)を追加。編集対象は作業分類/日付/コメントの3項目のみ(時間・プロジェクト移動・カスタムフィールドは対象外 — TimeEntryにカスタムフィールドは存在しないため)。認可は既存の`TimeEntryPolicy::update`/`delete`をエントリごとにループ適用(自分の記録は`edit_time_entries`権限がなくても対象、他人の記録は同権限が必要)。プロジェクト横断一覧(`time-entries.global-index`)は対象外(Issueのbulk操作が`issues.global-index`を対象外にしているのと同じ判断) A8-01(2026-09-20): 単体編集にプロジェクト移動を追加(`log_time`のあるプロジェクトのみ。移動時は別プロジェクトの課題を外し、作業分類/担当者も移動先に合わせる)。一括編集は作業分類/日付/コメントに加えて**プロジェクト・課題(番号または「課題を外す」)・担当者・時間**を変更可能に(移動時は課題の扱いの指定が必須)。移動先/課題/担当者の検証はA5-09の`timelog_*`設定を含む`TimeEntryService`経由。 A8-03/A13-03(2026-09-20): `time_entries.author_id`(記録者。`user_id`は工数の持ち主)を追加し、Web/API/インポート(インポート実行者)/コミットキーワードで記録。権限`edit_own_time_entries`(自分の工数のみ編集・削除)、`log_time_for_other_users`(他ユーザー分の記録。従来は`edit_time_entries`を流用)、`import_time_entries`、`import_issues`を追加し、ポリシー/フォーム/API/インポートに適用。**マイグレーションが従来の代用権限を持つロールへ新権限を自動付与**(log_time→edit_own/import_time_entries、edit_time_entries→log_time_for_other_users、add_issues→import_issues)するため既存ロールの挙動は変わらない。
| 工数種別(TimeEntryActivity) | done | プロジェクト別の有効/無効上書きに対応(2026-07-22、詳細は上の「プロジェクト別 Enumeration」行を参照)。カスタムフィールドはなし |
| 課題の実績工数合計 | done(訂正2026-07-22) | **訂正**: 従来「partial」と誤記されていたが、`Issue::totalSpentHours()`は`descendantIds()`の再帰CTEで子孫全体を合算済み(上の「子孫を含めた予定/実績工数の集計」行と同一実装、本行が重複・古いまま残っていたため訂正)。課題詳細画面にも「合計: X時間」として表示される |
| プロジェクトの実績工数合計 | done(2026-07-22) | `projects/show.blade.php`に「実績工数」ブロックを追加(`view_time_entries`権限保有時、工数が1件以上ある場合のみ表示)。Redmineの`ProjectsController#show`の`@total_hours`相当だが、`display_subprojects_issues`設定自体が本アプリに存在しないためサブプロジェクト分の合算は対象外(このプロジェクト自身のTimeEntryのみ) A3-06(2026-09-20): 設定`display_subprojects_issues`(**既定オフ**、Redmineは既定オン)を追加(`SubprojectScope`)。オンのとき、課題一覧(プロジェクト単位)が閲覧できるサブプロジェクトの課題も表示(各プロジェクトの課題可視範囲を維持、列「プロジェクト」を追加)、プロジェクト概要の実績工数合計が`view_time_entries`のあるサブプロジェクトの工数を含む。ガント/カレンダー/レポートへの適用と`subproject_id`フィルタは未対応(A1-17)。
| ~~多次元工数レポート(ピボット表)~~ | **done**(2026-07-30) | Redmine::Helpers::TimeReport(`lib/redmine/helpers/time_report.rb`で確認)を参照実装として、`/projects/{project}/time_entries/report`(`time-entries.report`)を新設。新規`App\Support\TimeReport\{TimeReportCriterion,TimeReportPeriod,TimeReportBuilder,TimeReportTable}`が実装の中核 — `TimeReportBuilder::build()`は選択された最大3つの行軸(criteria)+`time_entries.spent_on`でSQL側を1クエリ集計(`SUM(hours)`)し、期間(年/月/週/日)へのバケット化はRedmine同様PHP側で行う(SQLはあくまで日単位のまま集計、週/年境界の粗い括りはSQLでなくRuby/PHP側で計算するのはRedmine自身も同じ二段構え)。行軸はステータス/バージョン/カテゴリ/担当者/トラッカー/作業分類/課題の7種(`TimeReportCriterion`)、列軸は年/月/週/日(`TimeReportPeriod`、週は`isoWeekYear`/`isoWeek`でISO週跨ぎの年境界を正しく扱う)。ステータス/バージョン/カテゴリ/トラッカーの4軸は`issues`テーブルへの`leftJoin`が必要(課題に紐付かない工数記録でも`(なし)`行として総計に含まれることを専用テストで確認済み — advisorレビューで「INNER JOINへの誤った簡略化に対する回帰検知が無い」と指摘され追加)。**Redmine本家からの意図的なスコープ縮小(3点、advisorレビューで指摘され本記載を追加)**: (1) カスタムフィールドを行軸として選択する機能は非対応(list/bool型カスタムフィールドをRedmineは動的にcriteriaへ追加するが、このアプリの他の簡略化パターン=受信メールキーワード解析のカスタムフィールド非対応と同じ判断)。(2) プロジェクト横断のレポート(Redmine本家の`project`criteria、プロジェクト非指定での`/time_entries/report`)は非対応、常に単一プロジェクトスコープ。(3) レポートのCSVエクスポート(`report_to_csv`)は非対応。**活動(activity)のCOALESCE省略について(既知の小さな乖離、advisorレビューで発見)**: Redmine本家は`COALESCE(activity.parent_id, activity.id)`で行をグループ化する(プロジェクト固有の上書き行とグローバルの親行が別々のactivity_idを持ちうるため、素のグループ化だと同一の論理的な作業分類が複数行に分裂しうる)。当初「`Project::activities()`が新規記録時の選択肢を常に1行に絞るためこの収束処理は不要」と判断したが、advisorレビューで「新規記録時の選択肢が1つに絞られることと、過去に記録済みのデータが1つに揃っていることは別問題」と指摘され、実際にテストで確認したところ本アプリはこのケースを収束させない: 同一の論理活動に対し、上書きが作られる**前**にグローバルの活動行へ記録された工数と、上書きが作られた**後**に上書き行へ記録された工数は、`activity`軸選択時に2行に分裂して表示される(`time_entries.activity_id`が記録時点で選ばれた具体的な行への確定的な外部キーであり、後から上書きが追加されてもさかのぼって書き換わらないため)。両方の工数は合計には正しく含まれる(データの欠落ではなく、表示上の分裂のみ)。この乖離は意図的な実装判断ではなく、`COALESCE`相当の収束処理を追加していないことによる既知の制約として記録する。**既知の制約(Redmine本家も同じ、意図的に踏襲)**: 期間列は最大100列(`TimeReportBuilder::MAX_PERIODS`、Redmine同様「100 columns max」)で打ち切るが、行合計・総計は打ち切られた期間も含めた全期間分を集計する(Redmine本家の`@total_hours = @hours.inject(0)`も同じく打ち切りを無視)ため、`day`粒度で100日を超えるプロジェクトでは表示列の合計と総計欄が一致しない場合がある。デフォルトの日付範囲フィルタも無いため、初回表示は最初の工数記録日から始まる(Redmine本家も同様、事前の期間フィルタなしでは同じ挙動)。テストは`tests/Feature/TimeEntries/TimeEntryReportTest.php`に12件(権限不足時の拒否、単一/複数行軸、leftJoinのNULL側、週/年の期間バケット化、3軸超過時の切り捨て=実際に`build()`まで到達させて確認、Own可視性ロールでの絞り込み、行軸未選択時の空表示、実HTTPリクエストでの描画確認、**上記activity COALESCE省略の乖離を裏付けるテスト**=上書き行(`parent_id`がグローバルの活動行を指す)と、その同じグローバルの活動行自体の両方に工数を記録し、`activity`軸選択時に2行に分裂すること(=収束されないこと)と、それでも合計には両方の工数が含まれることを確認、`issue`軸のラベル形式="#123 件名"の確認)。**advisorレビューで指摘・追加**: 当初は3軸超過時のテストが`selectedCriteria`の件数のみ確認して`report`自体を一度も構築しておらず、`activity`/`version`/`category`/`issue`の4criteriaが未実行のままdoneと記載しようとしていた(前述の再オープン主張と同型の「未検証の主張」問題)。**(2026-09-20、A8-04)**: 行の軸に工数/課題のカスタムフィールド(単一値で、リスト/真偽/列挙/整数/小数/日付の形式のみ。役割で見えないものは除く)を追加(`TimeReportAxes`/`TimeReportAxis`に一般化)、報告のCSV(`exportCsv()`、Shift_JIS/区切り文字選択可)、プロジェクト横断の`/time_entries/report`(`time-entries.global-report`、「プロジェクト」軸あり、閲覧できる工数だけを集計し、プロジェクトごとの`time_entries_visibility`を尊重)。**Redmineとの差分**: 文字列形式のカスタムフィールドは軸にできない(Redmineも同様)、ユーザー/バージョン形式は未実装のため軸にならない。テスト: `TimeReportExtrasTest.php` |
| プロジェクト横断の工数一覧 | done(2026-07-22) | トップレベル`/time_entries`(`time-entries.global-index`)を追加(Issues側の`issues.global-index`と同じ構成)。ヘッダーナビゲーションに「工数」リンクを配置。`view_time_entries`権限を持つ全プロジェクトを`news.global-index`と同じ「取得後にメモリ内でPolicy判定」方式で解決し、`TimeEntry::scopeVisibleToAcrossProjects()`(新規)がプロジェクトをAll/Own可視性ティア(工数の可視性はIssueと異なり2段階のみ)でバケット分け。フィルタ/グループ化/列選択は既存の`QueryFilterEngine`+`InteractsWithQueryFilters`+`<x-query-filter-builder>`を再利用、`TimeEntryFilterFieldRegistry::forProjects()`(新規)がプロジェクト列と担当者/作業分類の選択肢(可視プロジェクト全体の集合)を提供。編集/削除・CSVエクスポート・保存済みクエリは意図的に対象外(いずれもプロジェクト単位の前提に依存、Issues側と同じ判断)。**注**: 直下の「多次元工数レポート(ピボット表)」行とは別物 — この行は単純な横断一覧。~~ピボット表自体はプロジェクト単体でも未実装のまま~~ → 「多次元工数レポート(ピボット表)」行は`done(2026-07-30)`(2026-09-19訂正)。**行の編集/削除とCSVを追加(2026-09-20、A8-05)**: 各行に、そのプロジェクトで権限がある場合だけ「編集」(各プロジェクトの編集画面へ)「削除」(`deleteEntry()`が閲覧できる工数だけを対象に、工数ごとにポリシーで認可)を表示し、絞り込み後の一覧を選択列でCSV出力(UTF-8 BOM付き/Shift_JIS、区切り文字選択)。保存済みクエリは従来から対応。一括編集/コンテキストメニューはプロジェクト単位の一覧のみ。テスト: `GlobalTimeEntryActionsTest.php` |
| 工数フィルタ(ユーザー/種別/日付/時間) | done | — |
| 工数のCSVインポート | done(2026-07-22) | 課題CSVインポート(`IssueImport`/`ImportIssuesJob`)と同一パターンで`TimeEntryImport`/`ImportTimeEntriesJob`を新規実装。列マッピングは日付・時間(必須)・作業分類(名前で解決、既定の作業分類にフォールバック)・課題(`#番号`)・担当者(メールアドレス)・コメント。担当者列は`edit_time_entries`権限を持つインポート実行者のみ有効(Redmineの`TimeEntryImport#build_object`が`log_time_for_other_users`を要求する挙動を、本アプリの既存の「他人の代理記録」ゲート`edit_time_entries`(`time-entries/form.blade.php`の`canManageOthers`)で代替)。マッチしない/権限が無い場合は常にインポート実行者自身の記録になる。担当者メールは対象プロジェクトのメンバーに限定(`assigned_to`と同じスコープ保護)。ブラウザで実際にCSVアップロード→列マッピング自動検出→インポート→一覧反映まで確認済み |

### ダッシュボード横断機能

| 機能 | 状態 | 備考 |
|---|---|---|
| マイページ(ブロック追加/削除/ドラッグ並べ替え) | partial(2026-07-23) | 保存済みクエリのブロック化(Redmineのissuequery相当)を実装: 閲覧可能な課題クエリを「+ クエリ: {名前}」チップから追加でき、ブロックキー`issue_query:{id}`で複数クエリを並行配置可能(同一クエリは1ブロックまで — Redmineは同一クエリの重複配置も許すが意図的簡略化)。実行は`SavedIssueQueryBlock`が課題一覧と同じ`QueryFilterEngine`+ロール別課題可視性スコープで行い、クエリ削除/プロジェクトアクセス喪失時は空表示+フォールバックラベルに退化(クラッシュしない)。**documents/activityブロックを追加(2026-07-23)**: `DocumentsBlock`(`LatestNewsBlock`と同一パターン、所属プロジェクトの最新文書)、`ActivityBlock`(既存の`ActivityProviderRegistry`をプロジェクト横断で再利用 — 所属プロジェクトごと×プロバイダごとに`entries()`を呼び出し集約、直近7日分を新しい順に最大10件)。ブラウザで両ブロックの追加・表示を確認済み。calendarブロックは一覧形式のブロックインターフェースに馴染まないため意図的に対象外のまま。**ブロックを追加(2026-09-20、A9-03の一部)**: 「自分が更新した課題」(`UpdatedByMeBlock`、ジャーナルの投稿者基準、閲覧できる課題だけ)と「今週のカレンダー」(`CalendarBlock`、所属プロジェクトの未完了課題のうち開始日/期日が今日から7日以内のものを一覧形式で)。**未対応**: 同一クエリの複数配置(`max_occurs`)とブロックごとの設定(A9-03b)。テスト: `UpdatedByMeAndCalendarBlockTest.php` |
| グローバルアクティビティフィード | partial(2026-07-29) | 8種類のプロバイダを集約(日付範囲・種別チェックボックス)。`ActivityFeedController`+`feeds/activity-atom.blade.php`でプロジェクト単位のAtomフィードを追加(直近10日/最大15件、Redmineの`activity_days_default`/`feeds_limit`既定値を踏襲、既存の`ActivityProviderRegistry`をそのまま再利用)。**トップレベル`/activity`(`activity.global-index`)を追加(2026-07-29)**: Redmineの`ActivitiesController#index`が`@project`の有無だけで同一コントローラ/ビューを共用する設計(`/Users/sesoko/Desktop/workspace/redmine/app/controllers/activities_controller.rb`で確認)であることを踏まえ、`activity.index`と対になる独立コンポーネントとして追加(他のglobal-index群と同じ「対になる専用コンポーネントを並存させる」既存方針)。可視プロジェクトを`issues.global-index`等と同じ「取得後にメモリ内でPolicy判定」方式で解決し、各プロジェクトごとに8プロバイダを呼んでマージ・日付降順ソート(`ActivityProvider::entries()`が単一プロジェクト固定のインターフェースのため、他のglobal-indexが使う`scopeVisibleToAcrossProjects()`によるSQL単一クエリ化は適用不可)。**既知の制約(意図的、次点候補)**: ページネーション・件数上限が無く、コストは「可視プロジェクト数×8プロバイダ」で線形に増える(既定7日の日付範囲でのみ緩和)。将来対応する場合は8プロバイダすべてに複数プロジェクト対応の`entries()`オーバーロードを追加する設計が必要 — 今回は範囲外として見送り。**with_subprojectsトグルを追加(2026-07-29)**: `activity.index`(プロジェクト単位)側に「サブプロジェクトを含む」チェックボックスを追加、Redmineの`with_subprojects`パラメータ相当(既定値は本アプリに`display_subprojects_issues`設定が無いため`false`固定、Redmine本家は`Setting.display_subprojects_issues?`依存)。`search.index`の`searchableProjects()`と同じ`_lft`/`_rgt`範囲クエリ+`can('view', ...)`フィルタで実装、非公開/アーカイブ済み子孫プロジェクトからの漏洩がないことをテストで確認済み(アーカイブ済みは`ProjectPolicy::view()`が拒否するため追加ガード不要と検証)。グローバル側(`activity.global-index`)には同トグルを設けず(Redmineの`with_subprojects`もプロジェクト非バインド時は実質no-op、可視プロジェクトが元々全て含まれるため)。**注**: `activity.index`の`entries()`自体の内部実装をプロジェクト単一呼び出しから複数プロジェクト(`scopedProjects`)対応のflatMapへ変更 — 既存の`done`扱いだったコードパスへの改修であり、純粋な追加ではない点を記録。グローバルAtomフィード(`/activity.atom`相当の全プロジェクト版)は、他のglobal-index群(News/Issues/TimeEntries/Search/Calendar)もいずれも同種のAtom版を持たない既存の前例に合わせ、意図的に対象外のまま A7-11(2026-09-20): ファイル追加(プロジェクト/バージョンのFilesタブ、`view_files`、アップローダー付き)を`FileActivityProvider`として9種目に追加。マーカー`OffByDefault`(Wiki編集・フォーラム・工数)を付けたプロバイダは、プロジェクト/グローバルの活動画面で初期状態のチェックが外れる(Redmineの`default: false`)。Atomフィード・マイページ・プロフィールは従来どおり全種別。。**Atomと表示種別の記憶を追加(2026-09-20、A9-04の一部)**: 全プロジェクトのAtom(`/activity.atom`、`GlobalActivityFeedController`、Atomキー認証、`feeds_limit`と`activity_days_default`に従う)、個人設定`activity_scope`(活動ページで「適用」した種別を記憶し、プロジェクト/全体の活動ページとAtomの既定にする。存在しない種別は無視)。**未対応**: 日付単位のページングと件数上限、各プロバイダの複数プロジェクト対応(A9-04b)。テスト: `GlobalActivityAtomAndScopeTest.php` |
| **プロジェクト横断の課題一覧** | partial(2026-07-22) | トップレベル`/issues`(`issues.global-index`)を追加、ヘッダーナビゲーションに「課題」リンクを配置。`view_issues`権限を持つ全プロジェクトを`news.global-index`と同じ「取得後にメモリ内でPolicy判定」方式で解決し、`Issue::scopeVisibleToAcrossProjects()`(新規)がプロジェクトをAll/Default/Own可視性ティアごとにバケット分けして単一クエリのWHERE句として組み立て(1プロジェクトで「全件」・別プロジェクトで「自分の課題のみ」が同時に成立するケースに対応)、`LengthAwarePaginator`によるSQLレベルのページネーションを維持。フィルタ/ソートは既存の`QueryFilterEngine`を再利用し、`IssueFilterFieldRegistry::forProjects()`(新規)がトラッカー/カテゴリ/担当者/対象バージョンの選択肢を可視プロジェクト全体の集合として構築(ステータス/優先度は元々グローバル)。新規`project_id`フィルタ列も追加。スコープを絞るため、一括編集/移動/コピー/削除・CSVエクスポート・保存済みクエリ・複数レベルソート(課題一覧本体は3階層だがこちらは1階層のみ)は意図的に対象外 — いずれもプロジェクト単位の前提(メンバー/バージョン/トラッカーの検証)に強く依存するため、必要になった時点で別途well-scopedな項目として着手 |
| カレンダー | partial(2026-07-22) | 開始日/期日マーカーを実装: Redmineのカレンダーヘルパーと同様、課題を開始日(▶)と期日(◀)の2箇所にマーク表示(期間中の全日には展開しない)。開始日=期日の課題は◆1件に集約、片方の日付のみの課題はその日付にのみ表示。**さらに(2026-07-22)**: トップレベル`/issues/calendar`(`calendar.global-index`)を追加(Redmineの`IssuesController#calendar`相当)。ヘッダーナビゲーションに「カレンダー」リンクを配置。`Issue::scopeVisibleToAcrossProjects()`をそのまま再利用でき、`weeks()`のグリッド構築ロジックは無変更。1点だけ新規UI追加が必要だった: 複数プロジェクトの課題が同じ日に混在するため、各エントリにプロジェクト識別子のプレフィックスを付与(件数キャップは実装せず、1日あたりのエントリ数が多い場合の視覚的な崩れは既存のプロジェクト単体版と同様に許容)。**クエリフィルタとの連動(2026-07-22)**: Ganttで先に実装したのと同じパターン(`InteractsWithQueryFilters`トレイト+`IssueFilterFieldRegistry`+`QueryFilterEngine`+`<x-query-filter-builder>`)を`calendar.index`/`calendar.global-index`の両方に配線。Ganttのツリー構造とは異なりカレンダーは階層を持たない日付グリッドのため、Gantt特有の「フィルタに一致しない祖先課題も深さの整合性のため表示に残す」特殊処理は不要で、マッチした課題IDセットをそのまま`WHERE`条件として適用するだけで済んだ。ブラウザで実際にステータスフィルタを適用し、一致しない課題がカレンダーから消えることを確認済み **バージョンの期日を表示(2026-09-20)**: Redmineの`calendars_controller`が`@query.versions`(`effective_date`が範囲内)をマージするのに合わせ、プロジェクト内カレンダーは当プロジェクトのバージョン、全プロジェクトカレンダーは`view_calendar`のあるプロジェクトのバージョンを期日に📦で表示(ロードマップの該当バージョンへリンク、`roadmap-version-{id}`アンカーを追加)。**未対応**: 他プロジェクトから共有されたバージョン(ガントと同じ意図的スコープ)。テスト: `CalendarTest.php`・`GlobalCalendarTest.php` |
| ガント | partial(2026-07-23) | クエリフィルタ連動を実装: 課題一覧と同じ`QueryFilterEngine`+`IssueFilterFieldRegistry`のフィルタビルダー(共通Bladeコンポーネント`<x-query-filter-builder>`に抽出、課題一覧/工数一覧も同コンポーネントへ移行)をガントに配線。フィルタ一致IDをEloquent側で解決し、`GanttService::issueTree()`が一致課題+その祖先(深さ表示の整合性のため保持)にツリーを絞り込む。CTE自体は無変更。**バージョンのマイルストーン表示(2026-07-23追加)**: プロジェクト自身の`due_date`が設定されたバージョンを◆マーカーとして日付軸上に表示(`Version::completedPercent()`を再利用してラベルに進捗率も表示)。マイルストーンの期日が全課題の期日より後ろにある場合はチャートの日付範囲(`rangeEnd`)を自動的に延長。他プロジェクトから共有されたバージョンは対象外(このプロジェクト自身のバージョンのみ、意図的なスコープ限定)。**別件で発見(未修正)**: 検証中、既存の課題バー(`bg-indigo-400`/`bg-gray-400`)が実際には描画されない(背景色が透過)ことを発見 — Tailwindの特定シェード(400番台等)がコンパイル済みCSSに含まれておらず、かつこの環境では`npm run build`自体が`rolldown`のネイティブバインディング欠落で失敗する状態。本機能の実装自体(位置計算・データ取得)には影響しない(マーカーの図形・ラベル・進捗率は正しい位置に表示されることをブラウザで確認済み、色のみ未適用)ため、アセットビルド環境の修復は別件として対象外とした。**関連線を追加(2026-07-23)**: Redmineの`Redmine::Helpers::Gantt::DRAW_TYPES`相当を実装、`precedes`(青 `#228be6`)/`blocks`(赤 `#fa5252`)の2種別のみ線を描画(Redmine本家も他の関連タイプは描画しない)。`GanttService::relationsWithin()`が現在チャートに表示中の課題ID集合の中に両端が収まる関連のみ抽出(フィルタ適用中はフィルタ後の可視行に自動追従)、両端の課題がともに日付範囲を持つ場合のみ線を描画(片方でも未設定なら描画しない)。行の高さが固定32pxである前提でY座標をpx計算、X座標はバー左右端の%をそのままSVGの`x1`/`x2`属性に渡す(パーセントとpxの混在座標はSVGが軸ごとに独立解決するため問題なし)。線はインラインSVGの`stroke`属性で色指定しているため、既知の未修正Tailwindビルド問題(直上の記載)の影響を受けない(ブラウザで実際に色付きで描画されることを確認済み)。PDF/PNGエクスポート・プロジェクト横断ガントは引き続き未対応 A5-15(2026-09-20): `gantt_items_limit`(既定500、0で無制限。超過は先頭N件のみ描画して注記)・`gantt_months_limit`(既定24、0で無制限。チャートは最初の開始日からN か月までで、はみ出す棒はクリップして注記)を設定に追加。
| 検索(モジュール横断) | partial(2026-07-22) | all_words(既定オン、全単語AND/任意単語OR)・titles_only(タイトル/件名のみ)・open_issues(オープン課題のみ、他タイプは影響なし)のトグルと、`#123`(`#`省略可)での課題直接ジャンプ(存在しない/閲覧不可の場合は通常検索にフォールバックし他プロジェクトのID存在を漏らさない)を実装。`SearchService`はScout経由から直接クエリへ移行 — 単語単位のAND/OR・タイトル限定はScoutのdatabaseエンジン(単一文字列LIKE)では表現できないため。`Searchable`トレイトは将来の検索エンジン切替の契約として維持。**さらに(2026-07-22)**: トップレベル`/search`(`search.global-index`)を追加し、Redmine本来の「検索は既定でサイト全体、プロジェクト内では絞り込みが狭くなる」挙動に合わせた(従来の`/projects/{project}/search`はプロジェクト内限定版としてそのまま維持)。`SearchService::search()`を内部的に`searchAcrossProjects(Collection $projects, ...)`(新規の複数プロジェクト対応版)に委譲するよう再実装し、既存の単一プロジェクト呼び出し側・テストは無変更で全て通過することを確認。プロジェクト一覧の解決は他のグローバル一覧と同じ「取得後にメモリ内でPolicy判定」方式。**チェンジセット検索を追加(2026-07-23)**: `searchChangesets()`を既存の`searchIssues()`等と同一パターンで実装(`view_changesets`権限でプロジェクトを絞り込み、コミットメッセージ列`comments`をLIKE検索)。Redmineの`acts_as_searchable :columns => 'comments'`(単一列)を踏襲し、`titles_only`モードは実質no-op(Redmine本家の`acts_as_searchable`実装も先頭列のみを使うため、列が1つしかないこのタイプでは通常検索と同じ挙動になる、という点まで再現)。`repository_id`経由でプロジェクトに紐づくため`whereHas('repository', ...)`でスコープ。**プロジェクト検索を追加(2026-07-23)**: `searchProjects()`を実装(`name`/`identifier`/`description`をLIKE検索)。他タイプと異なりタイプ別の追加権限チェックは不要 — 呼び出し元がすでに「閲覧可能なプロジェクト集合」を渡しているため、そのままフィルタとして使うだけでよい(Redmine本家の`Project.acts_as_searchable`も`:view_permission`を指定していない点と一致)。**調査の結果、Journalの検索対象化は対象外と判断**: Redmineの`acts_as_searchable`プラグインには`search_journals`オプションが実装されているものの、本家Redmineの現行コードではIssue/どのモデルにも`:search_journals => true`が実際には設定されておらず、Journal本文はRedmine本家の検索でも一切対象になっていないことをソース調査で確認(死んでいる設定オプション)。したがって「Journal検索が無い」のはこのアプリのギャップではなく、Redmine自体の実際の挙動と一致した状態であり、チェックリストの記載を訂正。**プロジェクト絞り込みトグルを追加(2026-07-23)**: Redmineの`SearchController#index`の`params[:scope]`(`all`/`my_projects`/`subprojects`、`bookmarks`は**検索スコープとしては未配線**、2026-09-19訂正: `ProjectBookmark`/`project_bookmarks`(2026-07-21)と`projects/index.blade.php`の`bookmarkedOnly`は既に存在、バックログA9-06)を実装。プロジェクト内検索(`search.index`)に「サブプロジェクトを含む」トグルを追加、オンの場合`_lft`/`_rgt`範囲(kalnoyのクエリ拡張ではなく`Project::rootProject()`と同じ素のEloquentビルダーで比較)でこのプロジェクト+子孫全体をSearchServiceに渡す(既存の`search()`単一プロジェクト呼び出しから`searchAcrossProjects()`への切り替え)。全体検索(`search.global-index`)には「自分のプロジェクトのみ」トグルを追加、オンの場合`visibleProjects()`(閲覧可能な全プロジェクト)を`auth()->user()->projects()`(実際にメンバーになっているプロジェクト)で絞り込む。`#123`直接ジャンプの挙動(プロジェクト内検索は自プロジェクトのみを対象に既存動作のまま)は今回のトグルと独立、変更なし A7-12(2026-09-20): 添付ファイル検索(`attachments` = 0/1/only)を追加。`AttachmentSearchMode`+`SearchService`が課題・Wiki・お知らせ・文書・フォーラムの添付のファイル名/説明を検索(Redmineの`acts_as_searchable`と同じ規則: titles_only時は`only`のみ、`only`ではプロジェクト/リビジョンを除外)。所有レコードの可視性で絞り込み。検索画面のラジオ・APIの`attachments`パラメータ対応。 A9-06(2026-09-20): `bookmarks`スコープ(ブックマークしたプロジェクトのみ)を追加。グローバル検索は`bookmarkedOnly`チェックボックス、APIは`scope=bookmarks`。ページ分割はA2-07、添付検索はA7-12で対応済み。 |

---

## 5. リポジトリ(SCM)・REST API・拡張性

### リポジトリ連携

| 機能 | 状態 | 備考 |
|---|---|---|
| リポジトリブラウズ(指定リビジョンでのツリー表示) | done | `GitAdapter`/`SvnAdapter` |
| 対応SCM種別 | partial | Git・SVNのみ。Redmine は Mercurial/Bazaar/CVS/Filesystem等も対応(4種欠落) |
| チェンジセット一覧・単体表示 | done | `RepositorySyncService`, `Changeset` |
| Diff表示 | done(2026-07-23) | 任意リビジョン間Diffを実装(2026-07-22): `ScmAdapter::diff()`に`$fromRevision`を追加(Git=`git diff from to`、SVN=`svn diff -r from:to`)、リビジョン一覧に旧/新ラジオ+「選択したリビジョンを比較」ボタン、新規`repository.compare`ルート。端点はコミット日時で正規化(古い方が常にdiffベース、Wiki差分と同じ規約)、両端不変のため差分は無期限キャッシュ。**単一ファイルへのdiffスコープ限定を追加(2026-07-23)**: `ScmAdapter::diff()`に第3引数`?string $path`を追加(Git=`git show`/`git diff`に`-- path`を付与、SVN=diff対象を`{リポジトリURL}`から`{リポジトリURL}/{path}`に変更)。`repository.show`に`#[Url] $path`を追加し、指定時はキャッシュキー・adapter呼び出しの両方に反映、diff見出しに対象ファイル名+「全体の差分を見る」リンクを表示 |
| 単一ファイルの変更履歴 | done(2026-07-23) | Redmineの`RepositoriesController#changes`相当。新規`repository.file-history`ルート(`browse_repository`権限)で、そのパスを変更したすべてのChangeset(同期済みの`ChangesetFile`から取得、`ScmAdapter`への追加アクセス不要)をコミット日時降順で一覧表示、各行から`repository.show`(該当コミットのdiff)へリンク。ファイル一覧・ファイル詳細画面の双方に「履歴」リンクを追加。**単一ファイルdiffエンドポイントを追加したため、各行のリンクは該当ファイルにスコープされたdiffを直接指すよう更新(2026-07-23)**、上の「Diff表示」行を参照。**リネーム追跡を追加(2026-07-24)**: `git log --follow`相当。`GitAdapter::log()`に`-M`(リネーム検出)を追加し、`R<類似度>\t旧パス\t新パス`形式の3列出力から旧パスを取得(従来は新パスのみ保持し旧パスを破棄していた)。SVN側は`svn log --xml --verbose`が元々返す`copyfrom-path`属性を読み取るよう変更(移動はSVN上「削除+`copyfrom-path`付き追加」で表現される)。`changeset_files`に`from_path`列を追加、`repository.file-history`の一覧クエリを単一パスの完全一致から「新しい変更から遡り、`from_path`を検出したら以降はそのパスに切り替えて追跡を続ける」チェーン走査に変更(各行は当時のパスでdiffへリンク、リネーム前の行には旧パス名を注記表示) |
| Annotate/Blame | done(2026-07-22) | `ScmAdapter`に`blame(revision, path): ScmBlameLine[]`を追加。Git実装は`git blame --line-porcelain`(全行についてコミットヘッダをフル反復出力する形式のため、直前コミットの状態を覚えておくパーサ状態機械が不要)。Svn実装は`svn blame --xml`(行内容を含まない)で取得したリビジョン/著者列を、別途`fileContentAt()`で取得した内容行とインデックスで結合(XML側は改行有無に関わらず実際の行数のみを返すため、`fileContentAt()`側の末尾改行による余分な空要素を除去してから結合)。新規`repository.annotate`ルート+Volt画面で行番号/リビジョン(先頭8文字)/著者/内容を表形式表示、`repository.entry`にバイナリファイルを除き「注釈」リンクを追加。~~Redmineの色分けブロック表示(同一リビジョンの連続行をまとめて色付け)は対象外、全行に個別表示~~ → **色分けブロックを実装(2026-09-20)**: `BlameBlocks::annotate()`が同一リビジョンの連続行を1ブロックとして扱い、リビジョン/作者はブロック先頭行のみ表示、リビジョンごとに出現順で12色(`k.size % 12`、戻ってきたリビジョンは同色)、別ブロックに続く先頭行に境界線(Redmineの`bloc-change`)、各行に`L{n}`アンカー。テスト: `BlameBlocksTest.php`・`RepositoryBrowseTest.php`。**要ビルド**: 新しいTailwindクラス(`bg-*-50`)を使うため`npm run build`が必要 |
| 生ファイルダウンロード | done(2026-07-22) | 新規`RepositoryRawController`(`GET /projects/{project}/repository/raw/{path}`)を追加。`browse_repository`権限で認可後、`ScmAdapter::fileContentAt('HEAD', $path)`の生バイト列を`finfo_buffer`によるMIMEタイプ判定+`Content-Disposition: attachment; filename="..."`付きで返却(既存の`AttachmentController`と同じ強制ダウンロード方式)。テキスト/バイナリを問わず動作(既存の`repository.entry`はUTF-8として解釈できないバイナリを一切表示できなかったが、これはバイト列をそのまま返すだけなので影響を受けない)。単一ファイル表示画面(`repository/entry.blade.php`)に「ダウンロード」リンクを追加(Redmineの`entry.html.erb`の`@raw_url`ダウンロードリンクに相当) |
| リポジトリ統計・コミットグラフ | done(2026-07-22) | `/projects/{project}/repository/stats`(`repository.stats`)を新規追加。作成者別・月別のコミット数を`Changeset`の`committer`/`committed_on`から集計(DB非依存にするためPHP側のCollectionでグループ化、DB固有の日付関数SQLは使わない)。作成者はRedmineと同様、User解決はせず生の`committer`文字列(`Name <email>`形式)でグルーピング。可視化は`versions/roadmap.blade.php`/`gantt/index.blade.php`と同じCSS幅バー方式を流用(新規チャートライブラリなし)。`repository.index`にリンクを追加。ブラウザで実際にコミット数の多い作成者が上位に表示され、バー幅が件数比で正しく描画されることを確認済み |
| プロジェクトあたり複数リポジトリ | **done**(2026-07-30、スライス1〜2b後半まで全て完了) | Redmine本家の`Project`(project.rb)は`has_one :repository, -> { where(is_default: true) }` + `has_many :repositories`という構造(=専用の1:1テーブルではなく、1:多テーブルに`is_default`フラグで「既定リポジトリ」を表現)。**スライス1(スキーマ+モデル)**: `repositories.project_id`の`unique()`制約を撤去し1:多に変更、`is_default`列を追加(新規マイグレーション、事前データは全行`is_default=true`にバックフィル — Redmineの`20120115143100_add_is_default_to_repositories`相当)。`Repository`モデルに`saving`/`created`/`updated`フックを追加(単一の真実源): `saving`で新規作成時に対象プロジェクトの既存リポジトリが0件なら自動的に既定リポジトリに設定(Redmineの`set_as_default?`と同じ判定)。`is_default=true`で保存されたリポジトリが同一プロジェクトの他リポジトリの`is_default`を自動でfalseに戻すスイープ(Redmineの`check_default`相当)は、`created`(新規作成時は無条件でスイープ)と`updated`(`wasChanged('is_default')`のときのみスイープ)の2つのリスナーに分割 — Eloquentは`created`/`saved`イベント発火前にoriginal状態を同期し直すため、挿入直後は`wasChanged('is_default')`が常にfalseを返してしまい(実際にtinkerで確認済み)、`saved`一本+`wasChanged`ガードという当初案では新規作成時のスイープ自体が動かなくなる(既存の他リポジトリが正しくfalseに落ちない)。更新時は本来のoriginal値が保持されるため`wasChanged`が正しく機能する。`Project::repository()`を`hasOne(...)->where('is_default', true)`に変更(既存の全画面/ジョブが`$project->repository`で参照する既定リポジトリの解決先は変わらない)、新規`Project::repositories(): HasMany`を追加。既存の10画面(Volt)・`RepositoryRawController`・`RepositorySyncService`・`RepositoryPolicy`は無改修(`AutofetchRepositoryChangesetsJob`は元々`Repository::query()`で全リポジトリを走査する設計だったため、複数リポジトリが存在しても同期は最初から正しく機能する)。マイグレーションのバックフィルはRefreshDatabaseだと空のテーブルに対してしか実行されず検証にならないため、マイグレーションの`up()`/`down()`を直接呼び出し既存データを模したシナリオでのテストを追加(`tests/Feature/Repository/RepositoryDefaultTest.php`)。**advisorレビューで発見・修正したバグ(2段階)**: (1)当初`saved`フックが`is_default`カラムが実際に変更されたかを見ずに無条件でスイープを実行していたため、`RepositorySyncService::sync()`が同期対象のChangesetごとに呼ぶ`$repository->update(['last_synced_revision' => ...])`(is_defaultとは無関係な属性更新)のたびに毎回このスイープが走ってしまっていた(Redmine本家の`check_default`は`is_default_changed?`でこれを防止済み、repository.rb:517)。最終的な`is_default`値は変わらないため、この種のバグは値だけをアサートするテストでは検出できず、クエリログの内容を直接アサートするテストを追加して再現・検出できることを確認済み。(2)その修正として単純に`saved`フックに`wasChanged('is_default')`ガードを追加したところ、上記の理由(挿入直後は`wasChanged`が常にfalse)により新規作成時のスイープが完全に無効化され、既存のテスト(2件)が実際に失敗して発覚 — `created`/`updated`の2リスナーに分割することで解決。**スライス2a(2026-07-30、identifier列+解決ヘルパーのみ、ルーティング/UI変更なし)**: `repositories`テーブルに`identifier`列(nullable、`project_id`との複合ユニーク制約——Redmine本家(repository.rb:46)と同じく識別子はプロジェクト単位のスコープでのみ一意、グローバル一意ではない。PostgreSQLは複合ユニーク制約内のNULLを別値として扱うため、識別子未設定のリポジトリが同一プロジェクト内に複数存在しても衝突しない、というのが移行直後の全行の状態でもあり、テストで確認済み)。`Repository::identifierParam()`(識別子があればそれ、無ければid文字列、repository.rb:135相当)と`Repository::scopeWhereIdentifierParam()`(数字のみのパラメータはid解決、それ以外はidentifier解決、`find_by_identifier_param`repository.rb:151-157相当)を追加。「一度設定した識別子は変更不可」という不変条件(repository.rb:126-132の`identifier=`オーバーライド相当)は`saving`フックに実装(単一の真実源、Redmineが検証エラーではなく書き込みそのものを黙って無視する挙動もそのまま踏襲)。**あえてこのスライスではしなかったこと**: フォーム側の書式バリデーション(小文字英数字+ハイフン/アンダースコアのみ・純数字禁止・予約語除外)とUIフィールド自体の追加はスライス2bに送った——ルーティングが`identifier`を消費するようになるまでは、値を入力できても何も変わらない「飾りの入力欄」になってしまうため(スライス1で`identifier`列自体を先送りしたのと同じ理由)。**スライス2b前半(2026-07-30、リゾルバ+呼び出し元の統一+`route()`呼び出しの堅牢化。ルーティング自体は変更なし)**: 新規`Project::resolveRepository(?string $repositoryIdentifier): ?Repository`(`find_project_repository`相当 — 引数ありなら`repositories()->whereIdentifierParam()`で解決、引数なしなら既定→先頭リポジトリにフォールバック)を追加し、10画面(Volt)全部+`RepositoryRawController`の`$project->repository`直接参照をこれ経由に統一(各`mount()`は`?string $repositoryParam = null`引数を新設(`RepositoryRawController::__invoke()`は後のスライス2b後半で`Illuminate\Http\Request $request`経由の読み取りに変更済み、下記参照)、現状どのルートもこの引数を渡さないため常にnullで既定リポジトリに解決される=既存の単一リポジトリ挙動と完全に同じ結果になることをテストで確認済み。**この引数は現時点で到達不能だが、意図的に残してある**——スライス2b後半でルーティングが実際にidentifierを渡すようになった時、`mount()`側の変更なしにそのまま機能するようにするため)。**副産物として発見した既存のバグ予備軍**: `route('repository.show', [$project, $changeset])`のような複数値の配列渡しは、Laravelの`route()`ヘルパーがURI内のパラメータ出現順に位置引数を割り当てるため、ルートに新しいセグメントが増えると値のズレが起きる(下記の失敗した試行で実際に発生・検出)。**このスライスで恒久対応**: 全`route('repository.*', ...)`呼び出し(本体10箇所+テスト5箇所)を`['project' => ..., 'changeset' => ...]`等の名前付き配列に書き換え済み——現在のルート形状でも将来ルートにセグメントが増えても壊れない書き方になった。**失敗した試行とそこから得た確定事実(重要、次回はここから再開しないこと)**: 当初`{repositoryParam?}`という「任意セグメントを動詞の前に挿入」ルーティング(`/repository/{repositoryParam?}/browse/...`)を試みたが、実際にHTTPリクエストを投げてテストしたところ全滅した。原因をコンパイル済み正規表現を直接ダンプして確認したところ、`(?P<repositoryParam>[^/]++)`(オプショナルを示す`?`が全く反映されていない、必須キャプチャグループ)になっていた——**Laravelのルートパラメータは末尾に置かれた場合のみ本当にオプショナルになり、後ろに固定リテラル(動詞など)が続く中間セグメントの`{param?}`は`?`が実質無視されて必須になる**、という文書化されていないフレームワークの制約が実機検証で判明。ルート自体はスライス1着手前の形(識別子セグメントなし)に完全に戻した。**スライス2b後半(未着手、次点候補)向けの正しい実装方針**: 中間の任意セグメントは使わず、アクションごとに「識別子なしの既存ルート」と「識別子ありの別名ルート」を2本登録し、両方を同じVoltコンポーネント/コントローラに向ける方式にする(例: `repository.browse`(既存URL、識別子なし)+`repository.browse.repo`(`/repository/{repositoryParam}/browse/...`、識別子必須)の2ルート、`mount()`は共通のまま)。識別子ありルートは識別子なしルートの**後**に登録すること(そうしないと`/repository/browse/...`が`repository.browse.repo`の`{repositoryParam}=browse`解釈に飲み込まれる)。フォームの識別子入力欄+書式バリデーション(予約語は本アプリの実際のルート末尾セグメント`edit`/`committers`/`stats`/`compare`/`revisions`/`browse`/`entry`/`annotate`/`history`/`raw`を採用予定——Redmine本家の予約語リストとは対象ルート名が異なるため単純流用不可)、複数リポジトリが存在する場合の画面ごとの切替UI(どちらのルート名を使うかを選ぶロジックが新たに必要——既定リポジトリなら識別子なしルート、そうでなければ識別子ありルート、といった振り分けを想定)、リポジトリ作成/既定変更フォームのUIも未着手のまま。**フォーム実装時の注意点**: `''`(空文字)は複合ユニーク制約`(project_id, identifier)`上ではNULLと異なり「実際の値」として扱われるため、同一プロジェクト内で識別子未入力のリポジトリを2つ以上保存すると空文字同士が衝突し`QueryException`になる——フォームの保存処理で空文字を`null`に正規化してから保存すること(advisorレビューで指摘、2a実装時に`identifier_frozen?`のblank判定を確認する過程で判明)。これらが揃うまでは「2つ目のリポジトリを追加する」UI自体が存在しないため、実質的にはスライス1+2a+2b前半単体では挙動は変化しない(既存の単一リポジトリ運用と完全互換、`resolveRepository()`と`whereIdentifierParam()`自体はテストで直接検証済み)。**スライス2b後半・ルーティング部分(2026-07-30)**: 直前段落の実装方針どおり、`routes/web.php`に既存11ルート(index/edit/committers/stats/compare/show/browse/entry/annotate/file-history/raw)それぞれの識別子ありの別名ルートを追加(ルート名は`repository.index.repo`のように元の名前へ`.repo`を付与、URLは`/repository/{repositoryParam}/...`を`/repository`の直後に挿入)。**登録順序**: 識別子なしルート全体(既存のブロック)を先に、識別子ありルート全体を後に登録(コメントに残していた既知の注意点どおり)。識別子ありルートの内部でも一応`repository.index.repo`(`/repository/{repositoryParam}`、1セグメントのみの汎用パターン)を他の`.repo`ルートより後ろに置いたが、検証の結果`{repositoryParam}`はLaravelのデフォルト正規表現でスラッシュを跨がずセグメント単位でしかマッチしないため、他の`.repo`ルート(いずれも2セグメント以上)と実際には衝突しない——念のための予防的な順序付け。**URL生成の一本化**: 新規`Repository::routeName(string $baseName): string`(既定リポジトリならそのまま、非既定なら`.repo`サフィックス付き)と`Repository::routeParameters(array $extra = []): array`(非既定の場合のみ`repositoryParam`キーを追加)を追加し、10画面(Volt)+`ChangesetActivityProvider`+`SearchService`の`route('repository.*', ...)`呼び出しを全て`route($repository->routeName(...), $repository->routeParameters([...]))`形式に置換(`$repository`が存在しないコンテキストでは`$changeset->repository`から解決)——これにより非既定リポジトリを閲覧中に生成される内部リンク(パンくず・ファイル一覧・注釈・履歴・ダウンロード・diff・コミッター設定・統計等)はすべて`.repo`ルートを指し、遷移後も同じ非既定リポジトリの文脈を維持する(既定リポジトリの場合は従来どおり識別子なしルートを指すため、単一リポジトリ運用時のURLは一切変化しない)。**このスライスでやらなかったこと**: 複数リポジトリが存在する場合の画面ごとの切替UI(プルダウン等)とリポジトリ作成/既定変更フォームのUIは未着手のまま次点(いずれも現状は`repository/{project}?repositoryParam=xxx`をブラウザのアドレスバーに直接入力しないと2つ目以降のリポジトリへ到達できない——`repository/form.blade.php`は既存1件の編集のみに対応し新規作成UIも無いため、実運用上は本スライスの効果はまだ表に出ない)。テストは`tests/Feature/Repository/RepositoryMultiRepoRoutingTest.php`に13件新規追加(うち1件は`{repositoryParam}`+複数セグメントの`{path}`という、下記の束縛バグと同じ形の組み合わせをネストしたファイルパスで再確認する回帰テスト、advisorの2回目レビューで追加指示)(`routeName`/`routeParameters`のユニット的な検証、識別子なし/ありルートそれぞれの解決、未知のrepositoryParamで404、数値repositoryParamでのid解決、非既定リポジトリのbrowse画面が生成する実際のリンクHTMLの検証+そのリンクを実際にたどって同じリポジトリのファイルが読めることを確認するE2E、index画面の各アクションボタンのリンク検証)。**advisorレビューで発見・修正した3件(2026-07-30)**: (1)上記9件は全て`Livewire::test('repository.browse', ['repositoryParam' => ...])`のようにVoltコンポーネントのテストヘルパー経由で書かれており、これは`mount()`にルートパラメータを直接注入するためルーティング自体(`routes/web.php`の実際のURLパターンやパラメータ解決)を一切経由しない——「ルーティングがテーマの機能なのにルーティングを検証していない」という指摘。実際に`$this->get(route('repository.browse.repo', ...))`のような本物のHTTP GETを4パターン分(browse.repo・index.repo・show.repo・raw.repo、それぞれ形の異なるルートパターンを代表)追加したところ、**実際に2件のバグが発覚**: ①`repository/browse.blade.php`の`mount(Project $project, string $path = '', ...)`は`{path?}`セグメントが省略されたとき、LaravelがPHP側のデフォルト値`''`を使わせず明示的に`null`を渡すため`TypeError`になっていた(Livewireコンポーネントテストは常に`path`を明示的に渡していたため今まで検出されなかった)——`?string $path = null`に変更し本体側で`$path ?? ''`に正規化して解決。②`RepositoryRawController`(唯一Voltでない、素の invokable コントローラ)は`repository.raw.repo`(`{repositoryParam}`が実在するURL)への実リクエストが原因不明のルーティング404になっていた——`Route::getRoutes()->match()`によるルート照合自体は成功するのに実際のHTTPディスパッチだけ失敗するという再現性のある挙動を実機で確認した上、コンストラクタ引数の宣言順序(`$project, $path, $repositoryParam`)をURLのセグメント順序(`{project}/{repositoryParam}/raw/{path}`)に合わせて並べ替えると`repository.raw.repo`は直るが今度は`repository.raw`(identifier無し、`{repositoryParam}`セグメント自体が存在しない)側の`$path`束縛が壊れるという二律背反が発生——最終的に型付き引数への自動束縛に頼るのをやめ、`Illuminate\Http\Request $request`を受け取り`$request->route('repositoryParam')`/`$request->route('path')`で明示的に読む方式に変更して両ルートとも解決(Voltの`mount()`は同じ状況でも名前ベースの束縛が正しく機能しており影響を受けなかった——影響は素のControllerルートのみ)。(2)`ChangesetActivityProvider::entries()`が新たに`$changeset->repository->routeName()`/`routeParameters()`(`routeParameters()`内部で`$this->project`にもアクセス)を呼ぶようになったことで、`Changeset::query()->get()`にEagerロードが無いままエントリごとに`repository`+`repository.project`の追加クエリが発生するN+1が新規混入していた(修正前のコードは`route('repository.show', [$project, $changeset])`で`$changeset->repository`に一切触れていなかったため発生していなかった)——`->with('repository.project')`を追加して解消(`SearchService`側は元々`->load('repository.project')`済みで無傷)。合わせて`repository/show.blade.php`の`mount()`にも`repository.project`のeagerロードを追加(同じ理由での予防的対応)。**次のイテレーション(切替UI+作成/既定変更フォーム)への申し送り**: `identifier`の書式バリデーション(予約語・純数字禁止)は現状未実装のまま——`identifier`が`edit`/`stats`/`compare`等の予約語と一致すると`route('repository.index.repo', ['repositoryParam' => 'edit'])`が`/repository/edit`というURLを生成し、識別子なしの`repository.edit`(設定フォーム)ルートに先に一致してしまうため、閲覧しようとしたリポジトリではなく設定画面に飲み込まれる——現状はフォームに識別子入力欄自体が無いため潜在化しているだけで、次のイテレーションでフォームを実装する**同じイテレーション内**でこのバリデーションも実装must。またRepositoryモデルの`saving`フックにより識別子は一度設定すると変更不可(かつ検証エラーではなく黙って無視される)ため、フォームは識別子が既に設定されている場合は入力欄を編集不可/非表示にすること(Redmineの`identifier_frozen?`と同じ判定)。**スライス2b後半・UI部分(2026-07-30、これでスライス2b後半=このセクション全体が完了)**: 直前段落の申し送り事項どおりに実装。①**識別子の書式バリデーション**: `repository/form.blade.php`に`identifier`の検証ルール(`regex:/^[a-z0-9_-]+$/`・純数字拒否・予約語拒否——予約語リストは本アプリの実際のルート末尾セグメント`new`/`edit`/`committers`/`stats`/`compare`/`revisions`/`browse`/`entry`/`annotate`/`history`/`raw`の11個、`new`は本イテレーションで新設した`/repository/new`ルート自身の予約——+同一プロジェクト内でのユニーク検証)を追加。既存リポジトリの識別子が既に設定済みの場合は入力欄自体を出さず読み取り専用のプレーンテキスト表示に切り替え(`identifierEditable()`ヘルパーで判定、識別子がnull/空文字の場合のみ入力欄を表示)。空文字入力は保存前に`null`へ正規化(複合ユニーク制約上、空文字はNULLと異なる実値として衝突するため)。②**リポジトリ作成フォーム**: 新規ルート`/projects/{project}/repository/new`(`repository.create`)を追加、既存の`repository.form`コンポーネントを共用しつつ、ルートの`->defaults('isNew', true)`(Laravelのルートパラメータデフォルト値機構)経由で「既存リポジトリの編集ではなく新規追加」であることをmount()に伝える——`request()->routeIs()`でルート名を判定する案は却下した(Livewire::test()はmount()にルートパラメータを直接注入するため実際のルートマッチを経由せず、`routeIs()`ベースの判定はコンポーネントテストで一切シミュレートできない、直前のイテレーションで学んだ教訓の直接適用)。`isNew`時は既存の`resolveRepository()`を呼ばず`$this->repository = null`に固定するため、プロジェクトに既に既定リポジトリがあっても2件目以降を素直に新規作成できる(Repositoryモデルの`saving`フックが「既存リポジトリ0件のときだけ自動的に既定にする」判定を持つため、2件目以降は自動的に非既定として作成され、追加のロジック不要)。保存後は新規作成したリポジトリ自身の`routeName()`/`routeParameters()`でリダイレクト(既定リポジトリ作成時のみ暗黙に`repository.index`と同じ結果になる)。③**画面ごとの切替UI**: `repository/index.blade.php`に`allRepositories()`(`$project->repositories()`を`is_default`降順→`identifier`昇順)を追加、プロジェクトのリポジトリが2件以上のときのみ一覧テーブルを表示(1件だけの既存運用ではUIが増えない)。各行は`identifierParam()`をリンクテキストとして自分自身の`routeName()`/`routeParameters()`へ、既定リポジトリには「既定」バッジ、`canManage`かつ非既定の行には「既定にする」ボタン(`setDefault(int $repositoryId)`アクション、`is_default=true`に更新するだけでRepositoryモデルの`updated`フック(check_default)が他リポジトリのis_defaultを自動的にfalseへスイープするため、このアクション自体は対象1行の更新のみで完結)。ヘッダーの操作ボタン群に「リポジトリを追加」リンク(`repository.create`)も追加(canManageの場合のみ)——これで新規追加UIと切替UIの両方が既存の`repository.index`画面(必ず経由する画面)からリンクされ、単独のURL直打ちでしか到達できない状態を解消。**発見した注意点(修正済み)**: `setDefault()`実行後、`$this->repository`(現在表示中のリポジトリを指すプロパティ)が更新前の`is_default`値をメモリ上に保持したままになり、同一リクエスト内で`routeName()`の判定がズレる可能性があったため、`$this->repository = $this->repository?->fresh();`で明示的に再読み込みして解決。テストは`tests/Feature/Repository/RepositoryCreateAndSwitchTest.php`に14件新規(`repository.create`への実HTTPリクエストでの疎通確認、2件目リポジトリの追加+既定フラグが立たないことの確認、識別子バリデーション(純数字/予約語/大文字)の拒否確認3件、空文字→null正規化、識別子確定後は入力欄が出ないことの確認+その状態での再送信が既存値を保ったままエラーにならないことの確認、既定変更の成功確認、非manager権限での既定変更が403になることの確認、切替一覧の表示内容確認、1件のみの場合は一覧テーブル自体が出ないことの確認)。予約語拒否のガード条件は一時的に無効化してテストが実際に失敗することを確認した上で復元済み。フルスイート実行時に`tests/Feature/Repository/RepositoryCreateAndSwitchTest.php`内の1件で`tests/Feature/Repository`共通の既知のgitサブプロセスflaky(”cannot update ref”)が発生したが、単体再実行で再現しないことを確認済み(このスライスの変更とは無関係な既存の環境要因)。**advisorレビューで指摘・対応した2件(2026-07-30)**: (1)`setDefault()`自体は`Repository::class, $this->project`に対して`manage`権限を`authorize()`しており、渡された`repositoryId`も`$this->project->repositories()->whereKey(...)`でプロジェクト範囲にスコープ済みだったため実質的な脆弱性ではなかったが、この権限チェックが「非既定リポジトリを`repositoryParam`付きでマウントした状態」でも同様に効くことを確認するテストが無かった(=このセッションで繰り返し確認してきた「クライアント設定可能な値を受け取る全てのハンドラに権限チェックを効かせること」を、テストで裏付けていない状態だった)——`repositoryParam`付きマウントでの403確認テストを追加。(2)`RESERVED_IDENTIFIERS`(`repository/form.blade.php`)は`routes/web.php`のリテラルセグメントを手動で複製したリストで、両者を結びつけるものが無いため将来ルートにセグメントが増えた際に黙って追従しなくなる恐れがあった——`routes/web.php`の`.repo`ブロック直前に`RESERVED_IDENTIFIERS`への参照コメントを追加(リファクタは不要と判断、コメントでの相互参照のみ)。また、リポジトリが0件のプロジェクトで`repository.edit`から初回リポジトリを作成する経路(`isNew`未設定でも`resolveRepository(null)`がnullを返すため実質的にcreateモードとして機能する既存の分岐)でも識別子入力欄が正しく表示され保存できることを実機テストで確認(既存の分岐で問題なし、コード変更は不要だった)。これでプロジェクトあたり複数リポジトリはスライス1〜2b後半まで全て完了、doneに更新。フルスイート1774/1774・phpstan 0エラー・pintクリーンを最終確認 |
| 非同期チェンジセット取得 | done | `RepositorySyncJob`(ユニーク制約・タイムアウト調整済み) |
| **コミットメッセージのキーワード連動**(`fixes #123`でクローズ、`refs #123`で単純リンク、工数記録`@2h`等) | partial(2026-07-22) | `fixes/fix/closes/close` キーワードを検出し、コミッターがメール/loginで実在ユーザーに一致し、かつそのユーザーが対象プロジェクトで`edit_issues`権限を持つ場合のみ最初の`is_closed`ステータスへ遷移(Journalも記録)。committer欄は攻撃者が任意に詐称可能なため、権限チェックなしでは他ユーザーになりすませてしまう(自動レビューで指摘・修正済み)。一致しない/権限がない場合は従来どおりリンクのみ。**工数記録`@2h`(2026-07-22追加)**: `#123 @2h`形式のトークンを検出し、`commit_logtime_enabled`設定(既定false、設定画面「リポジトリ」節)が有効かつコミッターに`log_time`権限がある場合のみ`TimeEntry`を自動作成。作業分類は`commit_logtime_activity_id`設定(未選択なら既定の作業分類)から解決。Redmineの`TIMELOG_RE`文法の一部のみ対応。**単語形式のトークンを追加対応(2026-07-30)**: `/Users/sesoko/Desktop/workspace/redmine/app/models/changeset.rb`の`TIMELOG_RE`定義を実際に確認したところ、`h`/`m`の単一文字だけでなく`hours?`(hour/hours)・`min`という単語形式も文法上正式に許容されていたため、`2hours`/`2hour`/`2hours30min`/`30min`/`2.5h`(小数時間+末尾h)を追加。**advisorレビューで発見・修正したバグ**: 正規表現の非アンカー版(抽出用)で`h|hours?`の順にalternationを書いており、PCRE左優先マッチにより`2hours`が`2h`のみでマッチしてしまい`ours`が取りこぼされていた(`hours?|h`の順に修正)。ただしこのバグは最終的な`hours`計算値には一切影響しない(`h`は`hours`の接頭辞、`m`は`min`の接頭辞のため、切り詰められても数値結果は偶然一致する)ため、計算結果だけをアサートするテストでは検出不可能と判明 — 抽出正規表現を`LOGGED_TIME_TOKEN_PATTERN`という公開定数として切り出し、生のキャプチャ文字列を直接アサートするテストを追加してこのクラスのバグを再現・検出できることを確認済み(修正前のパターンに戻すと実際にテストが落ちることを確認した上でコミット)。残る未対応は`if_tracker_id`(トラッカー限定オプション)・`done_ratio`(進捗率同時変更オプション)のみ(本アプリのワークフローエンジンとは別体系のため対象外のまま)。進捗率の自動更新は引き続き未実装。**キーワードごとの対象ステータス割り当てを追加(2026-07-24)**: `commit_fixing_keywords`(単一のキーワードリスト+固定の「最初のクローズ済みステータス」)を廃止し、`commit_fixing_keyword_rules`設定(`{keywords, status_id}`の配列)に置き換え。Redmineの`Changeset#fix_issue`と同じく、コミットで実際に使われた**特定のキーワード**が属するルールの`status_id`のみを適用(`Setting.commit_update_keywords_array.detect`相当、同じキーワードが複数ルールに現れる場合は配列の先頭ルールを優先)。設定画面は行の追加/削除が可能な複数行UI(このアプリで初めての「構造化された行のリスト」設定パターン、`RepositorySyncService::fixingKeywordRules()`が未設定時のみ旧来の既定値(`fixes/fix/closes/close`→最初のクローズ済みステータス)にフォールバック)。**Redmine本家との差分**: `if_tracker_id`(トラッカー限定)・`done_ratio`(進捗率同時変更)は対象外(本アプリのワークフローエンジンとは別体系のため)。既にクローズ済みの課題はキーワードに関わらず変更しない(本家の`return if issue.closed?`と同じ、従来の「対象ステータスと異なる場合のみ」という判定から変更) A5-12/A10-02(2026-09-20): コミットルールに`done_ratio`(進捗率)と`if_tracker_id`(トラッカー限定)を追加(同じキーワードの最初に一致するルールが有効、状態だけ/進捗だけのルールも可)。設定`commit_ref_keywords`(既定`*`=キーワードなしの#idも関連付け。Redmineの既定は`refs,references,IssueID`だが従来動作を保つため`*`)と`commit_cross_project_ref`(既定オン=従来どおり。Redmineの既定はオフ)を追加し、オフ時は同一/親/子プロジェクトの課題のみ関連付け・更新・工数記録の対象。コミットログのエンコーディング/履歴表示件数はA5-12bへ。 |
| チェンジセットへの関連課題の手動追加/削除 | done(2026-07-22) | Redmineの`RepositoriesController#add_related_issue`/`#remove_related_issue`相当。新規`manage_related_issues`権限(Repositoryモジュール)を登録、`RepositoryPolicy::manageRelatedIssues`で認可。チェンジセット詳細画面(`repository/show.blade.php`)に「#123」形式(`#`省略可)の入力欄と関連課題ごとの「解除」ボタンを追加。追加時は課題の存在+閲覧権限(`can('view')`、Redmineの`@issue.visible?`相当)+重複を検証。Redmineの`commit_cross_project_ref`設定によるプロジェクトツリー制限は、本アプリのコミットメッセージ自動リンク(`RepositorySyncService`)が元々プロジェクト制限なしで動作しているためそれに合わせて対象外(閲覧可能な課題ならどのプロジェクトでも可、他プロジェクト課題のリンクは所属プロジェクトのURLで生成) |
| コミッター→ユーザーのマッピング | done(2026-07-24) | `Changeset.committer` は引き続き自由文字列だが、`RepositorySyncService`がキーワードコマンド適用時にメール/loginでの一致を試みるベストエフォート解決を追加(2026-07-21)。**専用マッピングUIを追加(2026-07-24)**: 新規`repository_committers`テーブル(`repository_id`+`committer`の組み合わせでユニーク)+`RepositoryCommitter`モデル、新規`repository.committers`ルート(`manage_repository`権限で保護、`repository.form`と同じ権限を再利用)で、コミッター文字列(`Changeset.committer`に表示される生の文字列と完全一致)とユーザーの対応付けを追加/削除できる一覧+フォームを提供。`RepositorySyncService::resolveCommitter()`はこのマッピングをメール/login自動照合より先に確認(完全一致のみ、生文字列を再パースしない)。コミッター文字列はプッシュ権限を持つ誰でも詐称可能なため、マッピングされたユーザーであってもキーワードによる課題クローズ/工数記録は引き続き対象プロジェクトでの実権限(`edit_issues`/`log_time`)保有が前提(既存の自動照合と同じ防御をマッピング経由にも適用) |

### REST API

**現状(2026-07-24): Issues/Versions/Groups/Roles/Trackers/Issue statuses/Issue categories/Issue relations/Enumerations/Memberships/Time entries/Custom fields/Wiki pages/News/Queries/My account/Search/Watchers/添付アップロードが実装済み、Projects/Usersはindex/showのみ**。Redmineは ~20 リソース群を公開、残りはJournalsのみ未実装(対象外項目) — これでREST APIセクションの実装可能な項目は完了。

| Redmine APIリソース | 状態 | 備考 |
|---|---|---|
| Issues | partial(2026-07-24) | GET/POST/PUTに加え、**DELETEを追加(2026-07-24)**: `DELETE /issues/{id}`(`delete_issues`権限、`IssuePolicy::delete()`+`IssueService::delete()`を既存のWeb UI・Webhook経路とそのまま共有、`204 No Content`を返却)。`include=`パラメータ(2026-07-24): `show`(`GET /issues/{id}`)は`journals`/`relations`/`attachments`/`children`/`watchers`の5種、`index`(`GET /projects/{id}/issues`)は`relations`のみに対応(Redmine自体もindexでは`relations`しか対応しないため、その挙動に合わせた)。~~`allowed_statuses`(ワークフローエンジンが必要)・`changesets`(SCM連携が必要)は対象外~~ → **両方を追加(2026-09-20)**: `include=allowed_statuses`(`WorkflowService::allowedTransitions()`+現在のステータス、`{id, name, is_closed}`、編集フォームと同じ一覧)と`include=changesets`(リビジョン/コミッター/コメント/日時、課題のプロジェクトとは別のリポジトリのコミットは`view_changesets`のあるものだけ。Redmineが返す`user`の解決は未対応でSCMのcommitter文字列を返す)。`journals`は非公開ノートを`view_private_notes`権限または本人のみに絞り込み(`issues/show.blade.php`の`visibleJournals()`と同じロジック)。`relations`は双方向(`relationsFrom`/`relationsTo`)を統合し、相手課題を閲覧できない場合はその関係自体を除外(Redmineの`other_issue.visible?`相当、非公開プロジェクトの課題の存在をリレーション経由で推測されないようにする)。`watchers`はRedmineが要求する`view_issue_watchers`権限が本アプリに存在しないため、既存のWeb UI(`issues/show.blade.php`)がウォッチャー一覧を課題の閲覧権限のみで表示している実際の挙動に合わせ、新規権限は追加せず課題の閲覧権限のみで判定(Web/APIで一貫)。~~`children`はRedmine本家の無限再帰と異なり直下の1階層のみ~~ → **`children`を再帰化(2026-09-20)**: 各子に`children`を入れ子で返す(Redmineの`render_api_issue_children`と同じ、階層は子孫を1レベルずつ先読みしてN+1なし、深さ上限25)。**併せて修正**: 従来は閲覧権限のない子課題(非公開のサブタスク等)も題名付きで返していたため、呼び出し元が閲覧可能な子だけに絞った。テスト: `IssueApiIncludeTest.php` **2026-09-20**: (1) **セキュリティ不具合の修正**: `GET /projects/{id}/issues`は`view_issues`の確認のみで課題単位の可視性(`issues_visibility`の`default`/`own`、非公開課題)を適用しておらず、default/own可視性のロールの利用者に他人の非公開課題・無関係の課題を返していた(課題一覧画面・Atom・`show`は正しく制限)。`visibleTo()`を適用して修正、回帰テストを追加。(2) **グローバル`GET /issues`を追加**(Redmineの`/issues.json`): 閲覧可能な全プロジェクトを対象に、プロジェクトごとの可視性ティアを適用(`visibleToAcrossProjects()`、Web版グローバル一覧と同じ)。(3) 両一覧に基本フィルタ(`status_id`=open/closed/`*`/ID、`project_id`、`tracker_id`、`priority_id`、`category_id`、`fixed_version_id`、`parent_id`、`author_id`、`assigned_to_id`(`me`可))と`sort=column[:desc]`(id/subject/created_on/updated_on)を追加、不正値は無視。**Redmineとの差分**: `status_id`省略時はRedmineの「open」ではなく全ステータス(既存クライアントの結果を変えないため)、ページングは従来どおりLaravelの`page`。**未対応**: カスタムフィールド等による高度なフィルタ(`f[]`/`op[]`)、`limit`/`offset`。テスト: `IssueApiIndexTest.php` |
| Projects | partial(2026-07-29) | **追加**: `POST /projects`(トップレベルは管理者専用`create`ポリシー、`parent_id`指定時はその親への`createSubproject`権限、`projects/form.blade.php`の`save()`と同じ認可分岐)、`PUT /projects/{project}`(`edit_project`、`parent_id`を実際に変更する場合のみ新しい親への`createSubproject`を再チェック — Web版save()のコメントと同じ理由で、変更しない限り再チェックしない)、`POST /projects/{project}/{close,reopen,archive,unarchive}`(それぞれ`close`/`archive`ポリシー、Web版`projects/show.blade.php`の同名アクションと同一ロジック)を実装。バリデーションはWeb版と同水準を再現: 自分自身/子孫への付け替え禁止(循環防止)、使用中トラッカーの取り外し拒否(422)、識別子重複禁止。カスタムフィールド値の設定はAPIでは対象外(Version/Group等の既存APIリソースと同じ意図的な簡略化)。~~**DELETEは実装せず**(Redmine本家の`ProjectsController#destroy`相当は本アプリのWeb UI自体にも存在しない)~~ → **DELETEをdone化(2026-07-30)**: `DELETE /projects/{project}`(`api.projects.destroy`)を追加、詳細は本節下部「プロジェクト削除」行参照。**実装レビューで発見・修正した既存バグ**: `Project`モデルに`status`列のDBデフォルト('active')に対応する`$attributes`初期値が無く、生成直後(未リフレッシュ)のモデルで`status`が`null`になっていた(Tracker/Userで既出の「Eloquentは生成直後のモデルにサーバー側カラムデフォルトを反映しない」問題と同種)。Web版はプロジェクト作成後に`show`ページへリダイレクトし再取得するため潜在化していたが、作成直後のモデルをそのまま返すAPIの`store()`で顕在化(`ProjectResource`が`status->value`で例外)。`$attributes = ['status' => 'active']`を追加して修正 — Web側も含め恒久的に解消。**もう1点**: `close`/`archive`等の状態遷移で`$project->update(['status' => ...])`を使うと`status`が`#[Fillable]`に含まれず静かに無視される(保存されない)ことが実機テストで判明、Web版の`setStatus()`と同じ直接プロパティ代入+`save()`に変更。**セキュリティ修正(2026-07-31、push後の自動レビューで発見)**: `UpdateProjectRequest::authorize()`(と、同じ穴を持っていたWeb版`projects/form.blade.php`の`save()`)が、既存のサブプロジェクトを`parent_id: null`で最上位プロジェクトへ「格上げ」する操作を、`edit_project`権限だけで無条件に許可してしまっていた(新規プロジェクトの最上位作成のみ`create`権限を要求する分岐に、既存プロジェクトの再配置ケースが素通りしていた)。Redmine本家の`Project#allowed_parents`(`@allowed_parents << nil`が`user.allowed_to?(:add_project, nil, :global => true)`の場合のみ、自分が既に親無しの場合の無変更ケースを除く)を確認し、両経路とも「`parent_id`を`null`へ実際に変更する場合は`create`権限(本アプリでは管理者専用)を要求」するよう修正。テストを追加(Web: `tests/Feature/Projects/SubprojectManagementTest.php`、API: `tests/Feature/Api/V1/ProjectApiTest.php`、各2件——非管理者は拒否・管理者は許可) |
| REST APIのカスタムフィールド(`custom_fields`) | done(2026-09-20、A11-10。`include=attachments`の共通化は対象外) | 応答に`custom_fields: [{id, name, value}]`(複数値は`multiple: true`と値の配列。値は保存値=列挙型は選択肢ID、真偽は`1`/`0`)、作成/更新は`custom_fields: [{id, value}]`(`App\Support\Api\CustomFieldPayload`)。役割で見えない項目は出さず、編集不可の項目と未知のIDは無視、形式違反・作成時の必須漏れは422で何も保存しない。課題/工数/プロジェクト/バージョン/グループ/ユーザー(管理者と本人のみ)が対象。課題の一覧はトラッカーごとに項目を解決するため、トラッカーが多様だと一覧のクエリ数が増える。テスト: `CustomFieldsApiTest.php` |
| Users | partial(2026-07-24) | `GET /users`・`GET /users/{id}`のみ(読み取り専用、`GroupController`と同じ管理者限定パターン — `UserPolicy`は全アクション`false`を返し`Gate::before`の管理者バイパスのみで許可)。POST/PUT/パスワード/`is_admin`変更は対象外(`is_admin`はモデルの`#[Fillable]`から意図的に除外済みで、書き込み系APIを実装する際は別途慎重な設計が必要なため今回は見送り)。**Redmine本家との差分**: 本家は`index`のみ管理者限定で`show`は`@user.visible?`(本人または公開設定次第で誰でも閲覧可)、さらに管理者/本人/一般で応答フィールドを出し分ける(`admin`/`mail`/`status`/`api_key`等)が、本アプリの`UserPolicy`はindex/showともに管理者限定のため、本家のような差分のあるフィールド出し分けは存在せず`UserResource`は常に単一の形状。`name`はRedmineの`firstname`/`lastname`分割ではなく単一フィールド(本アプリのスキーマの既存差分)。カスタムフィールド値は他のAPIリソースと同様に対象外。`?status=`(`active`/`registered`/`locked`)・`?name=`(name/email部分一致検索)のオプションフィルタに対応(既存Web UIの管理画面自体にはフィルタUIがなく、本家のindex APIパラメータに倣った新規追加)。**書き込みと可視性の段階を追加(2026-09-20、A11-07)**: `POST /users`・`PUT /users/{id}`・`DELETE /users/{id}`(管理者のみ。ユーザーフォームと同じ検証、`is_admin`は明示的に設定、ディレクトリ認証のユーザーはパスワード不要、`must_change_passwd`対応。削除は匿名化で、自分自身と最後の有効な管理者は拒否)。`GET /users/{id}`は公開プロフィールの可視性(`User::isVisibleTo`)に従い、見えなければ404。応答項目は、全員にid/login/name/last_login/日時と(`hide_mail`でなければ)email、管理者と本人に管理者フラグ/言語/通知設定、管理者にstatus/認証方式/パスワード変更日時、本人にapi_key。**Redmineとの差分**: firstname/lastnameは`name`1つ、`send_information`(アカウント情報メール)と`generate_password`は未対応、カスタムフィールドは未対応(A11-10)。テスト: `UserApiTest.php` |
| Time entries | done(2026-07-24) | `GET/POST /projects/{project}/time_entries`・`GET/PUT/DELETE /time_entries/{id}`。`VersionController`と同じ形(`TimeEntryService`委譲、既にWebhookペイロード用に実装済みの`TimeEntryResource`をそのまま流用)。~~Redmine本家は`/issues/{issue}/time_entries`への課題ネストルートと、プロジェクト非依存のトップレベル`GET /time_entries`も公開するが、本アプリの他リソースに課題ネストルートの前例がなく、他の一覧もすべてプロジェクトスコープのため両方とも対象外~~ → **両方を追加(2026-09-20)**: `GET /time_entries`(閲覧可能な全プロジェクト、プロジェクトごとの`time_entries_visibility`ティアを`visibleToAcrossProjects()`で適用)、`GET /issues/{issue}/time_entries`(課題の閲覧+工数の閲覧権限、own可視性を適用)、`POST /issues/{issue}/time_entries`(課題と`log_time`が必要、本文の`issue_id`/`project_id`は無視して課題側を採用、`edit_time_entries`が無ければ記録者は常に自分)。全一覧に`project_id`/`issue_id`/`activity_id`/`user_id`(`me`可)/`from`/`to`フィルタ(不正値は無視)。ロール別の可視性は従来のプロジェクト一覧も含め正しく適用済みであることを確認。テスト: `TimeEntryApiTest.php`。**`time_entries_visibility`(own)のindex/show反映**: 既存Web UI(`time-entries/index.blade.php`)の`filteredTimeEntriesQuery()`と同じく`AuthorizationService::timeEntryVisibilityFor()`が`Own`を返す場合は`user_id`で絞り込み(`show`は`TimeEntryPolicy::view()`が既に同じロジックで単体判定)。**`user_id`の扱い**: 本家の`log_time_for_other_users`権限は本アプリの権限体系に存在しないため、既存Web UIと同じく`edit_time_entries`を「自分以外への記録」の権限としても再利用。省略時は常に本人IDを既定値とする(`edit_time_entries`保有者が省略した場合にNULL制約違反になっていたバグを実装直後の実機検証で発見・修正)。`user_id`バリデーションはプロジェクトメンバーシップにスコープしない(`exists:users,id`のみ) — Web UIの「他者への記録」ドロップダウンとは異なり、本人の既定値は非メンバーロールや管理者バイパス経由で`log_time`を保有するケースでも常に成立する必要があるため。`comments`は本家に合わせ1024文字上限(上限のない既存Web UIフォームより厳格) |
| Versions | done(2026-07-24) | GET(一覧/単体)/POST/PUT/DELETE。`VersionController`+`Store`/`UpdateVersionRequest`は既存の`IssueController`と同じ形(FormRequestで認可+バリデーション、`VersionService`委譲、201/204ステータス)。`VersionResource`はWebhookペイロード用に既に実装済みのものをそのまま流用。`sharing`のバリデーションは`Version::allowedSharings()`(管理者以外は`system`共有を選択不可などの既存ルール)をWebフォームと共通で適用 |
| News | done(2026-07-24) | `GET/POST /projects/{project}/news`・`GET/PUT/DELETE /news/{id}`。`IssueCategoryController`と同じ形(専用Serviceクラス無し、モデル直接操作)。~~**グローバル一覧は対象外**: Redmine本家は`GET /news.json`でproject_id省略時に全プロジェクト横断一覧を返すが、本アプリの他のREST APIリソースは全てindexがproject-nested限定のため、一貫性を優先しグローバル一覧は対象外~~ → **`GET /news`を追加(2026-09-20)**: `view_news`のあるプロジェクトのお知らせを新しい順に返す(Web版`news.global-index`と同じく、プロジェクトごとの可否を判定してからSQLでページング)。任意の`project_id`で絞り込めるが閲覧不可のプロジェクトへは広がらない。`comments_count`は先読みで行ごとのクエリなし。テスト: `NewsApiTest.php`(Web UI側の`news.global-index`はプロジェクト単位の可視性をクライアント側で解決する別実装であり、APIでの再現はスコープ外)。レスポンスは本家の`author`/`project`ネストオブジェクトではなく本アプリの既存Resource群と同じ扁平な`author_id`/`project_id`。`comments_count`は`withCount('comments')`で付与(該当カラムなし)。~~**コメント機能は対象外**: 本家の`POST/DELETE /news/:id/comments`相当は今回追加せず、件数のみ`comments_count`で開示~~ → **コメントAPIを追加(2026-09-20)**: `POST /news/{news}/comments`(`comment_news`権限、`content`必須、Web版と同じ`NewsCommentCreated`イベントでメール通知/Webhookに連動)と`DELETE /news/{news}/comments/{comment}`(`manage_news`、コメント作成者に特別な権限はなし — Redmineの`comments#destroy`と同じ、他のお知らせ経由では404)。`comments_count`は従来どおり一覧・詳細で開示。テスト: `NewsApiTest.php`。バリデーションは既存Web UIフォーム(`news/form.blade.php`)の`title`max:255をそのまま踏襲(本家はmax:60)。添付ファイルのインラインアップロード・`?include=attachments`は対象外 |
| Memberships | done(2026-07-24) | `GET/POST /projects/{project}/memberships`・`GET/PUT/DELETE /memberships/{id}`(ルートパラメータ名`membership`を`App\Models\Member`へ暗黙バインド、公開名とモデル名の不一致はRedmine本家の`MembersController`/`Member`も同じ)。新規`MemberPolicy`を追加、Redmine本家が`index`/`show`/`create`/`update`/`destroy`すべてを単一の`manage_members`権限でゲートする(専用の「閲覧のみ」権限が存在しない)のに合わせ、全アクションを同一権限でゲート。**ロール割り当ての権限スコープ制御**: 既存のWeb UI(`projects/members.blade.php`の`addMember()`)が実装済みの「担当外ロールは無視して既存値を保持」ロジック(Redmineの`Member#set_editable_role_ids`相当、`AuthorizationService::managedRolesFor()`を利用)をAPIコントローラの`syncManagedRoles()`に移植・共通化: 送信された`role_ids`のうち担当範囲外のIDは422で弾かず黙って無視し(Web UIはチェックボックス自体を出さないため弾く先がないが、APIは任意ID送信を受け付けるため明示的な無視が必要)、結果として最終ロール集合が空になる場合のみ422。**削除時のガード**: Redmine本家の`Member#deletable?`(自分の担当ロールの範囲内でしか削除できない)を移植、本アプリに`inherited_from`相当の概念(グループ経由の継承ロール)がないためその半分は対象外、担当範囲外のロールを持つメンバーの削除は422で拒否。ユーザー/グループの二者択一は`user_id`/`group_id`の`required_without`+`prohibits`で検証(モデル層の`Member::booted()`の`LogicException`はDB整合性の最終防衛線、通常はリクエストバリデーションで弾く)。更新時は`role_ids`のみ受け付け、メンバーの身元(user_id/group_id)は作成後不変(既存Web UIと同じ、Redmine本家のPUTは`user_id`を受け取っても機能上無視するのと実質同義)。レスポンス形状はRedmine本家のネスト`{project:{id,name}, user:{id,name}, roles:[{id,name}]}`ではなく、本アプリの既存リソース(`IssueCategoryResource`/`VersionResource`/`GroupResource`)と同じ扁平な`project_id`/`user_id`/`group_id`/`role_ids`を踏襲 |
| Groups | done(2026-07-24) | GET(一覧/単体)/POST/PUT/DELETE。`GroupPolicy`が全操作を拒否しGate::before管理者バイパスのみで許可する設計のため、`viewAny`/`view`含む全アクションを明示的に認可(`TrackerController`の「index無条件公開」とは異なる)。`user_ids`配列で所属メンバーを作成/更新時に一括同期(Redmine本家の`safe_attributes=`(`user_ids=`)と同じ完全置き換え方式、~~本家が別途持つ`POST /groups/:id/users`のような追加専用のネストエンドポイントは対象外 — 既存コントローラにネスト型メンバーシップの前例がないため~~ → **追加/削除専用エンドポイントを実装(2026-09-20)**: `POST /groups/{group}/users`(`user_id`/`user_ids`、既に所属・存在しないユーザーは無視、1件も追加できなければ422 — Redmineの`add_users`)、`DELETE /groups/{group}/users`(`user_id`/`user_ids`、所属者が1人もいなければ404 — `remove_users`)、`DELETE /groups/{group}/users/{user}`(従来形式、`remove_user`)。いずれも管理者のみ。テスト: `GroupApiTest.php`)。カスタムフィールド値の公開は他のAPIリソース(Issue/Version/IssueCategory)と同様に対象外 |
| Roles | done(2026-07-24) | GET(一覧/単体)のみ、`TrackerController`/`IssueStatusController`と同じ形(認証のみをゲートとし、管理者限定の`RolePolicy`はバイパス — Redmine本家の`RolesController`も`index`/`show`ともに`require_admin_or_api_request`)。一覧は本家の`index.api.rsb`と同じ`Role.givable`相当(既存の`Role::scopeGivable()`を再利用、`builtin`のAnonymous/NonMemberロールを除外)、詳細取得は本家の`find_role`同様フィルタなし(builtinロールもid指定なら取得可)。本家はindex/showでレスポンス形状が異なる(indexは`id`/`name`のみ)が、本アプリの既存リソースはどれもindex/showで同一形状を返す慣習のため踏襲せず、両方とも`id`/`name`/`assignable`/`issues_visibility`/`time_entries_visibility`/`permissions`を返す。`users_visibility`は`Role.users_visibility`(2026-07-30実装)を`RoleResource`が**2026-09-20に露出**(Redmineの`roles/show.api.rsb`と同じ3種の可視性設定を返す、`tests/Feature/Api/V1/RoleApiTest.php`で検証) |
| Trackers | done(2026-07-24) | GET(一覧/単体)のみ(Redmine本家もPOST/PUT/DELETEなし、トラッカーは管理画面専用)。本家の`TrackersController`は`index`のみ`require_admin_or_api_request`(APIリクエストなら管理者でなくてもアクセス可、Web UIは管理者限定)という特殊な認可を持つため、既存の管理者限定`TrackerPolicy`(Web CRUD用)は使わず、ルートの`auth:api,api-key`ミドルウェア(認証のみ)だけをゲートとして実装。プロジェクトによる絞り込みなし、ページネーションなし(本家の`Tracker.sorted.to_a`と同じ、全件返却) |
| Issue statuses | done(2026-07-24) | GET(一覧/単体)のみ、`TrackerController`と全く同じ形(認証のみをゲートとし、管理者限定の`IssueStatusPolicy`はバイパス — Redmine本家の`IssueStatusesController`も`index`だけ`require_admin_or_api_request`で誰でもAPI経由アクセス可、書き込み系APIは本家にも存在しない)。本家は`show`アクション自体が存在しないが、`TrackerController`が既に確立した「本家より一歩踏み込んでshowも提供する」前例を踏襲 |
| Issue categories | done(2026-07-24) | GET(一覧/単体)/POST/PUT/DELETE。直前に実装した`VersionController`と全く同じ形(FormRequestで認可+バリデーション)だが、`IssueCategory`には対応するServiceクラスがない(既存のWeb側`issue-categories/form.blade.php`も`IssueCategory::create()`/`->update()`を直接呼ぶのみ)ため、コントローラも同様にモデルを直接操作。単一の`manage_categories`権限のみで一覧/詳細/作成/更新/削除すべてを制御(Versionのような`view_files`/`manage_versions`の分離はない) |
| Issue relations | done(2026-07-24) | `GET`/`POST /issues/{issue}/relations`・`DELETE /relations/{relation}`(削除のみRedmine本家と同じくフラット、課題のどちらか一方に従属するものではないため)。バリデーションは既存のWeb UI(`issues/show.blade.php`の`addRelation()`)の全ルールをそのまま`StoreIssueRelationRequest`に移植(自己関連禁止、重複禁止、`cross_project_issue_relations`設定によるプロジェクト間制限、祖先/子孫関係の除外、`relates`の逆重複、`blocks`の直接循環、`copied_to`は手動指定不可)。`index`は`manage_issue_relations`ではなく課題の`view`権限のみで許可(本家は`index`含む全アクションに管理権限を要求するが、本アプリの課題APIは`?include=relations`で既に閲覧者へ関連を公開済みのため、単独エンドポイントだけ厳格化すると矛盾する。`IssueResource::visibleRelations()`を`public`化しindex/`include=relations`の双方で同一の可視性フィルタを共有)。`destroy`は関連する双方の課題いずれかで`manage_issue_relations`を持てば削除可(本家の`IssueRelation#deletable?`と同じ「片側で十分」という判定、既存Web UIの`deleteRelation()`より広い — 単一課題にスコープされないフラットなルートには「現在の課題」という文脈がないため) |
| Enumerations | done(2026-07-24) | `GET /enumerations/{issue_priorities,time_entry_activities,document_categories}`のみ(Redmine本家もwrite系APIなし、列挙値の管理は管理画面専用)。`TrackerController`/`IssueStatusController`/`RoleController`と同じ「auth-onlyゲート」パターン(認証のみをゲートとし、管理者限定の`EnumerationPolicy`はバイパス — Redmine本家の`EnumerationsController`も`index`のみ`require_admin_or_api_request`)。本家は`GET /enumerations/:type`という動的な`:type`セグメント1本(`Enumeration.get_subclass`でSTIサブクラスを解決)だが、本アプリの他の読み取り専用リソースに動的セグメントの前例がないため、本家が実際に公開する3つの固定URL(issue_priorities/time_entry_activities/document_categories)に対応する3本の固定ルート/アクションとして実装(任意文字列を受け付ける汎用`{type}`ワイルドカードは避けた)。一覧は`project_id`が`null`の行のみ(本家の`shared`スコープと同じ、TimeEntryActivityのプロジェクト別上書き行はトップレベルの列挙値としては含めない)を`position`順で返却。非activeな行も本家の無フィルタ一覧と同様そのまま含める。レスポンス形状は本家の`index.api.rsb`と同じ`id`/`name`/`is_default`/`active`のみ(`position`/`type`/カスタムフィールド値は含めない) |
| Custom fields | done(2026-07-24) | `GET /custom_fields`のみ(Redmine本家も`index`のみ、`show`アクション自体が存在しない)。Redmine本家は`require_admin`が`index`にも無条件適用される(`:except => :index`が無い)ため、Tracker/IssueStatus/Role/Enumerationsの「auth-onlyゲート」パターンとは異なり、本家APIでも管理者限定 — `GroupController`と同じ`Gate::authorize('viewAny', CustomField::class)`(管理者専用の`CustomFieldPolicy`+`Gate::before`バイパス)を採用。~~レスポンス形状は本家の`index.api.rsb`から`description`/`is_for_all`/`is_filter`/`visible`/`default_value_mode`を省略(本アプリにこれらのカラム自体が存在しないため)~~ → **本家の形状に揃えた(2026-09-20)**: `is_filter`(A11-13で列追加)・`description`(新規`custom_fields.description`列、フォームの入力欄と入力欄下の補足表示も追加)・`is_for_all`(プロジェクト指定なし)・`visible`(ロール制限なし)を追加。`projects`・`trackers`(課題フィールドのみ)と`roles`(課題/プロジェクト/バージョンのフィールド)を本家と同じ`{id, name}`配列で追加、既存の`tracker_ids`/`role_ids`は互換のため残置。関連は一括で先読み。`default_value_mode`は従来から出力済み。テスト: `CustomFieldApiShapeTest.php`。`possible_values`は本家と同じ`{value,label}`配列形状(`CustomField::format()->options()`が既にlist/enumeration形式向けに同じvalue=>labelマッピングを提供、他形式は空配列)。`trackers`/`roles`は本家の`{id,name}`ネストオブジェクトではなく、本アプリの既存リソース(`GroupResource`の`user_ids`/`MembershipResource`の`role_ids`)と同じ扁平な`tracker_ids`/`role_ids`配列を踏襲。~~`projects`(IssueCustomField限定)は対象外~~ → 上記の通り2026-09-20に追加 |
| 添付ファイル(アップロードAPI) | done(2026-07-24) | Redmineの`POST /uploads.json`相当を実装: 生のオクテットストリームボディを送信すると`{upload: {id, token}}`を返し、そのトークンを課題作成/更新APIの`uploads: [{token, filename?, description?}]`配列に含めることで添付ファイルとして紐付けられる(Redmineの`Issue#save_attachments`と同じ二段階フロー)。**アーキテクチャ上の制約**: Spatie MediaLibraryの`media`テーブルは`model_type`/`model_id`が非nullのため、Redmineの`Attachment#container`のように「所有者なしで一時保存」ができない。新規`PendingUpload`モデル(`HasMedia`実装、`pending`コレクション)を一時的な入れ物として用意し、トークン(`{id}.{uuid}`、`uuid`列はSpatieが自動生成する既存の列を流用、Redmineの`id.digest`と同じ「id単体では推測不可能」という性質を持つ)を経由して`Media::move()`で実際の対象モデルへ物理ファイルを再割り当て(コピーではなく移動)。未消費のまま残ったペンディングアップロードは新規`PruneExpiredPendingUploadsJob`(`routes/console.php`に`hourly()`で登録)が1時間経過後に削除。トークンが不明/失効している場合はRedmine本家と同様にその添付だけをスキップしリクエスト全体は失敗させない。認可はRedmineの`authorize_global`相当(ログインのみ要求、プロジェクト権限は課題作成/更新時点の`add_issues`/`edit_issues`で別途担保) **添付ファイルAPIを追加(2026-09-20、A11-06)**: `GET /attachments/{id}`(Redmineの`render_api_attachment`相当: ファイル名・サイズ・種別・説明・作成者・ダウンロード数など)、`GET /attachments/{id}/download`(APIトークンで取得、ダウンロード数を加算)、`PUT|PATCH /attachments/{id}`(`description`・`filename`、名前は許可拡張子の再検証とパス除去付き)、`DELETE /attachments/{id}`(課題の添付は履歴に削除を記録)。認可は**添付先モデルのポリシー**(読み取り=view、変更/削除=update)で、ポリシーの無い対象(保留中のアップロード等)は403。課題のペイロードの`attachments`も同じリソースで`author`・`download_url`・`downloads`を返す(アップロード者は1回のクエリでまとめて解決)。テスト: `AttachmentApiTest.php` **Filesモジュール API を追加(2026-09-20、A11-05)**: `GET /projects/{id}/files`(プロジェクトと各バージョンのファイル、バージョン付きなら`version`、作成者・ダウンロード数付き、`view_files`が必要)と`POST /projects/{id}/files`(`POST /uploads`のトークンを、トップレベルまたはRedmine形式の`file: {…}`で受け取り、`version_id`があればそのバージョン(他プロジェクトのバージョンは404)に添付、`manage_files`が必要、`filename`は拡張子の再検証付き、トークンは1回限り)。`POST`は201とファイル情報を返す(Redmineは本文なしの204)。**併せて強化**: Files モジュールの添付は`/attachments/{id}`系でもプロジェクトの閲覧権限ではなく`view_files`/`manage_files`で判定する。**未対応**: Redmineの`digest`(ファイルのハッシュ)。共有ロジックは`PendingUploadAttacher`に切り出し(課題のAPIも同じ)。テスト: `FileApiTest.php` |
| Wiki pages | done(2026-07-24) | `GET/POST /projects/{project}/wiki`・`GET/PUT/DELETE /wiki/{id}`。**ルーティングの意図的な逸脱**: Redmine本家はタイトルをURLセグメントとして扱い(`/wiki/{title}.json`)、PUTがcreate-or-updateを兼ねる(新規なら201+本文、既存なら200+空`<ok/>`)が、本アプリは既存Web UI自体のルーティング規約(`Volt::route('/projects/{project:identifier}/wiki/{wikiPage}', ...)`、数値ID route-model-binding、タイトルではない)に合わせ、このAPIリソースも数値IDで扱い、store/updateを他リソースと同じ通常のREST動詞に分離(本家のcreate-or-updateセマンティクスは踏襲しない)。既存の`WikiPageService`(バージョニング込み)・`WikiPagePolicy`をそのまま再利用。レスポンスは`WikiPageResource`(Webhookペイロード用、メタデータのみ)と新規`WikiPageDetailResource`(本文/コメント込み、show/store/update用)の2形状に分割 — 本家の`index.api.rsb`(タイトル/親/バージョン/日時のみ)と`show.api.rsb`(+本文/コメント/著者)の使い分けと同じ。**`?version=N`**: 特定バージョンの本文取得に対応、本家は`view_wiki_edits`という別権限を要求するが本アプリの`WikiPagePolicy`にはWiki編集履歴専用の権限が存在しないため、通常の閲覧権限(`view_wiki_pages`)のみで判定。**保護ページ**: `WikiPagePolicy::update()`の`is_protected`+`protect_wiki_pages`チェックをそのまま適用。**リネーム制限**: 既存Web UIフォームの`canRename`と同じく、タイトル/親ページの変更は`rename_wiki_pages`権限保有時のみバリデーションルールに追加(無権限での送信は無視、拒否ではなく単に無視されるフィールドとして扱う)。**循環防止**: 既存フォームの`availableParents()`のBFS除外ロジックをFormRequest内に移植(自分自身/子孫を親に指定するリクエストを422で拒否)。添付ファイル(`?include=attachments`)は対象外(他のAPIリソースと同様、カスタムフィールド値含め本文以外の周辺情報は最小限に留める既存方針を踏襲) |
| Queries | done(2026-07-24) | `GET /queries`のみ(Redmine本家の`QueriesController#index`と同じく、保存済みクエリの一覧のみ — `id`/`name`/`is_public`/`project_id`、フィルタ/カラム/ソート条件の実行やCRUDは対象外)。`?type=`(`issue`省略時既定/`time_entry`)・`?project_id=`(省略時はグローバル一覧、`Query::visibleGlobally()`)に対応、本家は`type`をSTIサブクラス選択に使うが本アプリは`type`カラム+Enumで代用(レスポンス本文には本家同様`type`を含めない)。既存の`Query::visibleIn()`/`visibleGlobally()`(Public/Roles/Private可視性、既存Web UIの保存済みクエリ一覧と同一ロジック)をそのまま再利用。**プロジェクト単位の権限ゲートを追加**: `Query::visibleIn()`自体は本家の`Project.allowed_to_condition`(`view_permission`)相当のプロジェクト単位閲覧権限チェックを行わないため(Web UI側は呼び出し元の課題一覧画面が既にゲート済みで問題にならないが、独立したAPIエンドポイントではそうならない)、`project_id`指定時は`type`に応じて`view_issues`/`view_time_entries`をコントローラ側で明示的に`Gate::authorize`する。`project_id`省略時(グローバル)は認証のみ(本家は匿名ユーザーも閲覧可能だが、本アプリのAPIは`auth:api,api-key`ミドルウェアで元々認証必須のため踏襲せず) |
| Journals | done(2026-07-29、訂正あり) | `PUT /journals/{journal}`のみ(Redmine本家の`JournalsController`が`resources :journals, :only => [:edit, :update]`でルーティングし、`destroy`アクション自体を持たないのと一致 — 実チェックアウト`/Users/sesoko/Desktop/workspace/redmine`で確認済み。Journalは課題更新の副作用としてのみ作成され`POST /journals`は存在せず、`index`/`show`も本家含め無い。読み取りは課題APIの`?include=journals`側で対応済み)。認可は今回新設した`App\Policies\JournalPolicy`(`edit_own_issue_notes`/`edit_issue_notes`権限、Web UI側の個別Journal編集(§1参照)と完全共有)をそのまま再利用 — コントローラは薄いパススルーのみで新規ロジックなし。同じポリシー経由のため、Web版実装時に修正した可視性ゲート(課題の閲覧権限・非公開ノートの`view_private_notes`)もAPI側に自動的に効く。**訂正**: 当初`DELETE /journals/{journal}`も実装したが、Redmine自体に該当機能が無いこと(§1「個別 Journal の編集」行参照)が判明し撤去。`notes`は空文字での更新を許容(`sometimes`+`nullable`)、これがRedmineにおける実質的な削除操作に相当。`private_notes`フラグの切替は対象外(Web UIの編集フォームも本文のみを編集対象としている既存方針に合わせた)。**`private_notes`(`set_notes_private`権限者のみ有効、無ければ黙って無視=Redmineのsafe_attributes)を受け付け、レスポンスに`updated_by_id`を追加(2026-09-20)** |
| Watchers | done(2026-07-24) | `POST /issues/{issue}/watchers`(`user_id`をボディで指定)・`DELETE /issues/{issue}/watchers/{user}`。Redmine本家の`WatchersController#create`/`#destroy`と同じ「CRUDリソースではないアクション専用エンドポイント」形(レスポンスは本家の`render_api_ok`と同じ204 No Content、ボディなし)。追加/削除とも既存の`IssuePolicy::manageWatchers()`(`add_issue_watchers`権限)で認可 — 本家は`add_issue_watchers`/`delete_issue_watchers`を動的に使い分けるが、本アプリの`IssuePolicy`は元々1つの権限を両方に再利用する設計(既存のWeb UIの`addWatcher()`/`removeWatcher()`と同じ)のため、本家にしかない権限を新設せず踏襲。追加対象はプロジェクトメンバーに限定(`Rule::exists('members', 'user_id')`、Web UIと同じ検証) |
| Search | done(2026-07-24) | `GET /search`(全プロジェクト横断)・`GET /projects/{project}/search`(プロジェクト単位)。既存の`SearchService`(issue/wiki-page/news/document/message/changeset/projectの7種を横断検索、`RESULTS_PER_TYPE=20`で種別ごとに上限)をそのまま再利用 — 新規ロジックはコントローラの「閲覧可能プロジェクト集合の解決」のみで、既存Web UIの`search.global-index`/`search.index`Volt コンポーネントの`visibleProjects()`/`searchableProjects()`計算プロパティと全く同じ実装(`Project::query()->get()->filter(fn ($p) => $user->can('view', $p))`)。**このアプリで初めてEloquentモデルではない値オブジェクト(`App\Support\Search\SearchResult`)をラップするAPIリソース** — `id`/`project_id`を持たないDTOのため(本家Redmineの`id`フィールドに相当するものがない)、`url`(対象への絶対URL)のみで代替、7種すべての検索メソッドを改修してID/project_idを持たせる改修は本項目のスコープ外として見送り。レスポンス形状は本家の`event_*`接頭辞ではなく本アプリの既存リソース群と同じ扁平な`type`/`title`/`url`/`description`/`updated_at`。**ページネーションなし**: `SearchService`自体が種別ごとに20件上限(計算上最大140件)で全件を一度に返す設計で、既存Web UIの2つのVolt コンポーネントもページネーションなし全件表示のため、それに合わせてAPIも本家のoffset/limit方式を導入せず全件返却。クエリパラメータは`q`/`all_words`/`titles_only`/`open_issues`に加え、グローバル側は`scope=my_projects`(本家の`scope`パラメータ相当)、プロジェクト側は`subprojects=1`(本家の同名オプション相当、nested setの`_lft`/`_rgt`範囲で子孫プロジェクトを解決)。本家の`attachments`検索対象トグルは本アプリの`SearchService`に添付ファイル検索自体が存在しないため対象外。**セキュリティ修正(同コミットに含む)**: このAPI実装のレビュー中に、`SearchService::searchIssues()`が`issues_visibility`(Own/Defaultティア)と`is_private`のどちらもチェックしていなかったことが判明 — `IssuePolicy::view()`や課題一覧の`Issue::scopeVisibleToAcrossProjects()`では正しく適用されているこれらの可視性制御が、検索(Web UI・API両方で共有される`SearchService`自体)には元々存在しなかったため、Own可視性ロールのユーザーが他人の課題を検索結果で閲覧できてしまい、非公開課題も著者/担当者以外に漏洩していた。`searchIssues()`の最終結果構築クエリに`->visibleToAcrossProjects($viewer, ...)`を追加して修正(サブジェクト/説明文の全文一致・カスタムフィールド一致のどちらの経路で見つかったIDも、最終的にこの1箇所で可視性フィルタを通るため、修正点はここ1箇所で十分)。既存Web UIの`search.index`/`search.global-index`も同じ`SearchService`を呼ぶため、この修正はWeb側の同じ漏洩も同時に修正する A7-12(2026-09-20): `attachments`パラメータ(0/1/only)対応。 A9-06(2026-09-20): `scope=bookmarks`対応。 |
| Projects(続き、advisorレビュー2026-07-29) | done | **`is_public`未指定時のデフォルト欠落を修正**: `POST /projects`で`is_public`を省略すると、`status`と同じ「未リフレッシュのモデルはDBカラムデフォルトを読み返さない」問題により、レスポンスの`is_public`が`null`になっていた(かつ`default_projects_public`設定が無視されていた)。`projects/form.blade.php`の`mount()`と同じく、省略時は`Setting::get('default_projects_public', true)`にフォールバックするよう`store()`を修正。回帰テスト追加済み。**アーカイブ済みプロジェクトの保護を確認**: レビューで「`close`/`update`ルートがroute model bindingのみでアーカイブ状態を再チェックしていないのでは」という懸念が出たが、調査の結果`AuthorizationService::can()`が権限キーに関わらず`$project->isArchived()`を最初に判定して常に拒否する(Redmineの`Project#allows_to?`と同じ設計)ため、`close_project`/`edit_project`権限を持つメンバーであってもアーカイブ済みプロジェクトへの`PUT`/`POST .../reopen`は既に403で拒否される — 追加の実装は不要だったが、この事実を固定する回帰テストを追加(直接叩いて確認) |
| My account | partial(2026-07-24) | `GET/PUT /my/account`(Redmine本家の`MyController#account`相当、`require_login`のみでゲート、対象は常に自分自身)。`UserResource`をそのまま流用(`UserController`のような管理者限定ゲートは適用せず — 自分自身のレコード閲覧/編集に管理者権限は不要)。**Redmine本家との差分**: 本家はPUTにsudo mode要求・OAuthセッションでの変更禁止という追加制約を持つが、本アプリにはsudo mode/OAuth区別の仕組みがWeb側にも存在しないため対象外。フィールドは~~既存Web UIと同じくname/emailのみ(パスワード変更・2FA・通知設定・カスタムフィールドは対象外)~~ → **2026-09-20に拡張**: 通知設定`mail_notification`(6種の列挙、検証あり)・`no_self_notified`を更新可能にし、応答に両方と**本人のみ**`api_key`を追加(Redmineは管理者にも他人のキーを見せるが、本アプリは本人だけ)。`POST /my/api_key`でキーを再生成(古いキーは無効、本人のキーのみ対象)。**未対応**: パスワード変更(Redmineの`PUT /my/account`も受け付けず別画面`my/password`)・2FA・カスタムフィールド・言語(本アプリにi18n基盤なし)。テスト: `MyAccountApiTest.php`。バリデーションは他の`Update*Request`と同じく`sometimes`による部分更新に対応(既存Web UIフォームの`required`とは異なるが、本アプリの他のUpdate系APIエンドポイントの確立された規約に合わせた)。`is_admin`はモデルの`#[Fillable]`から除外済みのため、ペイロードに含めても無視される(実機検証で確認済み)。既存の`GET /user`(生のモデルダンプ、認証疎通確認用の最小ルート、`ApiKeyAuthTest`が依存)とは別物として維持 |

**API認証**: ~~OAuth2(Passport)のみ~~ → **done**(2026-07-24)。Redmineの3つの受付経路(`key=`クエリパラメータ、`X-Redmine-API-Key`ヘッダー、HTTP Basic認証のユーザー名欄にAPIキーを指定)すべてに対応する軽量な代替認証手段を追加。実装は`users.api_key`列(nullable、unique、40桁16進文字列 = Redmineの`Redmine::Utils.random_hex(20)`と同じ形式)+ `Auth::viaRequest('api-key', ...)`で登録した新規`api-key`ガード(`AppServiceProvider::registerApiKeyGuard()`)。`routes/api.php`の各ルートは`auth:api,api-key`でPassport/APIキーの両ガードを併用(Laravelの`Authenticate`ミドルウェアが先に解決した方を採用)。キーの表示/再生成はアカウント設定画面(`profile.index`)の新セクションから、2FAのリカバリーコード再生成と同じ`requirePasswordConfirmation()`ゲート・`wire:confirm`パターンを踏襲。Redmine本家のような`Token`モデル経由の`secure_compare`ではなく、単純なユニークインデックス検索(このアプリの他の認証情報検索と同じ簡略化、160ビットの鍵空間に対するタイミング攻撃は現実的でないため)。~~`rest_api_enabled`設定(本家にはあるがこのアプリのAPIには元々存在しない)による有効/無効切り替えは対象外のまま。~~ → **done**(2026-07-30)。新規`App\Http\Middleware\EnforceRestApiEnabledSetting`(`rest-api.enabled`エイリアス)を`routes/api.php`の`auth:api,api-key`より前段に配線 — Redmine本家(`config/settings.yml`の`rest_api_enabled: default: 0`)と同じく既定は無効、有効な資格情報であっても認証を試みる前に403で拒否する(`ApplicationController#find_current_user`が`Setting.rest_api_enabled?`をAPIキー/OAuth解決の前提条件にしているのと同じ設計)。APIキー・Passport OAuth2どちらの認証経路にも一律適用されることをテストで確認済み。**既存API自動テストへの影響**: この設定の既定値を無効のままにすると既存25ファイル・264件のAPIテストが軒並み403で落ちるため、`tests/Pest.php`の`Feature`バインディングに`beforeEach(fn () => Setting::set('rest_api_enabled', true))`を追加してテストスイート全体の既定値を`true`に上書き(本番のデフォルトはRedmine同様`false`のまま) — 個々のテストファイルへの追記ではなく1箇所での対応とした。当初`tests/TestCase::setUp()`(Unit/Feature共通の基底クラス)に置いていたが、`RefreshDatabase`を使わない`Unit`テストにまでDB書き込みが漏れる問題をadvisorレビューで指摘され、`Feature`バインディングのみに限定した。**本番環境への影響(アップグレードパスの注意点)**: このアプリのREST APIキー機能自体は2026-07-24に導入済みで、`profile.index`のセルフサービスAPIキー再生成UIを通じて既にキーが発行・利用されている可能性がある。今回`rest_api_enabled`の既定値をRedmine本家に忠実な「無効」にしたことで、既存の有効なAPIキー/Passportトークンを持つ利用者であっても、管理者がこの設定を明示的に有効化するまでは全APIリクエストが403で拒否される(本家と同じ挙動だが、既存キー利用者にとっては新規の破壊的変更点)。

### 拡張性(プラグイン/フック/Webhook/受信メール)

| 機能 | 状態 | 備考 |
|---|---|---|
| プラグイン登録面(権限/アクティビティ/ダッシュボードブロック/カスタムフィールド形式/メニュー項目/ビューフック) | done | `PluginManager` が既存レジストリに委譲。**`registerPlugin()`を追加(2026-07-29)**: 従来`Plugin`(メタデータ値オブジェクト)は`::metadata()`という命名の静的メソッド止まりで、アプリ側からは一切呼ばれていなかったが、今回`PluginManager::registerPlugin(Plugin $plugin, array $settingsDefaults = [])`を追加し、プラグインの`boot()`から呼ぶことで実際に登録・一覧化されるようになった(詳細は下の「プラグイン設定UI・永続化」行参照)。Redmineの`Redmine::Plugin.register :id do name ...; author ...; version ...; settings :default => {...}, :partial => '...' end`という単一DSL呼び出し(`/Users/sesoko/Desktop/workspace/redmine/lib/redmine/plugin.rb`で確認)に相当。**破壊的変更**: `Plugin`のコンストラクタに`id`(必須、第1引数)を追加 — プラグイン一覧表示のキー、および設定の永続化キー(`plugin_{id}`)として使う安定識別子で、表示用の自由文字列`name`とは独立させた(Redmine本家も`Redmine::Plugin.register`のシンボル引数`id`とブロック内`name`ブロックを別概念として持つ)。現状このAPIを呼ぶのはテストフィクスチャ(`tests/Fixtures/Plugins/SamplePlugin`)のみのため実害はないが、`PluginManager`のクラスdocが「プラグイン向けの表面は極力変更しない」と明記している箇所の変更である点を記録 |
| ランタイムでのプラグイン検出 | missing(意図的、第一段階) | `bootstrap/providers.php` への手動登録。計画書で明記済みのスコープ |
| プロジェクトモジュールのプラグイン拡張 | missing(意図的) | `ProjectModuleKey` はコンパイル時Enum。`PluginManager` のdocblockに明記 |
| クエリフィルタ演算子のプラグイン拡張 | missing(意図的) | `IssueFilterFieldRegistry` 等に `register()` が存在せず、そもそも呼び出し箇所もない(Phase 2由来の別の既存ギャップ) |
| プラグイン設定UI・永続化 | partial(2026-07-29) | 管理画面に`/plugins`(登録済みプラグイン一覧: 名前/バージョン/作者)・`/plugins/{id}/settings`(設定編集フォーム、`manage_settings`と同じ`SettingPolicy::manage()`の管理者専用ゲートを流用)を新設。永続化は既存の`Setting`ストア(`plugin_{id}`キーでJSON配列を保存、Redmine本家の`Setting.define_plugin_setting`/`plugin_#{plugin.id}`キーと同じ命名規約、`/Users/sesoko/Desktop/workspace/redmine/app/models/setting.rb`で確認)をそのまま流用、新規テーブルは追加せず。設定フォームは各キーの初期値の型(bool→チェックボックス、int→数値として保存、それ以外→文字列)から自動的にフィールド種別を推定して描画する汎用エディタとして実装 — Redmine本家が持つ`settings :partial => '...'`(プラグイン独自のBladeパーシャルを差し込む機構)は本アプリには存在せず、全プラグイン共通のこの汎用エディタのみが提供される、意図的な簡略化。`array_intersect_key()`でプラグインが宣言していないキーの保存を拒否(未知キーの注入防止)。**advisorレビューで発見・修正したバグ**: (1) `save()`にmount()と同じ管理者権限+プラグインID有効性の再チェックが無く、クライアント操作可能な`pluginId`プロパティを未登録IDに書き換えて`save`を呼ぶと空の設定行が作成できてしまっていた(権限昇格ではないが規約破り)、mount()と同じガードを追加して修正。(2) int型の既定値(例: `max_items => 10`)が保存のたびに文字列`"10"`へ変質する型変換バグを修正、bool/int/stringの3分岐に変更。プラグインの一覧表示自体(「ランタイムでのプラグイン検出」行が指す自動検出)は引き続き対象外のまま — 一覧に載るのは`registerPlugin()`を明示的に呼んだプラグインのみ **独自の設定ビューに対応(2026-09-20)**: Redmineの`settings :partial => '...'`に相当する`Plugin::$settingsView`(Bladeビュー名、任意)を追加。指定すると汎用エディタの代わりにそのビューが`$values`/`$pluginId`付きで描画され、入力は`wire:model="values.<キー>"`で結び、保存は従来と同じ型推定の`save()`を通る。ビューが存在しなければ汎用エディタにフォールバック(プラグインの綴り間違いでページが壊れない)。テスト: `PluginSettingsTest.php`(フィクスチャ`tests/Fixtures/Plugins/CustomViewPlugin`) |
| ~~プラグインのバージョン依存チェック~~ → **done**(2026-07-30) | Redmineの`Redmine::Plugin.requires_redmine(:version_or_higher => '...')`(`lib/redmine/plugin.rb`で確認、要件未達時に`PluginRequirementError`を送出)を参照実装として移植。本アプリにはリリース/タグの概念が無いため、新規`config/plugins.php`の`core_version`(既定`'1.0.0'`、`APP_CORE_VERSION`環境変数で上書き可、比較対象の暫定基準値)を追加。`PluginManager::registerPlugin()`が`version_compare($coreVersion, $plugin->requiresCoreVersion, '<')`で判定し、要件未達なら新規`App\Support\Plugins\PluginRequirementException`を送出してプラグイン自体もsettingsDefaultsも登録しない(Redmine同様、部分登録を許さないハード失敗)。ガードを一時的に外し、テストが実際に失敗することを確認した上で復元。テストは`tests/Feature/Plugins/PluginRequirementTest.php`に4件(現行以下の要求は登録成功、完全一致は登録成功、現行より新しい要求は例外+未登録、要求未達時はsettingsDefaultsも漏れないことの確認) |
| コントローラ/モデルのライフサイクルフック | partial(2026-09-20、A12-05) | ビュー描画フック(`<x-hook>`)に加え、サービスが保存後に発火する20種のLaravelイベントを`App\Support\Plugins\LifecycleHooks::catalog()`にフック名(`issue.created`等)と説明つきで一覧化し、`PluginManager::onLifecycle($hook, $listener)`で名前指定の購読ができる(未知の名前は有効な名前の一覧つきで例外)。`app/Events`のイベントが一覧から漏れると落ちるテストあり。**Redmineとの差分**: 保存前フック(`controller_issues_edit_before_save`等、保存の可否や値を変えられるもの)は無く、通知専用。テスト: `LifecycleHooksTest.php` |
| **Webhook**(~~Redmineコア機能ではない、本アプリでの追加機能~~ → Redmine 7.0.0はコアに持つ、2026-09-20訂正) | done(範囲限定、2026-07-22) | Issue作成/更新/**削除**イベントに対応(削除を追加)。`App\Events\IssueDeleted`+既存の`DispatchWebhooksForIssueEvent`リスナー(union型を`IssueCreated\|IssueUpdated\|IssueDeleted`に拡張)+`WebhookServiceProvider`の`Event::listen`配列に追加。従来2箇所に直書きされていた`$issue->delete()`(課題詳細画面の単体削除、一覧画面の一括削除)を新規`IssueService::delete()`に統一し、単一のディスパッチ経路に集約。**Wikiページ作成/更新/削除イベントを追加(2026-07-22)**: `WikiPageService`が唯一の書き込み経路であることを活かし、`create()`/`update()`にイベント発火を追加、新規`WikiPageService::delete()`(Issueと同じ「削除前にディスパッチ」順序)に`wiki/show.blade.php`の直書き`$this->wikiPage->delete()`を統一。`App\Events\WikiPage{Created,Updated,Deleted}`+新規`DispatchWebhooksForWikiPageEvent`リスナー+新規`App\Http\Resources\Api\V1\WikiPageResource`(Webhookペイロード専用、REST APIルートへの追加は範囲外)。管理画面のイベント選択チェックボックスは`WebhookEvent::cases()`を直接列挙しているため追加のUI変更は不要(ブラウザで新規イベント3件がチェックボックスに表示されることを確認済み)。**工数記録の作成/更新/削除イベントを追加(2026-07-23)**: 新規`App\Services\TimeEntryService`(`create()`/`update()`/`delete()`、Issue/WikiPage向けサービスと同じく認可は一切行わずイベント発火のみ、`delete()`は削除前にディスパッチする同じ順序規約)を抽出し、従来5箇所(`time-entries/form.blade.php`の新規作成・編集保存、`issues/form.blade.php`の課題フォーム内「工数を記録」欄、`ImportTimeEntriesJob`のCSV行インポート、`RepositorySyncService`のコミットキーワード`@Nh`工数記録、`time-entries/index.blade.php`の単体削除・一括編集・一括削除)に直書きされていた`TimeEntry::create()`/`->update()`/`->delete()`をすべてこのサービス経由に統一。`App\Events\TimeEntry{Created,Updated,Deleted}`+新規`DispatchWebhooksForTimeEntryEvent`リスナー+新規`App\Http\Resources\Api\V1\TimeEntryResource`(Webhookペイロード専用)。**バージョンの作成/更新/削除イベントを追加(2026-07-23)**: 新規`App\Services\VersionService`(同じく認可なし、イベント発火のみ)を抽出し、`versions/form.blade.php`の新規作成・編集保存、`versions/index.blade.php`の削除の3箇所を統一。**`Project::closeCompletedVersions()`(バージョン一覧の「完了したバージョンをクローズ」一括操作)もこのサービス経由に変更**(従来`$version->update()`を直書き、モデルからサービスを呼ぶ初めてのケースだが、同ファイルが既に`AuthorizationService`を`app()`解決で参照する慣習の書き込み版として許容)、一括クローズされた各バージョンでも`version.updated`が正しく発火することを確認。`App\Events\Version{Created,Updated,Deleted}`+新規`DispatchWebhooksForVersionEvent`リスナー+新規`App\Http\Resources\Api\V1\VersionResource`。プロジェクトイベントは、`ProjectService`自体が存在せず削除UI自体もない(ネストセット絡みで意図的に未実装)ため引き続き対象外 — 承認済みスコープになった時点で着手 **Newsイベントを追加(2026-09-20)**: Redmine 7.0.0で`acts_as_webhookable`なのはIssue/News/TimeEntry/Version/WikiPageの5モデル(`app/models/*.rb`で確認)で、本アプリに欠けていたNewsの`news.created`/`news.updated`/`news.deleted`を`NewsUpdated`/`NewsDeleted`イベントと`DispatchWebhooksForNewsEvent`(`NewsResource`ペイロード)で追加、Webhook管理画面のイベント一覧にも自動で出る。**併せて修正**: REST APIの`POST /projects/{id}/news`は`NewsCreated`を発火せず作成者のウォッチも作らなかった(APIで投稿したお知らせはメール通知されなかった)ため、Web版と同じにした。**未対応**: Redmineの所有ユーザー・可視性判定・`use_webhooks`権限(バックログA12-02)。テスト: `NewsWebhookTest.php`。**所有ユーザーと可視性(2026-09-20、A12-02/A13-07)**: `webhooks.user_id`(任意の所有ユーザー)、権限`use_webhooks`、設定`webhooks_enabled`(既定オン=従来どおり)。所有者付きのWebhookは、所有者が有効なユーザーで、プロジェクトで`use_webhooks`を持ち、対象を閲覧できるイベントだけ送信(`Webhook::deliverableFor()`、5種の配信リスナー共通)。所有者なしの従来のWebhookは従来どおり全件。管理画面のフォームに所有ユーザー、設定に有効/無効。**未対応**: ユーザー自身が自分のWebhookを管理する画面(A12-02b)。テスト: `WebhookOwnerTest.php` |
| 受信メールによる課題作成 | done(2026-07-22) | `[識別子]` プレフィックスでのプロジェクト振り分け、送信者=既存ユーザーの権限チェック。**追加**: Redmineの`MailHandler#receive_issue_reply`相当を実装し、件名が`[... #123]`形式の場合は新規課題作成ではなく既存課題#123へのコメント追加として処理(`edit_issues`権限で認可、添付ファイルも同様に既存課題へ追加)。~~本アプリはまだ送信メール通知を実装していないため件名を自動生成する経路は無い~~ → 送信メール通知は2026-07-29〜30に実装済み(2026-09-19訂正)。`In-Reply-To`ヘッダー経路のみ未対応(バックログA6-06)。**Webサービス受信と正規表現オプション(2026-09-20、A12-06/A5-10の一部)**: `POST /mail_handler`(`MailHandlerController`、設定`mail_handler_api_enabled`+`mail_handler_api_key`で保護、CSRF対象外、キーの比較は定数時間で空キーは一致しない)。`key`と生メール`email`を受け取り、IMAP/POPと同じ処理(`IncomingMailService::processRawMessage()`)で、受理なら201、不受理・不正な本文なら422、キー不一致・無効なら403。設定に`mail_handler_enable_regex_delimiters`/`mail_handler_enable_regex_excluded_filenames`(切り捨て行と除外ファイル名を正規表現として扱う。不正な正規表現は何にも一致しない)。**未対応**: `allow_override`によるキーワード上書きの許可リスト、カスタムフィールドのキーワード、`project_from_subaddress`、`default_group`、`no_account_notice`/`no_notification`(A12-06b)。テスト: `MailHandlerApiTest.php` |
| 受信メールでの`unknown_user`/`no_permission_check`相当 | missing(意図的) | メールからのアカウント自動作成を意図的に非対応としている(セキュリティ上の判断としてコード内に明記) |
| ~~メール返信による課題更新~~ | **done**(2026-07-30、チェックリスト記載修正) | 上の「受信メールによる課題作成」行がこの機能自体は既に実装済みと記録しているにもかかわらず、本行が別途「missing」のまま残っていた記載漏れ。`IncomingMailService::receiveIssueReply()`(件名`[... #123]`一致、`edit_issues`権限チェック、コメント追加、添付ファイル)は`tests/Feature/Mail/IncomingMailServiceTest.php`に46件のテストで検証済み、全件パス。**advisorレビューで指摘**: 本行の元の記述が謳っていた「再オープン」について、既存テストはクローズ方向(オープン→クローズ)のステータス変更しか検証しておらず、逆方向(クローズ→オープン)は未検証のまま「done」と記載しようとしていた。個別テスト(「a Status keyword line on a reply reopens a closed issue」、`is_closed=true`の課題に対する返信の`Status: Reopened`キーワード行で`is_closed`がfalseに戻ることを確認)を追加して実際に検証済み。Redmine本家の`MailHandler::ISSUE_REPLY_SUBJECT_RE`(`app/models/mail_handler.rb`で確認、`%r{\[(?:[^\]]*\s+)?#(\d+)\]}`)も件名の正規表現一致が主経路で、`In-Reply-To`ヘッダーはJournal通知への返信という別経路(このアプリはまだ送信メール通知自体が無いため対象外)でのみ使われるため、件名ベースの実装のみで本家の実用上の挙動と一致している A6-06(2026-09-20): 課題通知メールにRedmineと同じ`Message-ID`(`redmine.issue-<id>.<作成日時>.<宛先ユーザーID>@<mail_fromのドメイン、なければhost_name、なければ<hostname>.redmine>`。更新メールは`redmine.journal-…`で`References`に課題のID)を付与し、受信側は`In-Reply-To`/`References`の`redmine.(issue|journal)-<id>`を先に解釈(件名の`[… #123]`は次点)。`MessageIdentity`。ヘッダーが指す未対応の対象(wiki等)は無視して新規課題を作らない。
| **メール本文のキーワードコマンド**(`Status: Closed`, `Assigned to:`, `Priority:` 等) | partial(2026-07-24) | `status`/`priority`/`assigned to`/`done ratio`/`tracker`/`category`/`fixed version`/`start date`/`due date`/`estimated hours`/`private`の11キーワードを実装(`IncomingMailService::extractKeywordAttributes()`、tracker/category/fixed versionは2026-07-23、**start date/due date/estimated hours/privateを追加(2026-07-24)**)。新規課題作成・返信コメントの両方で動作し、マッチした行は本文/コメントから除去(Redmineの破壊的な行除去と同じ挙動)。`status`/`priority`はプロジェクト非依存の全体名で大文字小文字を区別せず照合、`assigned to`はメールアドレスまたはフルネームでプロジェクトの割当可能ユーザーから照合、`done ratio`は0〜100の整数のみ受理。`tracker`はプロジェクトに紐づくトラッカーのみから名前一致、`category`/`fixed version`はプロジェクト内のカテゴリ/バージョンから名前一致。`start date`/`due date`は`YYYY-MM-DD`形式のみ受理(`DateTime`によるラウンドトリップ検証、2月30日のような繰り上がりも拒否)。`estimated hours`は手動フォームと同じ0〜9999.99の範囲。`private`は送信者が対象プロジェクトで`set_issues_private`権限を持つ場合のみ反映(CSVインポートの`is_private`列と同じ権限判断、手動フォームが権限のないユーザーにチェックボックス自体を出さないのとも同じ扱い)。本アプリにi18n基盤が無いため、Redmineのように`Setting.default_language`に応じてキーワードラベル自体が翻訳されることはなく、固定の英語ラベルのみ対応。値が解決できない場合はその行を本文にそのまま残す(Redmineの`try(:id)`によるnil黙殺と異なり、送信者に何が反映されなかったか分かるようにする設計判断)。**`parent issue`キーワードを追加(2026-07-24)**: `parent_id`を設定(Redmine本家の`MailHandler#issue_attributes_from_keywords`の`parent_issue_id => get_keyword(:parent_issue)`相当)。値は`#123`形式・`123`形式のどちらも受理(非数字を除去して解析)。手動フォームの`parent_id`バリデーションと同じ2つの制約を移植: (1) 同一プロジェクト内の課題のみ許可、(2) 自分自身/子孫を親に指定する循環を拒否 — 手動フォーム(`issues/form.blade.php`)の親子チェックはLivewireコンポーネントのインラインクロージャで再利用不可なため、代わりに課題関連の循環検出でも使われている既存の`Issue::descendantIds()`を再利用(新規作成時は`$issue`が存在せず子孫を持ち得ないため、このチェック自体をスキップ)。ラベルは他のキーワードと同様Redmine本家の英語UIラベル("Parent task")をそのまま踏襲せず、本アプリの属性名に合わせた"parent issue"を採用(`done ratio`が本家の"% Done"と一致しないのと同じ、既存の非直訳の慣習に合わせた)。特別な権限ゲートは追加せず、他の一般属性キーワードと同じく`add_issues`/`edit_issues`(呼び出し元で既にチェック済み)のみで反映。カスタムフィールドキーワード、および`allow_override`相当の設定は引き続き未対応(コミットメッセージの`@Nh`工数記録キーワードと同様、意図的に絞り込んだサブセット) |

---

## 6. 次にトラッキング表を更新するタイミング

- 新しいフェーズ/機能追加のたびに該当行を `missing`/`partial` → `done` に更新する。
- 意図的にスコープ外とした項目は、その理由をコード側のdocblock/コメントに残し、このドキュメントの備考にも「意図的」である旨を明記する(既に実施済みの例: `PluginManager` のプロジェクトモジュール/フィルタ演算子除外、受信メールの `unknown_user` 非対応)。
- このドキュメント自体の生成方法: `/Users/sesoko/Desktop/workspace/redmine` の実装(コントローラ/モデル)と `/Users/sesoko/Desktop/workspace/artisan-pm` の実装を突き合わせる並列調査(5系統: 課題管理/管理機能・認証/コンテンツモジュール/クエリ・工数・ダッシュボード/SCM・API・拡張性)を実施し、その結果を統合した。
